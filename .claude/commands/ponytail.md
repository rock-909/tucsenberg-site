---
name: ponytail
description: Switch Ponytail intensity through the marketplace skill
user-invocable: true
---

Use the installed `ponytail:ponytail` skill with `$ARGUMENTS` if it is
available. If it is unavailable, apply the project policy in `CLAUDE.md`:
default to `full`; accept `lite`, `full`, `ultra`, and `off`; prefer no new
code, existing project code, standard library, native platform features, and
installed dependencies, in that order.
