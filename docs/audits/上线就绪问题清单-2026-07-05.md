# 上线就绪审查问题清单（2026-07-05）

> 供执行 agent（Codex）读取执行。审查基线：`main @ faeecac`，工作区干净。
> 每项都带证据（file:line）、修复建议、修后验证命令。执行前先读
> `CLAUDE.md`、`docs/项目基础/上线验证.md`、`docs/项目基础/部署.md`。

## 审查结论（TL;DR）

代码质量与构建链**全部绿**（见文末"已验证为绿"）。剩余问题集中在
**"绑定正式域名"这一步的配置切换**（4 项 P0）、**买家可见的内容欠账**
（TODO 标记、裂图、假社交链接、starter OG 图）、以及一批 starter 残留清理。

**总计 24 项**：P0 ×4，P1 ×9，P2 ×6，P3 ×5。
其中 Codex 可直接执行 15 项；其余 9 项阻塞在 owner 数据 / Cloudflare 仪表盘 / 外部服务，Codex 只能准备不能闭环。

## 执行约束（Codex 必读）

1. **不要提前把 `https://tucsenberg.com` 写进 baseUrl**。守卫测试
   （`tests/architecture/tucsenberg-site-contract.test.ts:245-256`）和
   `docs/项目基础/部署.md:51-55` 故意禁止在 owner 签核域名前使用正式域。
   Wave 3 的域名切换 PR 只能在 owner 明确签核后合并。
2. **GitHub Flow**：所有改动走 feature 分支 + PR，不直推 main。
3. 改 `messages/base/**` 后必须跑
   `tsx scripts/starter-profile/sync-message-compat.ts --write` 同步 compat，
   再用 `pnpm content:check` 验证新鲜度。
4. 删除文件用 `git rm`（走 PR，可从 git 历史恢复），不用 `rm`。
5. `pnpm build` 与 `pnpm website:build:cf` 写同一个 `.next`，不得并行。

---

## Wave 1 — Codex 现在就能做（不需要 owner 输入）

### C4 · Organization JSON-LD 输出假的示例社交账号 — P1

- **证据**（Confirmed by static evidence）：`messages/base/en/critical.json:224-225`
  与 compat 产物 `messages/en/critical.json:224-225`：
  `"twitter": "https://x.com/example"`、`"linkedin": "https://www.linkedin.com/company/example"`。
  消费链：`src/lib/structured-data.ts:83-84` → `src/lib/structured-data-generators.ts:109-116` 直接进 `sameAs`。
  对照 `src/config/single-site.ts:58-61` social 为空字符串（正确姿态），但 message 层假链接优先生效。
- **影响**：全站向搜索引擎声明两个不存在的社交主页。
- **修复**：清空 `structured-data.organization.social` 两个 key；让
  `generateOrganizationData` 过滤空值，`sameAs` 为空时省略整个字段；同步 compat。
- **验证**：`pnpm content:check && node scripts/starter-checks.js translations`；
  页面源码确认 `sameAs` 不含 example。

### C5 · 首页 OG 分享图仍是 starter 技术栈宣传图 — P1

- **证据**（Confirmed）：`src/lib/seo-metadata.ts:162,171,227` home/default 用
  `/images/og-image.jpg`，该图文案是 "Modern B2B Enterprise Web Platform — Next.js 16 · React 19…"。
  品牌 OG 图 `public/images/tucsenberg-og.png`（1200x630）已存在并配置在
  `src/config/single-site.ts:253`（`brandAssets.ogImage`），但无任何 metadata 消费。
  其余页面不设 image → 无 og:image。
- **影响**：首页分享到 WhatsApp/LinkedIn（本站买家主渠道）时预览卡片是框架宣传语。
- **修复**：`seo-metadata.ts` 的 home 与 default image 统一指向 `brandAssets.ogImage`。
- **验证**：`pnpm test`（seo-metadata 相关）+ 页面源码检查 `og:image`。

### C7 · 文档 proof 命令用错 profile，漏掉真实错误 — P2（C2 的结构性根因）

- **证据**（Confirmed by execution）：`docs/项目基础/内容.md:59` 写的是
  `content-readiness --profile company-site` → 跑出来全绿；
  但激活 profile 是 **catalog**（`src/config/active-starter-profile.ts:4-5`、
  `src/config/starter-profiles.ts:80`），`--profile catalog` 跑出 1 个 error（即 C2）。
- **修复**：把 `docs/项目基础/内容.md` 和 `上线验证.md` 的 proof 命令改为
  `--profile catalog`；理想修法是让脚本缺省读取 active profile，消除这类漂移。
- **验证**：按新文档命令执行能捕获 C2。

### T10 · 非 production 环境没有 noindex 保护 — P2

- **证据**（Confirmed）：`src/app/robots.ts:12-23` 无条件 `allow: "/"`；
  `src/app/[locale]/layout-metadata.ts:32-42` 无条件 `robots: { index: true }`。
  preview workers.dev 站点公开可收录，上线后会与正式域形成重复内容。
- **修复**：按 `APP_ENV !== "production"` 输出 `X-Robots-Tag: noindex, nofollow`
  并在 robots.ts 同步 disallow。
- **验证**：`curl -I https://tucsenberg-site-preview.faints-pudgier-9r.workers.dev | rg -i x-robots-tag`。

### T12 · 严格上线门识别不了 workers.dev / example.invalid 残留 — P2

- **证据**（Confirmed）：`scripts/quality/checks/production-config.js:143` 的
  starter-marker 正则不匹配 `workers.dev` 和 `example.invalid`——上线时若忘改域名，
  `PUBLIC_LAUNCH_STRICT` 照样绿。
- **修复**：marker 正则补 `\.workers\.dev|example\.invalid`（Wave 3 改域名前先落这条，
  让门先能兜住）。
- **验证**：production-config 相关单测 + 手跑严格门确认能报错。

### T6 · 严格上线门没接入部署工作流 — P1

- **证据**（Confirmed）：`.github/workflows/cloudflare-deploy.yml:95` production
  路径只跑 `pnpm release:verify`，release manifest 不含 `validate-production-config`；
  `rg PUBLIC_LAUNCH_STRICT .github/` 零命中。可以绕过严格门直接部署 production。
- **修复**：在 production 部署 step 之前加
  `PUBLIC_LAUNCH_STRICT=true APP_ENV=production node scripts/starter-checks.js validate-production-config`
  （复用已注入的 secrets env 块）。
- **验证**：工作流单测（`tests/unit/workflows/`）+ dispatch 干跑确认缺 secret 时该 step 红。

### P3 清理批（可合并为一个 starter 残留清理 PR）

| ID | 问题 | 证据 | 修复 |
| --- | --- | --- | --- |
| T14 | CSP img-src 残留 `images.unsplash.com`、`via.placeholder.com`（含 C10），connect-src 含浏览器用不到的 `https://api.resend.com` | `src/config/security.ts:81-91`、`next.config.ts:90-97` | 移除三项；remotePatterns 同步清理 |
| C8 | `public/` 内无引用 starter 资产（`og-image.svg` 内含 example.com 字样、`about-og.jpg`、`hero/showcase-hero.svg`、空 `blog/` 目录） | `public/images/og-image.svg:37` 等 | 确认 `rg` 零引用后 `git rm`；`profile-fixtures/catalog/products/sample-product-a.svg` 是运行时 fallback（`src/components/products/family-section.tsx:35`），等 C3 解决后再动 |
| C9 | MDX 内链带尾斜杠，全部经 308 跳转 | `content/pages/en/contact.mdx:19`、`flood-barrier-materials-guide.mdx:49-73` 等 | 内链统一去尾斜杠；`rg -n "\]\(/[a-z-]+/\)" content/pages/en` 归零 |
| C11 | robots disallow 引用不存在的 `/error-test/` | `src/config/single-site-seo.ts:160-164` | 删除该项 |
| C12 | 激活 message 包残留未消费的 "Under Construction" 文案 | `messages/en/critical.json:115-130` | 清理并同步 compat |
| C13 | home/products/request-quote 的 sitemap lastmod 钉死 2026-04-26 | `src/config/pages.config.ts:7`、`single-site-seo.ts:25` | 更新为当前定稿日期 |

Wave 1 总验证：`pnpm content:check && pnpm test && pnpm build`。

---

## Wave 2 — 需要 owner 提供数据（Codex 拿到数据后执行）

### C2 · 产品规格表出现买家可见的 "TODO-OWNER" — P1

- **证据**（Confirmed by execution）：
  `node scripts/starter-checks.js content-readiness --profile catalog` → 1 error；
  `src/constants/product-specs/tucsenberg-product-lines.ts:173`
  `["Section weight", "TODO-OWNER", "52 kg +/-5%"]`，渲染在 `/products/flood-tube-dams` 规格表。
- **需要 owner**：TB-TD500（5 m 段）的重量数据。
- **修复**：填真实值；暂无数据则改 "on request" 类文案，不留 TODO 标记。
- **验证**：`node scripts/starter-checks.js content-readiness --profile catalog` 回 0 error。

### C3 · 五个产品详情页的产品图全部指向不存在的文件 — P1

- **证据**（Confirmed）：`tucsenberg-product-lines.ts:25,71,115,157,199` 引用
  `/images/products/tb-*-placeholder.svg`，但 `public/images/products/` **目录为空**。
  `src/components/products/family-section.tsx:33-35` 因 images 非空不走 fallback，
  五个产品页首屏裂图；404 URL 还进了 ProductGroup JSON-LD
  （`src/app/[locale]/products/[market]/market-jsonld.ts:29-34`）。
- **需要 owner**：产品实拍图（或批准的临时品牌占位图）。
- **修复**：图片落位 `public/images/products/` 保持文件名；同时把
  `src/config/single-site.ts:250-252` `productPhotos.status` 从 `pending` 改 `ready`
  （这是严格门必查项之一，见 T5）。
- **验证**：`pnpm test` + 本地打开 5 个 `/products/*` 页确认无裂图。

### T5 · 严格门仓库层必红三项：电话 / 产品照片 / 法务复核 — P1

- **证据**（Confirmed by execution + static）：本次实跑严格门报错；
  `src/config/single-site.ts:175` `phone: ""`；`:250-252` `productPhotos: pending`；
  `PUBLIC_LAUNCH_LEGAL_CONTENT_REVIEWED` 全仓无处设置
  （门逻辑 `scripts/quality/checks/production-config.js:365-388`）。
- **需要 owner**：公开电话号码（或明确决策"不公开电话"）；产品照片确认（同 C3）；
  对 about/contact/privacy/terms 内容的法务/事实复核签字。
- **修复**：电话有则填、没有则按决策记录调整门逻辑；photos 同 C3；
  复核后在部署环境导出 `PUBLIC_LAUNCH_LEGAL_CONTENT_REVIEWED=true`。
- **验证**：`PUBLIC_LAUNCH_STRICT=true APP_ENV=production node scripts/starter-checks.js validate-production-config`
  仓库层错误清零（secrets 类错误在真实环境消）。

### C6 · 联系页 WhatsApp 条目不可操作 — P2

- **证据**（Confirmed）：`content/pages/en/contact.mdx:23` 只写 `@Tucsenberg (business account)`，
  无号码无 `wa.me` 链接，买家无法发起会话。
- **需要 owner**：WhatsApp 商业号码（或决定砍掉该渠道）。
- **修复**：改为 `[WhatsApp](https://wa.me/<号码>)`；顺手把 email 写成显式
  `[sales@tucsenberg.com](mailto:...)`。
- **验证**：本地渲染 `/contact` 点击链接可跳转。

---

## Wave 3 — 域名绑定 PR（owner 签核正式域后一次性执行）

> 这四项是同一个 PR：不做，绑定域名当天站点就是错域名/无绑定。
> 前置决策：owner 确定主域（apex `tucsenberg.com` 还是 `www`，见 T11）。

### T1/C1 · wrangler production 仍是占位域名 `example.invalid` — P0

- **证据**（Confirmed，双 lane 独立确认 + 本次抽查）：`wrangler.jsonc:43-45`
  `NEXT_PUBLIC_BASE_URL` / `NEXT_PUBLIC_SITE_URL` = `https://tucsenberg-site-production.example.invalid`。
  消费链：`src/app/robots.ts:6,21`、`src/app/sitemap.ts:34`、
  `src/app/[locale]/layout-metadata.ts:23`（canonical/OG）、
  `src/lib/structured-data-generators.ts:17,99,101`。
- **影响**：canonical、sitemap、robots、OG、JSON-LD 全部指向 `example.invalid`，收录报废。
- **修复**：两个 vars 改为签核后的正式域。

### T2 · wrangler 没有任何 production route / custom domain — P0

- **证据**（Confirmed）：`wrangler.jsonc` 全文无 `routes` / `custom_domain` / `workers_dev` 键。
- **修复**：`env.production` 增加
  `"routes": [{ "pattern": "tucsenberg.com", "custom_domain": true }, { "pattern": "www.tucsenberg.com", "custom_domain": true }]`，
  并加 `"workers_dev": false`（配合 T10 防 workers.dev 重复收录）。
  前提：tucsenberg.com zone 已在部署所用 Cloudflare 账号（owner 动作）。
- **验证**：`pnpm exec wrangler deploy --dry-run --env production` 输出含 custom domain 绑定。

### T3 · 部署工作流构建时不注入正式域，浏览器包会内联 preview 域名 — P0

- **证据**（Confirmed + 本次抽查）：`cloudflare-deploy.yml:113-134` 构建 env 块
  没有 `NEXT_PUBLIC_SITE_URL` / `NEXT_PUBLIC_BASE_URL`；
  `src/config/single-site.ts:54-56` fallback 是 preview workers.dev 域名；
  Next.js 构建期内联 `NEXT_PUBLIC_*` 进浏览器 bundle。
- **影响**：即使 wrangler 运行时 vars 改对，预渲染页面和客户端 JS 里的站点 URL 仍是 preview 域名。
- **修复**：给证明链和构建两个 step 按环境注入这两个变量
  （production 用 repo variable 如 `CLOUDFLARE_PRODUCTION_URL`）。
- **验证**：部署后 `curl -s https://tucsenberg.com | rg -o 'workers\.dev|example\.invalid'` 为空。

### T4 · 守卫测试当前故意禁止 tucsenberg.com，必须在同一 PR 同步更新 — P0

- **证据**（Confirmed）：`tests/architecture/tucsenberg-site-contract.test.ts:245-256`、
  `tests/unit/workflows/ci-preview-env.test.ts:56-59`。
- **修复**：同 PR 更新期望值。注意 CI preview 守卫保留——CI 构建继续用 preview URL
  是文档要求，只有 deploy workflow 的 production 路径换正式域。
- **验证**：`pnpm exec vitest run tests/architecture/tucsenberg-site-contract.test.ts tests/unit/workflows/ci-preview-env.test.ts`。

### T13 · 部署后验证只打 workers.dev，不打正式域 — P2（随 Wave 3 一起）

- **证据**（Confirmed）：`cloudflare-deploy.yml:166-171` 只从输出抓 workers.dev URL 跑 smoke。
- **修复**：workflow 加可选 `production_url` 输入再跑一轮 deployed-smoke，
  或上线后手动 `node scripts/starter-checks.js deployed-smoke --base-url "https://tucsenberg.com"`。

---

## Wave 4 — Owner / Cloudflare 仪表盘 / 外部服务动作（Codex 无法执行，列出供跟踪）

| ID | 动作 | 说明 |
| --- | --- | --- |
| — | tucsenberg.com zone 接入部署所用 Cloudflare 账号 | Wave 3 的前提 |
| T11 | 决策主域 www vs apex，建 Redirect Rule 301 收敛 | P2；`TURNSTILE_ALLOWED_HOSTS` 已同时列两个，但 canonical 只能选一个 |
| T7 | Turnstile widget 域名白名单加 tucsenberg.com/www（或新建 production 专用 widget 并同步 key） | P1（Blocked）；不做则正式域上表单 widget 渲染失败。服务端 hostname 校验代码已就绪（`src/lib/security/turnstile.ts:81-118`） |
| T8 | `wrangler secret put --env production`：TURNSTILE_SECRET_KEY、RESEND_API_KEY、AIRTABLE_API_KEY、AIRTABLE_BASE_ID、RATE_LIMIT_PEPPER、UPSTASH_REDIS_REST_URL/TOKEN、NEXT_SERVER_ACTIONS_ENCRYPTION_KEY；核对 AIRTABLE_TABLE_NAME（缺省 "Contacts"，`src/lib/airtable/service.ts:52`） | P1（Blocked）；production 无 Upstash 时限流层直接 throw（`src/lib/security/stores/rate-limit-store.ts:296-308`），公开写接口全挂 |
| T9 | Resend 验证发件域；确定 `EMAIL_FROM` / `EMAIL_REPLY_TO` 并配入 production；确认 sales@tucsenberg.com 可收发 | P1（Blocked）；未验证域时 owner 通知邮件静默失败（lead 记录成功但没人知道） |
| T5 部分 | 电话号码、产品照片、法务复核签字 | 见 Wave 2 |

---

## 上线当天的最终证明链（按 `docs/项目基础/上线验证.md`）

1. `PUBLIC_LAUNCH_STRICT=true APP_ENV=production node scripts/starter-checks.js validate-production-config`（含 `PUBLIC_LAUNCH_LEGAL_CONTENT_REVIEWED=true`）Errors 清零。
2. `gh workflow run "Cloudflare Workers 部署" -f environment=production`（main 分支）。
3. 正式域 deployed-smoke：`node scripts/starter-checks.js deployed-smoke --base-url "https://tucsenberg.com"`。
4. 真实 lead canary：`POST_DEPLOY_TEST=1 PLAYWRIGHT_BASE_URL="https://tucsenberg.com" pnpm exec playwright test tests/e2e/smoke/`，确认 Airtable 记录 + owner 邮件到达。
5. Owner 对品牌/法务/联系人/资产/分析链路签字。

---

## 已验证为绿（本次审查实测，proof label 标注）

| 项目 | 结果 | Proof label |
| --- | --- | --- |
| `pnpm brand:check` / `content:check` / `type-check` / `lint:check` / `test` | 全部 EXIT 0 | local/test-mode |
| Cloudflare 构建链：`pnpm build` → `website:build:cf` → `wrangler deploy --dry-run --env preview`（preview env vars） | 三步 EXIT 0，产物 gzip ≈ 2.1 MB | local/test-mode |
| 禁用措辞扫描（上线验证.md 的 rg 命令） | 0 命中 | local/test-mode |
| 预览站核心路由 `/`、`/products`、`/about`、`/contact`、`/request-quote`、`/api/health` | 全 200；`/zh` → 404 | public-preview-smoke |
| Phase 1 全部 16 个页面存在且 MDX 定稿（无 lorem/stub），法务页 lastReviewed 新鲜（2026-07-02/07-05） | 通过 | 静态核对 |
| English-only：locales 仅 en，`/zh` 404 + noindex，无 zh 残留 | 通过 | 静态核对 + smoke |
| 翻译与 compat 新鲜度（792 keys 一致） | 通过 | local/test-mode |
| 联系方式一致性（email/地址跨 contact/privacy/terms/config 一致；空 phone 在 JSON-LD 正确省略） | 通过 | 静态核对 |
| 五产品目录↔规格注册表映射、型号命名、公制单位一致；6 个被引用 PDF 全部存在 | 通过 | 静态核对 |
| sitemap 覆盖 16 页、robots 指向 sitemap、hreflang 仅 en+x-default | 通过 | 静态核对 |
| footer/CTA 由 `getCanonicalPath` 生成，无死内链 | 通过 | 静态核对 |
| 安全基线：CSP/HSTS 全路径下发、Turnstile bypass 仅 dev、CORS 白名单跟随 config、`/api/health` 无泄露、`public/_headers` 静态缓存契约在位 | 通过 | 静态核对 |

> 注意：以上是 local/test-mode 与 public-preview-smoke 级别证明，
> 不构成 public launch 证明。正式上线证明按上一节走。
