# Step 4.1 — Four-Page Fact-Aligned Rebuild — Master Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` to execute this plan phase-by-phase, task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Rebuild the four buyer pages (home, `/membranes/[product]`, `/compatible/[brand]`, `/quote`) to the full B1 narrative *structure* with every fact sourced from the real data layer / owner-signed baseline, restoring the trust modules silently dropped at the 2026-05-14 design lock, and reconciling the contradictory planning docs.

**Architecture:** A Phase-A foundations layer (single-source `catalog-facts`, shared async `@/components/trust` primitives, `trust.*`/`legal.*` i18n, dead-layer quarantine, doc reconciliation) freezes the interfaces; Phases B–E rebuild one page each against those frozen interfaces; Phase F is a cross-cutting final review. Structure comes from the B1 prototype; **every number/brand/SKU/stat comes only from `@/data/product-compatibility` or the signed baseline — fabricated prototype data is struck.**

**Tech Stack:** Next.js 16 App Router (async Server Components), React 19, TypeScript 6 strict, Tailwind 4, next-intl 4, Vitest, Playwright, OpenNext Cloudflare.

**Spec authority:** `docs/superpowers/plans/2026-05-16-step-4.1-fact-signoff.md` (resolved, owner-signed content-fact baseline). Every `§`-citation in the phase plans refers to it.

**Phase plan files (execute in this order):**
1. `2026-05-16-step-4.1-phase-a.md` — Foundations (MUST merge before B–E)
2. `2026-05-16-step-4.1-phase-b.md` — Home
3. `2026-05-16-step-4.1-phase-c.md` — `/membranes/[product]`
4. `2026-05-16-step-4.1-phase-d.md` — `/compatible/[brand]`
5. `2026-05-16-step-4.1-phase-e.md` — `/quote`
6. Phase F — below, in this file.

---

## Phase 0 — Repo-truth & branch lane (no feature code, controller-run, ~5 min)

> Added after a Codex plan-review + a read-only repo fact-check (2026-05-16). Establishes the real starting state so no phase acts on stale "PR still open / needs rebase" assumptions.

**Verified starting facts (2026-05-16, do not re-litigate):**
- Repo is on `main`, `main == origin/main` at `74e1d29`, working tree clean of code (only the untracked `2026-05-16-step-4.1-*.md` plan docs). PR #7 (Step 4) and #8 (docs) are squash-merged. **No open PR, nothing to rebase.**
- Single-branch model: this whole effort is **one** feature branch off clean `main`; "Phase A merged" means *Phase-A tasks committed on this branch and its exit gate green* — NOT a separate intermediate PR. Only Phase F8 opens the single PR. R3/R4's word "merge" = "committed-on-branch + gate green".
- Already-correct, must-not-regress (verified, no task needed — Phase 0 only records the guard): `search → /quote` already carries `partNumber` **and** `brand` (`home-hero-search.tsx:63-67`, `compatibility-search.tsx:70-74`); RFQ part-number log redaction already proven by `quote-api.test.ts:393` ("never sends raw buyer part numbers as productName"). Phases must not touch these; F3 re-asserts the named test.

- [ ] **Step 1 —** `git switch -c step-4.1-four-page-rebuild` from clean `main` (confirm `git status` clean of code first; the plan docs ride along untracked, get committed in Phase A A5).
- [ ] **Step 2 —** Record in `DEVELOPMENT-LOG.md` (as part of Phase A A5, not a separate commit) that Step 4.1 started from `main@74e1d29`.
- [ ] **Step 3 —** No build/test here; Phase A's gate is the first real gate.

---

## SELF-REVIEW RESOLUTIONS — BINDING (override any conflicting phase text)

The writing-plans self-review (spec coverage / placeholder scan / type consistency) found these cross-phase defects. These resolutions are **binding** and override any phase-plan text that conflicts.

### R1 — Path count is 17, not 19 (data truth)
The signed baseline §4 prose originally said "19"; the real `compatibilityMappings` length is **17** (Sanitaire 5 + EDI 7 + SSI Aeration 5). The baseline file has been corrected (2026-05-16). Phase A's `catalog-facts.test.ts` drift-guard locks 17 and the per-brand/fitStatus breakdown. **No page hardcodes any count — all import from `@/data/product-compatibility`.** Any phase prose citing 19/18 is superseded by the frozen interface value.

### R2 — One `TrademarkDisclaimer` test contract (normalize B/C/D/E)
Phase A's real component renders `data-testid="trademark-disclaimer"` + `data-variant={variant}` (single testid, variant attribute). Phase B/C/D test drafts variously invented `trademark`, `page-trademark-disclaimer`, `trademark-brand-notice`, `trademark-footer`. **Binding:** Phase A MUST expose `data-testid="trademark-disclaimer"` and `data-variant` on the component root (already specified in Phase A A2.1). All consumer-phase tests assert via `screen.getAllByTestId("trademark-disclaimer")` + filtering on `data-variant` (`"footer"|"brand-notice"|"inline"`), OR a page-side wrapper `data-testid` ONLY where two disclaimers of the same variant could collide. No consumer phase invents a new disclaimer testid. Implementer subagents reconcile their test to this contract during the task; the spec reviewer rejects any other testid.

### R3 — Shared-doc append order (serialize cross-phase edits)
`docs/specs/behavioral-contracts.md`, `DEVELOPMENT-LOG.md`, and `docs/quality/client-boundary-budget.json` are touched by multiple phases. **Binding:** these three files are edited **only in the phase's final task**, and phases execute strictly A→B→C→D→E→F (no parallel page execution at the controller level — subagent-driven-development runs one task at a time anyway). Each phase appends to `behavioral-contracts.md` under its own clearly headed `### Step 4.1 — Phase X` subsection (append-only, never edit a prior phase's subsection). Phase F consolidates/verifies coherence. This removes the "who edits BC first" ambiguity flagged by Phases D and E.

### R4 — Phase A is a hard gate
Every phase B–E opens with a Phase-A presence probe that STOPS if `@/components/trust` or the `catalog-facts` exports are absent. The controller MUST merge Phase A (all its commits, green exit gate) before dispatching any B–E task. Phases B–E are mutually independent (distinct page dirs + i18n namespaces: `home.*` / `membraneProduct.*` / `compatibleBrand.*` / `quote.*`; A owns `trust.*`/`legal.*`) so ordering among B–E is free, but R3 fixes it as B→C→D→E for shared-doc determinism.

### R5 — CRR-terms page deferral is consistent
Phase E ships the privacy-only consent line and keeps the dual-link `quote.legal.consentWithReviewTerms`/`reviewTermsLink*` keys present-but-unused (parity-safe). Phase A A5.c records "CRR-terms page → deferred to Step 5" in the DEV-LOG roadmap. Consistent — Phase F verifies no Phase-E surface links to a nonexistent CRR-terms page and that the unused keys don't trip a dead-key check.

### R6 — ES/ZH copy authoring boundary (acknowledged, gate-enforced)
Some phase tasks give complete EN copy and abbreviate ES/ZH as "full professional translation, technical tokens English, no banned adjectives, no struck numerics". This is a deliberate, gate-enforced boundary, not a placeholder: the implementer authors real ES/ZH under those explicit constraints, and `pnpm content:check` (equal leaf parity) + `tests/unit/i18n-public-es-placeholder.test.ts` + `pnpm brand:check` mechanically reject `[ES-TODO]`/English-as-Spanish/banned adjectives. The spec reviewer verifies each new leaf has real, non-English-identical ES and real ZH. (Authoring ~200 strings inline in the plan would not survive review better than the gate; the gate is the contract.)

### R7 — Frozen RFQ surfaces (Phase E)
`src/app/api/quote/route.ts`, `src/lib/lead-pipeline/lead-schema.ts`, `quote-form.tsx`, `use-quote-form.ts` are byte-frozen by Phase E (wrap-only). Phase F verifies via `git log` that no Phase-E commit touched them and that `quote-api.test.ts` is unchanged-and-green.

### R8 — Indexed-surface ES-TODO leak is a real gap the four-page guard does NOT cover (Codex #3, verified)
`tests/unit/i18n-public-es-placeholder.test.ts` only guards the four rebuilt pages' namespaces. Verified: `messages/es/critical.json` (338 `[ES-TODO]`) + `messages/es/deferred.json` (292) still carry placeholders, and **`legal` / `privacy` / `terms` namespaces contain `[ES-TODO]` while their pages are `sitemap.include:true` (indexed)** — the other indexed `include:true` pages are exactly `home, about, privacy, terms, capabilities, howItWorks, quote` (`pages.config.ts:70-197`; `products/blog/contact/customProject` are already `include:false`+noindex, contained — do NOT spend effort there). **Binding:** Step 4.1 OWNS only the four pages' copy; it does NOT rewrite `legal/privacy/terms` MDX/JSON. But Phase A A5 MUST record this as an explicit, scoped **pre-launch owner gate** in `DEVELOPMENT-LOG.md` ("indexed legal/privacy/terms/about/capabilities/how-it-works ES still placeholder — Step 5 / pre-launch ES pass, not Step 4.1 scope"), and Phase F F4 adds a **read-only** diagnostic grep (`grep -rl '\[ES-TODO\]' messages/es` → expected non-empty, logged not failed) so the leak is visibly tracked, never silently inherited. Scope discipline: resist the urge to fix it inside Step 4.1.

### R9 — RFQ safety-regression is proven by a NAMED existing test before any freeze (Codex #4, verified)
Codex feared the RFQ surfaces get frozen before their safety property is proven. Verified: the property is already proven by `src/app/api/quote/__tests__/quote-api.test.ts:393` `it("never sends raw buyer part numbers as productName ...")` (asserts `productName === "RFQ quote request"`, `.not.toContain` the sensitive tokens) plus the owner-notification-failure test. **Binding:** Phase E E0 (probe) MUST run `pnpm exec vitest run src/app/api/quote/__tests__/quote-api.test.ts` and confirm that named redaction test is **green at the pre-freeze baseline** (record the pass in the E0 checkbox). F3 Step 2 already re-runs it; F3 additionally asserts `git log` shows zero Phase-E commits to the frozen paths (R7). Freeze is only valid because the safety net is named, pre-verified green, and re-verified post-wrap — not assumed.

### R10 — Client-message-namespace budget (client payload ≠ client-boundary count) (Codex #6, verified)
`scripts/starter-checks.js client-boundary` counts client *component boundaries*, not serialized client *message* payload. Verified `src/lib/i18n/client-messages.ts` `CLIENT_MESSAGE_NAMESPACES` already = `[accessibility, apiErrors, contact, cookie, errors, home, language, navigation, quote, search]` (already includes `home`,`quote`,`search`). **Binding:** Phases B–E are wrap/compose-only and MUST NOT add a namespace to `CLIENT_MESSAGE_NAMESPACES` (all new trust/narrative copy is rendered in **Server** Components — `trust.*`/`legal.*`/the new page subtrees stay server-side, never shipped to the client bundle). Phase F F6 adds Step 4: `git diff origin/main..HEAD -- src/lib/i18n/client-messages.ts` MUST be empty (no client-message namespace added/grown by Step 4.1). If a phase genuinely needs a client string it must justify it in its commit body and F6 flags it for review rather than silently passing.

### R11 — `getFeaturedProductFacts()` slug-vs-canonical footgun (Codex #8, verified data truth)
The featured product's raw id/slug is `tuc-d9-epdm` but its canonical URL segment is `9-inch-epdm-disc-replacement` (`tuc-d9-epdm` **308-redirects** to canonical). A naive `` `/membranes/${facts.slug}` `` would emit the redirecting URL, not the canonical one. **Binding:** Phase A's `getFeaturedProductFacts()` MUST expose distinctly-named fields — `sku` (`TUC-D9-EPDM`), `canonicalSlug` (`9-inch-epdm-disc-replacement`), and a ready `quoteHref` shaped exactly `/quote?sku=TUC-D9-EPDM&product=9-inch-epdm-disc-replacement` — built with `encodeURIComponent` on each value, byte-identical in shape to the existing frozen producer at `src/app/[locale]/membranes/[product]/page.tsx:104` (Phase C's regression fence pins that exact absolute+encoded form; the SSOT module MUST match it, not a relative path-less variant) — and MUST NOT expose a bare ambiguous `.slug`. Consumer phases build product URLs from `canonicalSlug`/`quoteHref` only. The Phase-A `catalog-facts.test.ts` drift-guard locks these three exact strings; Phase C's existing `page.test.tsx` (unchanged) is the cross-check that the href shape did not drift. Spec reviewer rejects any `/membranes/${...slug}` that is not the canonical segment.

---

## Phase ordering & dependency graph

```
Phase A (Foundations) ──┬─→ Phase B (Home)
   freeze interfaces     ├─→ Phase C (Product)
   merge + green gate     ├─→ Phase D (Compatible)
                          └─→ Phase E (Quote)
                                   └─→ Phase F (Final cross-cutting review) ─→ PR ─→ merge
```

Controller executes A fully → B → C → D → E → F (R3/R4). Each phase has its own exit gate; Phase F is the branch-wide gate before PR.

---

## Phase F — Final cross-cutting review & merge

> Depends on merged Phases A–E. No new feature code. Verifies the four rebuilt pages cohere, every struck datum is absent branch-wide, frozen surfaces did not regress, i18n parity holds, and the contradictory docs are now consistent — then ships.

### Task F1 — Branch-wide spec-strike audit

**Files:** none modified (audit) → records evidence in `DEVELOPMENT-LOG.md` (final task).

- [ ] **Step 0 — Scope the audit precisely (Codex #11):** this grep is a **source-token** audit. It is one of three complementary layers, do not conflate them: (a) *source tokens* = this F1 grep over the four page trees + `src/components/trust` + the three `critical.json` locale files; (b) *rendered output* = the F2/F3 page tests asserting real catalog values and `.not` the struck strings in the DOM; (c) *public-SEO* = F3 sitemap/canonical/hreflang test + the F4 ES-TODO diagnostic. A hit inside `src/app/[locale]/{page,membranes,compatible,quote}` or `src/components/trust` is a **hard fail → STOP**. A hit in `src/components/sections` (shared by non-Step-4.1 pages too) is adjudicated: hard-fail only if a Step-4.1 page renders that string; if the section is not mounted by any of the four pages, log it as out-of-scope pre-existing debt (do not fix it inside Step 4.1, do not weaken the grep). Banned adjectives (`durable/efficient/premium/high quality`) are hard-fail anywhere in the four-page scope per CLAUDE.md #4 — no "quoted standard" exception.
- [ ] **Step 1 — Run the branch-wide strike grep** over all four page trees + all three locale files, excluding test files (they legitimately carry the forbidden tokens as negative assertions):

```bash
grep -rniE '\b24 (documented )?paths?\b|\b19 (documented )?paths?\b|\b6 OEM families\b|\bAercor\b|\bStamford\b|\bNopon\b|\bPhase 1\.5\b|2026-04-22|CRR v?2026|\blast reviewed\b|\b5\s?[–-]\s?7\s*(day|year)|\bMOQ\b|\b1\s?[–-]\s?2\s*weeks?\b|tear.?down|TB-DSC-|AFD350|bayonet|°\s?C|threaded ring|\bquote@|\bquality@|\blegal@|premium|high quality|better than EPDM|\bdurable\b|\befficient\b' \
  "src/app/[locale]/page.tsx" "src/app/[locale]/membranes" "src/app/[locale]/compatible" "src/app/[locale]/quote" "src/components/trust" "src/components/sections" messages/en/critical.json messages/es/critical.json messages/zh/critical.json \
  | grep -v "__tests__" | grep -v "\.test\." | grep -v "\.stories\." || echo "BRANCH-STRIKE-AUDIT-CLEAN"
```

- [ ] **Step 2 — Expected:** `BRANCH-STRIKE-AUDIT-CLEAN`. Any hit → STOP, route the offending file/line back to the owning phase's implementer to fix at source (never weaken the grep). Exception: `1–2 weeks`/`one to two weeks` is ALLOWED only as the §2 unified lead-time wording in word-form (`one to two weeks`); the digit-form `1–2 weeks` regex above must still be clean — confirm the lead-time copy is word-form.
- [ ] **Step 3 — Single-inbox audit:** `grep -rniE '[a-z0-9._-]+@tucsenberg\.com' src messages | grep -v __tests__ | grep -viE 'sales@tucsenberg\.com'` → must be empty (only `sales@tucsenberg.com` anywhere).

### Task F2 — Catalog-fact consistency across all surfaces

- [ ] **Step 1 — Run** `pnpm exec vitest run src/data/product-compatibility/__tests__/catalog-facts.test.ts` → green (17 / 3 brands / per-brand stats locked).
- [ ] **Step 2 — Assert no hardcoded catalog literal** outside the data layer: `grep -rnE "\b(17|19|24)\s*(documented )?paths?\b|6 OEM" "src/app/[locale]" src/components | grep -v __tests__ | grep -v "catalog-facts"` → empty (every count is rendered via `CATALOG_FACTS`/`getBrandPathStats`/`getOemBrandFacts`, never inline).
- [ ] **Step 3 —** Confirm `getBrandPathStats` values surfaced on home (`OemGridSection`) and `/compatible/[brand]` (`brand-stats`) equal the drift-guard values (sanitaire 5 / edi 7 / ssi-aeration 5) by running those page tests.

### Task F3 — Frozen-surface non-regression (RFQ pipeline + canonical/redirect)

- [ ] **Step 1 —** `git log --oneline origin/main..HEAD -- src/app/api/quote src/lib/lead-pipeline "src/app/[locale]/quote/quote-form.tsx" "src/app/[locale]/quote/use-quote-form.ts"` → only acceptable matches are Phase-A/unrelated; **no Phase-E commit may appear**. If one does → STOP (R7 violated), revert and re-wrap.
- [ ] **Step 2 —** `pnpm test src/app/api/quote/__tests__/quote-api.test.ts src/data/product-compatibility/__tests__/product-slug.test.ts src/app/__tests__/sitemap.test.ts "src/app/[locale]/membranes/[product]/__tests__/page.test.tsx" "src/app/[locale]/compatible/[brand]/__tests__/page.test.tsx"` → all green (RFQ backend, canonical-slug + 308 redirect, sitemap canonical-only en/es-never-zh, brand/product metadata intact).
- [ ] **Step 3 —** BC-026 still marked Partial (no Playwright RFQ smoke / no deployed-Airtable added) — confirm no phase silently upgraded it.

### Task F4 — i18n parity & namespace-ownership audit

- [ ] **Step 1 —** `pnpm content:check` → equal total leaf-key count en/es/zh.
- [ ] **Step 2 —** `pnpm test tests/unit/i18n-message-contract.test.ts tests/unit/i18n-public-es-placeholder.test.ts src/components/trust/__tests__/trust-i18n-parity.test.ts` → green; no `[ES-TODO]`/`[ZH-TODO]` in any public namespace; ES not English-identical except the curated allowlist.
- [ ] **Step 3 — Indexed ES-TODO leak diagnostic (R8, read-only, does NOT fail the gate):** `grep -rl '\[ES-TODO\]' messages/es` (expected: `critical.json`,`deferred.json` still listed) and `grep -nE '"(legal|privacy|terms)"' tests/unit/i18n-public-es-placeholder.test.ts` to confirm the four-page guard scope. This is **tracked, not fixed** in Step 4.1 — confirm Phase A A5 recorded the scoped pre-launch ES gate in `DEVELOPMENT-LOG.md`. If A5 did not record it → STOP, route back to Phase A.
- [ ] **Step 4 —** Ownership check: `git diff --stat origin/main..HEAD -- messages` plus a grep proving `trust.*`/`legal.*` were added only by Phase-A commits and `home.*`/`membraneProduct.*`/`compatibleBrand.*`/`quote.*` only by their owning phase (no namespace collision).

### Task F5 — Doc-reconciliation coherence + behavioral-contracts consolidation

- [ ] **Step 1 —** Verify Phase-A A5 landed: `grep -nE "^### 7\.[1-5] " DESIGN.md` (5 anchors), `grep -c "Step 4.1 supersession" PROJECT-BRIEF.md` (≥3), `grep -n "Step 4.1" DEVELOPMENT-LOG.md` (entry + cut-vs-deferred table).
- [ ] **Step 2 —** `behavioral-contracts.md`: confirm each phase appended its own `### Step 4.1 — Phase B/C/D/E` subsection (R3), BC-001 updated (single home quote CTA + footer disclaimer), BC-002/BC-023/BC-026 notes honest (no overclaim). Resolve any duplicate/contradictory note into one coherent Step-4.1 section (append-only edit; do not delete prior text).
- [ ] **Step 3 —** Confirm CRR-terms is recorded as Step-5-deferred (R5) and no `/quote` surface links to it; the dual-link i18n keys exist unused.

### Task F6 — Client-boundary budget & full branch gate

- [ ] **Step 1 —** `node scripts/starter-checks.js client-boundary` → count unchanged at the registered max (Phases B–E add no client islands; only register+bump if a phase legitimately introduced one, which the per-phase plans say it did not).
- [ ] **Step 1b — Client-message-payload budget (R10, Codex #6):** `git diff origin/main..HEAD -- src/lib/i18n/client-messages.ts` MUST be empty (no namespace added to `CLIENT_MESSAGE_NAMESPACES`; all Step-4.1 trust/narrative copy is server-rendered). Non-empty → STOP, justify or revert. This is distinct from Step 1 (boundary count ≠ serialized message payload).
- [ ] **Step 2 — Full sequential branch gate** (`build` and `website:build:cf` NEVER parallel — same `.next`):

```bash
pnpm type-check
pnpm lint:check
pnpm test
pnpm content:check
pnpm brand:check
pnpm component:check
node scripts/starter-checks.js client-boundary
pnpm build
pnpm website:build:cf
```

All zero-error / green.
- [ ] **Step 3 — Full Playwright** (chromium + Mobile Chrome + Mobile Safari): `pnpm exec playwright test` → all green (homepage, navigation, basic-navigation, user-journeys, no-js-html-contract, i18n specs). Update any spec asserting old structure to the new correct behavior with a cited reason (never weaken a URL/redirect/nav-target assertion).

### Task F9 — Trilingual visual smoke (Codex #7; runs after F6, before F7)

> Codex #7: the per-phase "manual dev render" proof is not a durable check that ES/ZH copy actually renders (no hardcoded English, no overflow) across the rebuilt pages. This adds one deterministic pass. Placed here (post-gate, pre-swarm) so layout/locale breakage is caught before review/PR.

- [ ] **Step 1 —** With `pnpm build` output already produced by F6, start the production server (`pnpm start` or the project's equivalent) on a known port.
- [ ] **Step 2 —** For the matrix **4 pages × 3 locales × 3 viewports** — pages: `/`, `/membranes/9-inch-epdm-disc-replacement`, `/compatible/sanitaire`, `/quote`; locales: `en`,`es`,`zh`; viewports: 390 / 768 / 1280 — capture a screenshot per cell via Playwright (`pnpm exec playwright` with a throwaway script or the existing visual harness if one exists; do NOT add a committed flaky visual test — this is a controller-run smoke, evidence saved to a temp dir, not the repo).
- [ ] **Step 3 —** Adjudicate each cell: (a) no raw English string on an `es`/`zh` page in the new trust/narrative sections (cross-check against the new `trust.*`/`*.intake`/`*.materialGuidance` keys), (b) no horizontal overflow / clipped text at 390, (c) the trademark disclaimer is present and legible on every OEM-referencing page (compliance). Any failure → STOP, route to the owning phase's implementer (fresh subagent, fix at i18n/source, re-run F4+F6+F9). ZH is internal-only but must still render correctly (it ships in dev/internal preview).

### Task F7 — Final read-only swarm review

- [ ] **Step 1 —** Run the `review-swarm` skill over the full branch diff (`git diff origin/main..HEAD`) with an intent packet derived from the signed baseline + this master plan. Four read-only reviewers (intent/regression, security/privacy, performance/reliability, contracts/coverage).
- [ ] **Step 2 —** Triage findings: fix high/medium-severity at source via the owning phase's implementer pattern (fresh subagent, TDD, re-gate). Re-run F1–F6 + F9 after any fix.
- [ ] **Step 3 —** Record the swarm verdict + this plan's completion in `DEVELOPMENT-LOG.md` (Step 4.1 complete; pages rebuilt to baseline; pre-launch owner gates still: `sales@tucsenberg.com` live mailbox, CRR-terms page is Step 5). Commit:

```bash
git add DEVELOPMENT-LOG.md docs/specs/behavioral-contracts.md
git commit -m "docs: record step 4.1 completion and final review" -m "Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task F8 — PR & merge

- [ ] **Step 1 —** Push branch; `gh pr create --base main` with a body summarizing: four pages rebuilt to B1 narrative, trust modules restored, all facts data-sourced (17 paths / 3 brands), fabricated prototype data struck, RFQ pipeline frozen, docs reconciled; pre-launch owner gates listed; ends with `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.
- [ ] **Step 2 —** Watch CI to green (基础质量 / 单元与集成 / 浏览器冒烟 / Cloudflare 构建证明 / Semgrep / 汇总). Fix failures at source, never `--no-verify`.
- [ ] **Step 3 —** `gh pr merge <N> --squash --delete-branch`; verify local `main` fast-forwards; prune.

### Phase F exit / Step 4.1 done

- [ ] Branch-strike audit clean; single inbox; catalog facts consistent (17/3); RFQ + canonical/redirect non-regressed (R7+R9 named test green); i18n parity + ownership clean; client-message payload unchanged (R10); docs coherent; client-boundary unchanged; full gate + full Playwright + F9 trilingual visual smoke green; swarm addressed; PR merged; branches cleaned.
- [ ] Owner pre-launch gates surfaced (not code): `sales@tucsenberg.com` must be a live mailbox before public launch; CRR-terms page is Step 5; **indexed `legal/privacy/terms/about/capabilities/how-it-works` ES is still placeholder — Step 5 / pre-launch ES pass, explicitly OUT of Step 4.1 scope (R8)**.

---

## Self-review checklist (run, resolved)

- **Spec coverage:** every signed-baseline § maps to a phase task — §1 SLA→A2.2/B1/C8/E6; §2 lead-time strike→C7/E3; §3 batch softening→A2.5/C7; §4 catalog truth→A1/F2; §6 legal→A2.1/A3/all footers; §7/§8 material→A2.4/B6/C5/D; §9.x→E8/E9/C8; doc reconciliation→A5/F5. Covered.
- **Placeholder scan:** EN copy is complete in every task; ES/ZH abbreviation is the gate-enforced R6 boundary (explicit, not a TBD). No "TODO/handle edge cases/similar to Task N" remain.
- **Type/interface consistency:** all phases cite the single FROZEN INTERFACES block (catalog-facts signatures + trust component props + i18n namespace map); R1 (17), R2 (one disclaimer testid), R11 (`canonicalSlug`/`quoteHref`, no bare `.slug`) reconcile the divergences. Consistent.
- **Codex plan-review triage (2026-05-16, verified before applying):** ACCEPTED → Phase 0 (repo-truth/single-branch lane, #1/#2), R8 (indexed ES-TODO leak tracked-not-fixed, #3), R9 (named pre-freeze RFQ redaction test, #4), R10 (client-message payload budget, #6), R11 (slug-vs-canonical footgun, #8), F1 Step 0 (audit-layer precision, #11), F9 (trilingual visual smoke, #7), DEV-LOG-top staleness folded into Phase A A5 + F5 (#14). REJECTED as already-handled (verified, no plan change) → #5 search→quote brand context (already carries `partNumber`+`brand`), RFQ redaction already test-proven (folded into R9 as a *named* pin rather than new code), client namespaces already include home/quote/search (turned into the R10 *no-growth* guard). CLARIFIED → #2 only `home/about/privacy/terms/capabilities/howItWorks/quote` are indexed (others already noindex), #10 zh is excluded from SEO via `publicLocales`+`alternateLinks:false`, not `routing.locales` (Phase D/C metadata fences already correct). Ellipsis-path/placeholder concern (#12/#13): the binding-resolution mechanism + spec-reviewer gate is the contract; phase files use real paths in all executable steps.
