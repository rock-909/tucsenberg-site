# Step 4.1 — Phase A: Foundations

> Part of `2026-05-16-step-4.1-master.md`. **Binding self-review resolutions R1–R7 in the master override any conflicting text here.** Authoritative spec: `2026-05-16-step-4.1-fact-signoff.md`. REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`. TDD per task: failing test → run (see fail) → minimal impl → run (see pass) → `pnpm exec prettier --write <files>` → gates → commit (lowercase conventional; body ends `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`; never `--no-verify`). Known-spurious IDE diagnostics (jest-dom matchers, fresh-file `@/...` missing, Zod/FormEvent star-hints) → trust only `pnpm type-check` + vitest run. **Phase A must merge (all commits, green exit gate) before Phases B–E start.**
>
> **Data-truth (R1):** real `compatibilityMappings` length = **17** (Sanitaire 5 / EDI 7 / SSI Aeration 5). `catalog-facts.test.ts` locks it. Do NOT change data to match any "18/19" prose.

## Task A1 — Catalog fact constants (single source, drift-guarded)

**Files:** Create `src/data/product-compatibility/catalog-facts.ts`; Create `src/data/product-compatibility/__tests__/catalog-facts.test.ts`; Modify `src/data/product-compatibility/index.ts` (re-export).

- [ ] **Step 1 — failing drift-guard test.** Create `catalog-facts.test.ts` recomputing from raw `compatibilityMappings`/`oemBrands`/`oemModels`/`productVariants` and asserting equivalence to the module: `CATALOG_FACTS.totalCompatibilityPaths === compatibilityMappings.length === 17`; `oemBrandCount === 3`; `getOemBrandFacts()` ids `["sanitaire","edi","ssi-aeration"]`, displayNames `["Sanitaire","EDI","SSI Aeration"]`, never contains Aercor/Stamford/Nopon; `getBrandPathStats` sanitaire `{paths:5,epdm:3,tpu:2,fitStatus:{exact:2,"verify-dimensions":1,custom:2}}`, edi `{7,5,2,{3,3,1}}`, ssi-aeration `{5,3,2,{1,2,2}}`; `CATALOG_FACTS.fitStatusTotals {exact:6,"verify-dimensions":6,custom:5}`; `fitStatusLabelKey === "membraneProduct.compatibility.fitStatus"`; `fitStatusOrder` keys `["exact","verify-dimensions","custom"]`; `getFeaturedProductFacts()` slug `tuc-d9-epdm`, sku `TUC-D9-EPDM`, material `epdm`, diameter `'9" / 228.6 mm nominal membrane class'` (each cross-checked against the matching `productVariants` entry).
- [ ] **Step 2 — run** `pnpm exec vitest run src/data/product-compatibility/__tests__/catalog-facts.test.ts` → FAIL (module missing — the real failure, not the spurious fresh-file hint).
- [ ] **Step 3 — implement** `catalog-facts.ts` deriving everything by computing over the Zod-parsed data arrays (Map indices model→brand, variant→material; `computeBrandPathStats`; `OEM_BRAND_FACTS` from `oemBrands`; featured from `productVariants.find(slug==="tuc-d9-epdm")` with thrown errors on missing). Exports per the master FROZEN INTERFACES block: `CATALOG_FACTS`, `getOemBrandFacts`, `getBrandPathStats`, `getFeaturedProductFacts`, types `OemBrandFacts`/`BrandPathStats`/`FeaturedProductFacts`. No baked literals except where the data layer itself bakes them.
- [ ] **Step 4 — run** the test → all pass; then `pnpm exec vitest run src/data/product-compatibility` (whole data suite) → green.
- [ ] **Step 5 — re-export** the symbols + types from `src/data/product-compatibility/index.ts` (grouped with catalog exports).
- [ ] **Step 6 — gate + commit:** prettier the 3 files; `pnpm type-check`; `pnpm lint:check`; `git commit -m "feat(data): add derived catalog fact constants with drift guard"`.

## Task A2 — Shared trust/legal primitives

**Folder:** `src/components/trust/`. Each component: `*.tsx` (async Server Component reading i18n via `loadCompleteMessages(locale)` + `readMessagePath`, except `NarrativeSection` which is synchronous/no-i18n) + a synchronous `*-view.tsx` sibling for Storybook + `.stories.tsx` + `__tests__/*.test.tsx`. Mirror the existing `faq-section`/`faq-section-view`/`faq-section.stories` quad. Zero hardcoded buyer English. **R2: every component root carries `data-testid="trademark-disclaimer"`-style stable testids; `TrademarkDisclaimer` root = `data-testid="trademark-disclaimer"` + `data-variant={variant}`.** i18n keys (`trust.*`/`legal.*`) added in A3 — A2 tests mock the message tree.

- [ ] **A2.1 TrademarkDisclaimer** props `{locale: Locale, variant: "footer"|"brand-notice"|"inline", brandName?: string}`. Copy from `legal.trademark.{footer,brandNotice,inline}`; brand list interpolated from `getOemBrandFacts().map(b=>b.displayName).join(", ")` → "Sanitaire, EDI, SSI Aeration" (never hardcoded, never the 3 fake brands). `footer` = full §6 statement; `brand-notice` = `{brand}` injected; `inline` = compact one-liner. TDD: failing test (footer lists only the 3 real brands, not Aercor/Stamford/Nopon; brand-notice injects single brand; inline compact; `data-variant` attr) → impl (delegate JSX to `trademark-disclaimer-view.tsx` `{variant,text}`) → story (`Trust/TrademarkDisclaimer` Footer/BrandNotice/Inline) → gate → commit `feat(trust): add trademark disclaimer primitive`.
- [ ] **A2.2 SlaCommitments** props `{locale, layout: "ribbon"|"stacked"}`. Renders EXACTLY 3 `<li data-testid="sla-commitment">` from `trust.sla.{review,standardRfq,urgent}`; wrapper `data-layout`. Test guards exactly-3 (no strayed 4th promise) + no banned adjectives. Impl + view + story + gate + commit `feat(trust): add sla commitments primitive`.
- [ ] **A2.3 CompatibilityProofBox** props `{locale, extraChecks?: readonly string[]}`. `trust.proof.{title,body}` body = "Compatibility is reviewed against documented dimensions and photos before pricing." Test asserts no `/tear.?down|CRR|teardown/i`; `extraChecks` → `<ul><li>`. View+story+gate+commit `feat(trust): add compatibility proof box primitive`.
- [ ] **A2.4 MaterialDecisionCard** props `{locale, defaultMaterial?: "epdm"|"tpu"}`. `trust.material.*` EPDM=default starting point, TPU=triggered by conditions; test asserts no `/premium|better than/i`. View+story+gate+commit `feat(trust): add material decision card primitive`.
- [ ] **A2.5 BatchControlsBlock** props `{locale}`. `trust.batch.{title,traceability,photos,sample}` — §3 softened: no 5–7yr, no day-count, no MOQ. Test guards `not.toMatch(/\b5[–-]7\b|\bdays?\b|\b500\b|\byears?\b/i)`. View+story+gate+commit `feat(trust): add batch controls block primitive`.
- [ ] **A2.6 NarrativeSection** props `{eyebrow?,title,body?,cta?:{label,href},children?,divider?}` — SYNCHRONOUS, no i18n, caller passes resolved strings. Renders `<section className={cn("section-divider",...)}>` (matching faq-section divider), overline span, `<h2>`, optional body, optional CTA link via the existing `@/components/ui` link/button wrapper (inspect first; no direct Radix), `children` slot. Test: heading level-2, eyebrow, CTA href, `section-divider` present/absent on `divider:false`. It is its own view. Story+gate+commit `feat(trust): add narrative section shell`.
- [ ] **A2.7 Barrel + governance:** `src/components/trust/index.ts` re-exports all six + views + prop types. Run `pnpm component:check`; if governance flags the new business folder, register it exactly as the adjacent `sections` entry. Gate + commit `feat(trust): add trust component barrel and governance entry`.

## Task A3 — i18n scaffolding (`trust.*` + `legal.*`, en/es/zh)

**Files:** `messages/{en,es,zh}/critical.json`; new `src/components/trust/__tests__/trust-i18n-parity.test.ts`.

- [ ] **Step 1 — failing parity-pin test** asserting all of: `trust.sla.{review,standardRfq,urgent}`, `trust.proof.{title,body}`, `trust.material.{title,epdm.{label,condition},tpu.{label,condition},note}`, `trust.batch.{title,traceability,photos,sample}`, `legal.trademark.{footer,brandNotice,inline}` are non-empty strings in en/es/zh; `legal.trademark.footer` contains `{brands}`; `brandNotice` contains `{brand}`; footer does NOT match `/proforma|pro forma|\bPI\b|Co\.,? Ltd/i` (R5/§6 — struck legal entity must not reappear).
- [ ] **Step 2 — run** → FAIL.
- [ ] **Step 3 — add EN keys** (spec §1/§3/§6/§7/§9.6 verbatim): SLA review "Compatibility review — within 24 business hours.", standardRfq "Standard RFQ — quote or missing-info request within 48 business hours.", urgent "Urgent shutdown cases — acknowledged the same business day."; proof body "Compatibility is reviewed against documented dimensions and photos before pricing."; material EPDM "default starting point / Municipal and light industrial wastewater conditions.", TPU "selected by conditions / Oil, chemical, and high-grease wastewater where EPDM degrades.", note "Material is chosen by process conditions, not as a price tier…"; batch traceability "Each shipment is checked before dispatch and is traceable by order and production lot.", photos "First production orders can include outgoing photo records on request.", sample "A verification sample is available before the first batch."; legal.footer the §6 statement with literal `{brands}`, brandNotice with `{brand}`, inline the compact one-liner. `{brands}`/`{brand}` are NOT translated (interpolated at render).
- [ ] **Step 4 — add ES keys** (real professional Spanish, same structure).
- [ ] **Step 5 — add ZH keys** (real 工程化 Chinese, same structure).
- [ ] **Step 6 — run** parity test + `pnpm content:check` → equal leaf totals en/es/zh, exit 0.
- [ ] **Step 7 — gate + commit:** prettier 3 json + test; `pnpm type-check`; `pnpm lint:check`; `pnpm test src/components/trust`; `git commit -m "feat(i18n): add shared trust and legal namespaces in en/es/zh"`.

## Task A4 — Dead-layer quarantine

Orphan set (confirmed zero external importers): `src/data/{products,compatibility,index,queries,oem-brands,oem-models,schemas,i18n-types}.ts` + `src/data/__tests__/`. **Never `rm` — use `trash`.**

- [ ] **Step 1 —** re-confirm zero external importers via the documented `grep -rn` over `src` excluding `src/data/{the orphan files}` and `src/data/__tests__/`; also confirm live layer untouched (`grep -rln "@/data/product-compatibility" src/app`). **STOP** + surface if any importer found outside `src/data/`.
- [ ] **Step 2 —** add arch-guard test `src/data/product-compatibility/__tests__/no-flat-data-layer.test.ts` using `spawnSync("grep",["-rEl",…,'@/data/(products|compatibility|queries|oem-brands|oem-models|schemas|i18n-types)"|from "@/data"',"src"])` asserting `status===1` (no match) and empty stdout. Run → FAILS now (files still exist, self-import).
- [ ] **Step 3 —** `trash src/data/products.ts src/data/compatibility.ts src/data/index.ts src/data/queries.ts src/data/oem-brands.ts src/data/oem-models.ts src/data/schemas.ts src/data/i18n-types.ts src/data/__tests__`. Confirm `src/data/` now only has `product-compatibility/`.
- [ ] **Step 4 —** re-run guard → PASS; `pnpm type-check` → 0 errors (if a moved file errors, an importer was missed → restore from Trash, surface, STOP).
- [ ] **Step 5 —** `pnpm test` full → green; `pnpm content:check` + `pnpm brand:check` green; prettier guard test; `pnpm lint:check`; commit `chore(data): quarantine orphaned flat data layer with arch guard`.

## Task A5 — Doc reconciliation

- [ ] **A5.a DESIGN.md §7 anchors:** renumber the four/five prose `###` §7 subsections to `### 7.1 首屏…` `### 7.2 兼容页…` `### 7.3 产品页…` `### 7.4 报价页…` `### 7.5 跨页交互…`; add under `## 7.` a lock note: §7 templates were intentionally narrowed at the 2026-05-14 lock; Step 4.1 restores the brief's trust modules but facts are data-layer-sourced. Verify `grep -nE "^### 7\.[1-5] " DESIGN.md` → 5 lines.
- [ ] **A5.b PROJECT-BRIEF.md:** at the top of regions ~:266-280, ~:490-498, ~:848-857 insert a `> **[Step 4.1 supersession]**` annotation block (authoritative structure = DESIGN.md §7.1–§7.5; authoritative facts = `src/data/product-compatibility/**`, 17 paths, 3 brands; Step 4.1 restores TrademarkDisclaimer/SlaCommitments/CompatibilityProofBox/MaterialDecisionCard/BatchControlsBlock). Inline-mark any brand∉{Sanitaire,EDI,SSI Aeration} or count≠17 with `<!-- struck: not in data layer -->`. Verify `grep -c "Step 4.1 supersession"` = 3. Do not delete historical text.
- [ ] **A5.c DEVELOPMENT-LOG.md:** append a `## Step 4.1 — 四页事实对齐（Foundations 起步）` entry (17 paths truth, Phase A deliverables, spec-file reference) + a "Step 4.1 内容处置" cut/延/上线 table including `CRR 条款页 → 延 → gate 到 Step 5`. Verify `grep -n "Step 4.1"` ≥2.
- [ ] **A5 gate + commit:** prettier DESIGN.md/PROJECT-BRIEF.md/DEVELOPMENT-LOG.md; `pnpm lint:check`; `pnpm content:check`; commit `docs: reconcile design, brief, and dev-log for step 4.1 facts`.

## Phase A exit gate
- [ ] `pnpm type-check` 0 · `pnpm lint:check` 0 · `pnpm test` green · `pnpm content:check` green (en/es/zh parity) · `pnpm brand:check` green · `pnpm component:check` green (trust governed + storybook builds) · all commits present · merged before B–E.

## FROZEN INTERFACES (Phases B–E import only — never redefine)
- `@/data/product-compatibility` re-exports `CATALOG_FACTS {totalCompatibilityPaths:17, oemBrandCount:3, fitStatusOrder, fitStatusLabelKey:"membraneProduct.compatibility.fitStatus", fitStatusTotals:{exact:6,"verify-dimensions":6,custom:5}}`; `getOemBrandFacts():readonly{id,slug,displayName}[]` order `[sanitaire"Sanitaire", edi"EDI", ssi-aeration"SSI Aeration"]`; `getBrandPathStats(id):{paths,epdm,tpu,fitStatus}` (sanitaire 5/3/2/{2,1,2}, edi 7/5/2/{3,3,1}, ssi-aeration 5/3/2/{1,2,2}; throws on unknown); `getFeaturedProductFacts():{slug:"tuc-d9-epdm",sku:"TUC-D9-EPDM",material:"epdm",diameter:'9" / 228.6 mm nominal membrane class'}`.
- `@/components/trust` async Server Components (prop `locale: Locale` from `@/types/content.types`): `TrademarkDisclaimer{locale,variant:"footer"|"brand-notice"|"inline",brandName?}` root `data-testid="trademark-disclaimer"`+`data-variant`; `SlaCommitments{locale,layout:"ribbon"|"stacked"}` (exactly 3 lines); `CompatibilityProofBox{locale,extraChecks?}`; `MaterialDecisionCard{locale,defaultMaterial?}`; `BatchControlsBlock{locale}`; `NarrativeSection{eyebrow?,title,body?,cta?:{label,href},children?,divider?}` synchronous/no-i18n.
- i18n: Phase A owns `trust.*`+`legal.*` (B–E read only). B owns `home.*`, C `membraneProduct.*`, D `compatibleBrand.*`, E `quote.*`. `content:check` enforces equal total leaf-key count en/es/zh; every new key gets real ES + real ZH. `membraneProduct.compatibility.fitStatus.{exact,verify-dimensions,custom}` is the canonical fit-label key — reuse, never duplicate. Existing coarse `home.trust.sla` is replaced by shared `trust.sla.*` in Phase B (removed from all 3 locales at parity).
