# Step 4.1 — Phase B: Home Page Rebuild

> Part of `2026-05-16-step-4.1-master.md`; **master R1–R7 binding**. Depends on **merged Phase A**. Phase B OWNS `home.*` only (read `trust.*`/`legal.*`, never modify). Per-task TDD loop + gates + commit conventions as in Phase A (failing test → see fail → minimal impl → see pass → prettier → `type-check`/`lint:check`/`test`/`content:check`/`brand:check`/`component:check` → conventional commit + Co-Authored-By trailer; never `--no-verify`; known-spurious diagnostics ignored). Reuse the existing `faq-section`/`-view`/`.stories`/`__tests__` quad pattern for new `Home*Section`s. Facts are import-only from frozen accessors — **zero hardcoded counts/brands/SKUs**. No banned adjectives; TPU by workload; single `sales@tucsenberg.com`; SLA only via shared `SlaCommitments`/`trust.sla.*`.

**Final render order in `src/app/[locale]/page.tsx`:** Hero+search (keep) → HomeConfirmSection → HomeMembraneTypeSection → OemGridSection (frozen facts) → SlaCommitments ribbon → HomeRisksSection → CompatibilityProofBox → MaterialDecisionCard → BatchControlsBlock → HomeFaqSection (Q01–Q06) → FinalCta (single → /quote) → TrademarkDisclaimer footer.

## Task B0 — Phase-A presence gate + home-contract baseline (no commit)
- [ ] Probe: `node -e` checks `CATALOG_FACTS`/`getOemBrandFacts`/`getBrandPathStats`/`getFeaturedProductFacts` are functions/object; `ls src/components/trust/`; `trust`/`legal` present in en critical, old `home.trust.sla` still "…2 business days". If Phase A absent → STOP.
- [ ] Capture old-contract assertions to update: `grep -rn 'home\.' src/test/i18n-validation.ts tests/e2e/homepage.spec.ts src/app/[locale]/__tests__/page.test.tsx` (home.trust.*, cta.viewMembranes, MaterialsSection text, "Browse All Membranes", second-CTA FEATURED_MEMBRANE_HREF). Run baseline gates green before editing.

## Task B1 — Trust ribbon → shared SlaCommitments; remove dead `home.trust.*`
- [ ] Failing test in `src/app/[locale]/__tests__/page.test.tsx`: remove the 4 `trust.*` entries from `homeMessages`; add a `vi.mock("@/components/trust", importOriginal)` stubbing `SlaCommitments` as a 3-`<li>` `data-testid="sla-commitments" data-layout`; assert ribbon renders, 3 items, old "Aftermarket replacement membranes only"/"Quote response within 2 business days" gone.
- [ ] Impl `page.tsx`: import `SlaCommitments`; delete `TrustRibbon` fn + `TRUST_ITEMS`; replace `<TrustRibbon/>` with `<SlaCommitments locale={locale as Locale} layout="ribbon"/>`.
- [ ] Delete `home.trust` object (scope/leadTime/sla/noFit) from en/es/zh (parity). Update `src/test/i18n-validation.ts` if it referenced the key.
- [ ] Update `tests/e2e/homepage.spec.ts`: assert the SLA ribbon shows 24h/48h text using the **actual** Phase-A `trust.sla.*` EN/ES wording (read `messages/{en,es}/critical.json`→`trust.sla` first; do not invent).
- [ ] Run page+section tests + `content:check` (4 keys removed from each of 3 → parity holds). Gate + commit `feat(home): replace coarse trust strip with shared sla commitments ribbon`.

## Task B2 — HomeConfirmSection "What we help you confirm" (NarrativeSection, 3 points)
- [ ] Add `home.confirm.{overline,title,body,points.{compatibility,material,fit}.{title,body}}` to en/es/zh (real translations; EN: overline "BEFORE YOU ORDER", title "What we help you confirm", body "We resolve replacement membrane requests against a documented compatibility review, not a brand-name guess.", points compatibility/material/fit bodies per the authored copy — no SLA numbers, no banned adjectives).
- [ ] Failing test `src/components/sections/__tests__/home-confirm-section.test.tsx` (mock `loadCompleteMessages` with the subtree): h2 "What we help you confirm", 3 point titles present, no `/\d+\s*business (hours|days)/i`, no banned adjectives.
- [ ] Impl `home-confirm-section-view.tsx` (sync; `NarrativeSection` shell + 3-card grid as children) + `home-confirm-section.tsx` (async; `loadCompleteMessages` + `readMessagePath`, fixed keys `["compatibility","material","fit"]`). Confirm `NarrativeSection` prop names against `@/components/trust`.
- [ ] Story + `section-story-fixtures.ts` fixture (Default + ChineseCopy). Wire into `page.tsx` directly after `HeroSection`. Run tests + page test. Gate + commit `feat(home): add what-we-confirm narrative section`.

## Task B3 — HomeMembraneTypeSection "Find by membrane type" (real routes only)
- [ ] Confirm real route targets: disc = `getFeaturedProductFacts().slug` → canonical `FEATURED_MEMBRANE_HREF` (`/membranes/9-inch-epdm-disc-replacement`); tube = a real tube variant slug (`tuc-t62-epdm`) via the same canonicalization helper. Never hardcode a slug the data layer doesn't expose; never a bare `/membranes` index.
- [ ] Add `home.membraneType.{overline,title,disc.{name,body},tube.{name,body},cta}` en/es/zh.
- [ ] Failing test: disc/tube cards link `^/membranes/[a-z0-9-]+$`, not `/membranes`. `vi.unmock("zod")` (data layer parses at load).
- [ ] Impl view (sync, NarrativeSection + 2-card grid, hrefs as props) + wrapper (async, resolves hrefs from data layer). Story+fixture. Wire after HomeConfirmSection, before OemGridSection. Gate + commit `feat(home): add find-by-membrane-type section linking real routes`.

## Task B4 — OemGridSection → frozen `getOemBrandFacts()` + per-brand documented-path counts
- [ ] Add `home.oemGrid.pathCount` ICU plural en/es/zh (zh = `other` only).
- [ ] Failing test: grid renders 3 brands in frozen order `[/compatible/sanitaire,/compatible/edi,/compatible/ssi-aeration]`; each shows `getBrandPathStats(b.id).paths` documented-paths (assert equals the accessor value, never hardcoded 5/7/5).
- [ ] Impl `page.tsx`: replace `oemBrands` import with `getOemBrandFacts`/`getBrandPathStats`; map `getOemBrandFacts()` keyed by `brand.id`, `brand.displayName`/`brand.slug`; add path-count line `t("oemGrid.pathCount",{count:getBrandPathStats(brand.id).paths})`; keep existing `oemGrid.viewAll` accessible-name so brand-link + e2e contracts pass.
- [ ] e2e `homepage.spec.ts` unchanged unless displayName diverges. Run page test + `content:check`. Gate + commit `feat(home): drive oem grid from frozen brand facts with documented-path counts`.

## Task B5 — HomeRisksSection "The four risks buyers avoid" (NarrativeSection, §8)
- [ ] Add `home.risks.{overline,title,body,items.{wrongFit,wrongMaterial,blindOrder,shutdownSlip}.{title,body}}` en/es/zh; §8.5 BTEX appears only in `wrongMaterial.body` as a workload trigger (not superiority); no fabricated statistics.
- [ ] Failing test: h2 "The four risks buyers avoid", exactly 4 risk titles, BTEX wording in material-risk body, absent `/premium|high quality|better than|durable|efficient|\d+%/i`.
- [ ] Impl view+wrapper (NarrativeSection + 4-item list, fixed key list). Story+fixture. Wire after SlaCommitments, before CompatibilityProofBox. Gate + commit `feat(home): add four-risks narrative section`.

## Task B6 — Mount frozen CompatibilityProofBox / MaterialDecisionCard / BatchControlsBlock; drop legacy MaterialsSection
- [ ] Confirm frozen prop shapes (`grep` the 3 trust components). Failing test: extend the `@/components/trust` mock to stub the 3 (testids `compat-proof`/`material-decision`/`batch-controls`); remove old MaterialsSection text assertions; assert the 3 markers render in order and old `home.materials` copy gone.
- [ ] Impl `page.tsx`: import the 3; delete `MaterialsSection` fn + `MATERIAL_ITEMS`; render after `HomeRisksSection` in order ProofBox → MaterialDecisionCard → BatchControlsBlock with `locale={locale as Locale}`.
- [ ] Remove dead `home.materials.*` from all 3 locales (parity); `grep` no other consumer; update `i18n-validation.ts` if referenced. Run page test + `content:check`. Gate + commit `feat(home): mount frozen trust proof/material/batch blocks, drop legacy materials section`.

## Task B7 — HomeFaqSection Q01–Q06 via reused FaqSection (§5, direct mode)
- [ ] Use `FaqSection` **direct `faqItems` mode** (page resolves `home.faq.*` and passes `{id,question,answer}[]`) so copy stays home-owned and still produces FAQPage JSON-LD (no hand-rolled schema; `.claude/rules/content.md` exception — home is the no-MDX structured campaign landing).
- [ ] Add `home.faq.{sectionTitle,items.q01..q06.{question,answer}}` en/es/zh. Q04 = §2 unified lead-time wording NO bands; Q01/Q06 = §1 SLA + §3.7 softened remedy ("we work from the documented compatibility-review record" — no warranty/result promise); Q05 made-to-order, no "5–7 day"; only email `sales@tucsenberg.com`.
- [ ] Failing content-guard test `home-faq.test.tsx`: per locale, no banned adjective, no lead-time band/`5–7 day`/`MOQ`/year, no `quote@|quality@|legal@`, only `sales@tucsenberg.com`, keys exactly `q01..q06`.
- [ ] Wire `FaqSection` (direct mode) into `page.tsx` after `BatchControlsBlock`, before `FinalCta`; update `page.test.tsx` (add `home.faq.*` to messages, assert Q01 + a FAQPage JSON-LD script). Run faq tests + `content:check`. Gate + commit `feat(home): add q01-q06 faq via reused faq-section with spec-aligned copy`.

## Task B8 — Single quote CTA + footer TrademarkDisclaimer; BC-001 update
- [ ] Failing test: stub `TrademarkDisclaimer` (R2: assert via `data-testid="trademark-disclaimer"` + `data-variant="footer"`); rewrite final-CTA test to single quote CTA (`Request a Quote` → `SINGLE_SITE_ROUTE_HREFS.quote`), assert NO "Browse All Membranes" link, assert footer disclaimer present.
- [ ] Impl `page.tsx`: in `FinalCta` delete the second "Browse All Membranes" Button/Link; remove now-unused `BROWSE_ALL_MEMBRANES_HREF`/`FEATURED_MEMBRANE_HREF` import iff unused in-file (grep first — keep the exported const, other modules use it); render `<TrademarkDisclaimer locale={locale as Locale} variant="footer"/>` as last child.
- [ ] Remove dead `home.cta.viewMembranes` from 3 locales (parity; grep no consumer). Update `homepage.spec.ts` single-CTA + footer-disclaimer contract (use real Phase-A `legal.*` footer wording). Update `docs/specs/behavioral-contracts.md` BC-001 (single final CTA → quote; page ends with footer trademark disclaimer) — append under `### Step 4.1 — Phase B` (R3). Run page test + `content:check`. Gate + commit `feat(home): single quote cta and footer trademark disclaimer`.

## Task B9 — §4 strike audit + full home regression + DEV-LOG
- [ ] Failing/guard test `home-strike-audit.test.tsx` (same mock harness, `vi.unmock("zod")`): rendered home text matches NONE of the §4 strike list (24/19 paths, 6 families, Aercor/Stamford/Nopon, Phase 1.5, 2026-04-22, CRR v2026, last reviewed, 5–7 day/year, MOQ, 1–2 weeks digit-form, tear-down, premium/high quality/better than EPDM/durable/efficient); only email `sales@tucsenberg.com`. Fix at source if any token found (never weaken regex).
- [ ] Full home regression: `pnpm test src/app/[locale]/__tests__/ src/components/sections/__tests__/`; manually verify render order. e2e `pnpm exec playwright test tests/e2e/homepage.spec.ts` (en+es). Phase-B exit gate (sequential incl. `pnpm build` then `pnpm website:build:cf`, never parallel).
- [ ] Append Phase-B entry to `DEVELOPMENT-LOG.md`. Commit `test(home): add spec §4 strike audit and record phase b completion`.

## Phase B exit
- 12-item structure in order; all facts via frozen accessors; `home.trust`/`home.materials`/`home.cta.viewMembranes` removed at parity, every new `home.*` has real EN/ES/ZH; no §4 strike tokens; single sales email; BC-001 updated; full gate + both builds green.
