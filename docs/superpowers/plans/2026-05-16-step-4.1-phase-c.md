# Step 4.1 — Phase C: `/membranes/[product]` Rebuild

> Part of `2026-05-16-step-4.1-master.md`; **master R1–R7 binding**. Depends on **merged Phase A**. Phase C OWNS `membraneProduct.*` only. Per-task TDD + gates + commit conventions as Phase A. Reuse the existing `page.test.tsx` locale-resolving message mock + async-server-component test pattern. **Regression fence (do NOT touch): `generateStaticParams`, `generateMetadata`, the SKU→canonical 308 `permanentRedirect`, `quoteHref` `?sku=&product=` shape — the existing `page.test.tsx` (11 assertions) + `product-slug.test.ts` + `sitemap.test.ts` + nav e2e are the fence.** No Client Components added. R2: assert disclaimers via `data-testid="trademark-disclaimer"`+`data-variant`.

## Verification Gate 0 (no commit, blocks all C tasks)
- [ ] Probe `getFeaturedProductFacts`/`getOemBrandFacts`/`getBrandPathStats`/`CATALOG_FACTS`, `src/components/trust/index.ts`, `trust`/`legal` i18n. Any `*-MISSING` → STOP (Phase A not merged); do not stub.

## Task C1 — Trademark-compliance fix: page-bottom inline disclaimer (highest-risk, first)
- [ ] Probe current state: `grep -n "TrademarkDisclaimer\|trust\." src/app/[locale]/membranes/[product]/page.tsx` (expect `PAGE-HAS-NO-TRADEMARK-DISCLAIMER`); `grep -c trademarkDisclaimer …/compatibility-section.tsx` (non-zero — per-brand disclaimers stay, never moved).
- [ ] Failing test `__tests__/trademark-compliance.test.tsx`: stub `CompatibilitySection` away + stub `@/components/trust` `TrademarkDisclaimer` as `data-testid="trademark-disclaimer" data-variant`; assert a page-level disclaimer with `data-variant="inline"` renders AFTER the compatibility section (proves it is the page-level one, not the in-section per-brand ones). Mocks: `next/navigation`, `@/i18n/routing`, `next-intl/server`, `@/components/seo` per existing pattern; `vi.unmock("zod")`.
- [ ] Impl: import `TrademarkDisclaimer`; render `<section className="px-6 pb-20"><div className="mx-auto max-w-[1080px]"><TrademarkDisclaimer locale={locale as Locale} variant="inline"/></div></section>` as the final block after the quote CTA section, before closing `</div>`. Do not touch `CompatibilitySection`.
- [ ] Run trademark test → pass; run existing `page.test.tsx` → all 11 still green (canonical params, spec bar, 308 redirect w/ locale prefix, unknown-slug 404, route-specific canonical, en+es+x-default hreflang never zh, indexable robots, empty metadata). Gate + commit `fix(membranes): add page-bottom inline trademark disclaimer for oem-page compliance`.

## Task C2 — i18n: `membraneProduct.*` rebuild keys (en/es/zh, additive)
- [ ] Read current `membraneProduct` block (keep existing `hero`/`compatibility`/`cta`; do not rename — `page.test.tsx`/`i18n-message-contract` pin them). Add subtrees `useCase{eyebrow,title,body}`, `materialFit{eyebrow,title,body}`, `confirmFit{eyebrow,title,body,steps.{partNumber,dimensions,mounting,material,perforation,release}}`, `leadTime{eyebrow,title,body}` (body = §2 unified word-form "…typically ship within one to two weeks of confirmed compatibility and an accepted quote. Staged delivery for shutdown windows can be arranged on request." — NO digit band, NO MOQ, NO day count), `qc{eyebrow,title,sampleNote}` (sampleNote no day/piece numbers), `quote{eyebrow,title,body,email:"sales@tucsenberg.com"}` — full EN per authored copy, real ES, real ZH.
- [ ] Add `"membraneProduct.quote.email"` to `ES_IDENTICAL_ALLOWLIST` in `tests/unit/i18n-message-contract.test.ts` (shared sales address, technical-token category). Confirm `i18n-public-es-placeholder.test.ts` passes (no `[ES-TODO]`/`[ZH-TODO]`).
- [ ] `pnpm content:check` equal leaf totals; `i18n-message-contract` + placeholder tests green; `brand:check` (no banned adjectives). Commit `feat(i18n): add membraneproduct narrative, material, confirm-fit, lead-time and qc keys`.

## Task C3 — Hero + spec strip: strike non-data fields, source diameter from `getFeaturedProductFacts`
- [ ] Failing/contract test `__tests__/spec-strip-contract.test.tsx` (copy the `resolveMessage`+`CRITICAL_BY_LOCALE` setup from `page.test.tsx`): spec `<dl>` has Diameter/Material/Type/SKU; SKU = `TUC-D9-EPDM`; diameter matches `9"|228.6 mm`; text has NO `°C`, NO `threaded|ring|bayonet`, NO `5–45`. (May pass immediately = valid contract guard per §4.)
- [ ] Route featured-variant diameter through `getFeaturedProductFacts()` (when `entry.sku===getFeaturedProductFacts().sku` use its `.diameter`; else keep `diameterFor()`). No DOM-shape change — existing spec-bar assertions stay green.
- [ ] Run contract + full `src/app/[locale]/membranes/[product]/` suite. Gate + commit `feat(membranes): lock spec strip to real data fields via featured-product facts`.

## Task C4 — "What this product is for" narrative (B1 §3)
- [ ] Failing test `__tests__/narrative-use-case.test.tsx` (C3 scaffold; `NarrativeSection` mock renders title/body/children): resolved `membraneProduct.useCase.title` ("A drop-in aeration membrane replacement") renders between hero spec strip and `CompatibilitySection`.
- [ ] Impl: import `NarrativeSection`; render `<NarrativeSection eyebrow={t("useCase.eyebrow")} title={t("useCase.title")} body={t("useCase.body")}/>` after hero, before `CompatibilitySection`. Gate + commit `feat(membranes): add what-this-product-is-for narrative section`.

## Task C5 — Material-fit card + confirm-fit process (B1 §4–§5)
- [ ] Failing test `__tests__/material-and-confirm-fit.test.tsx`: `MaterialDecisionCard` present with `defaultMaterial="epdm"`; all six `confirmFit.steps.*` strings render in order; confirm-fit section text has NO `/\d+\s*(day|days|business day|week|weeks)\b/i`, NO `500`, NO `°C`.
- [ ] Impl: import `MaterialDecisionCard`; after use-case section render a material-fit section (eyebrow/title/body + `<MaterialDecisionCard locale={locale as Locale} defaultMaterial="epdm"/>`) then a confirm-fit `NarrativeSection` whose children is an ordered `<ol>` mapping fixed keys `["partNumber","dimensions","mounting","material","perforation","release"]` → `t("confirmFit.steps.<k>")`. Phase C does NOT re-list EPDM/TPU triggers — the card owns that from Phase-A `trust.*`. Gate + commit `feat(membranes): add material-fit card and confirm-fit process section`.

## Task C6 — CompatibilitySection fabrication guard (§4)
- [ ] Contract test `__tests__/compatibility-section.test.tsx` rendering `CompatibilitySection` with the real `getProductCompatibilityByCanonicalSlug("9-inch-epdm-disc-replacement")` entry: fit labels only from `{Exact Fit, Verify Dimensions, Custom}` (resolve each `model.fitStatus` via the canonical key and assert that exact label appears); text has NO `TB-DSC-`, `AFD350`, `bayonet`, `\bSS-9\b`, `other.?batch`, `Recorded fit`, `Custom review`; real data tokens present (first model name + one OEM part number from the entry). Likely passes first run = intended executable §4 guard; if it fails, fix the section to read only canonical keys/data (never weaken). Gate + commit `test(membranes): pin canonical fit labels and forbid fabricated compatibility tokens`.

## Task C7 — Lead-time (no bands) + QC/sample (B1 §7–§9)
- [ ] Failing test `__tests__/lead-time-and-qc.test.tsx`: resolved `leadTime.title`/`body` render; `BatchControlsBlock` present; QC/lead-time region matches NONE of `/\d+\s*[–-]\s*\d+\s*(day|days)/i`, `/\d+\s*day/i`, `/500\+?\s*(piece|pcs|pieces)/i`, `/\bMOQ\b/i`, `/quantity band|qty band/i`; the allowed word-form "typically ship within one to two weeks" is present.
- [ ] Impl: import `BatchControlsBlock`; after confirm-fit, render a text-only lead-time `NarrativeSection` (no table) then a QC section (eyebrow/title + `<BatchControlsBlock locale={locale as Locale}/>` + `qc.sampleNote`). Gate + commit `feat(membranes): replace lead-time table with unified wording and qc/sample block`.

## Task C8 — Quote CTA panel + stacked SLA + proof box; single sales email
- [ ] Failing test `__tests__/quote-cta-and-proof.test.tsx`: existing quote link href UNCHANGED (`/quote?sku=TUC-D9-EPDM&product=9-inch-epdm-disc-replacement`); `SlaCommitments` present `data-layout="stacked"`; `CompatibilityProofBox` present; body contains `sales@tucsenberg.com`, NONE of `quote@|quality@|legal@`, no `/tear.?down/i`.
- [ ] Impl: import `SlaCommitments`,`CompatibilityProofBox`; replace bare CTA `<section>` with a panel keeping the exact existing `quoteHref` Button/Link + `quote.*` eyebrow/title/body + `mailto:sales@tucsenberg.com` + `<CompatibilityProofBox/>` + `<SlaCommitments layout="stacked"/>`; keep the C1 `TrademarkDisclaimer` as the final element. Gate + commit `feat(membranes): add quote cta panel with stacked sla, proof box and single sales email`.

## Task C9 — Behavioral-contract + e2e alignment
- [ ] Consolidated structure-contract test `__tests__/page-structure-contract.test.tsx` (real data + real messages, full trust mock): asserts document order hero spec strip → use-case → MaterialDecisionCard → 6 confirm-fit steps → CompatibilitySection → lead-time → BatchControlsBlock → quote CTA (exact href) → stacked SLA → page-bottom inline TrademarkDisclaimer; plus no `°C`/`\d+\s*day`/`500`/`tear.?down`/`quote@|quality@|legal@`.
- [ ] Append a `### Step 4.1 — Phase C` subsection to `docs/specs/behavioral-contracts.md` (R3) recording the new product-page structure + the unchanged canonical-slug/308/sitemap sentences; cite the 9 spec files. Run cited e2e (`navigation`, `basic-navigation`, `user-journeys`) → nav/URL/redirect assertions intact (update only genuinely-changed internal-structure assertions, with cited reason, never the URL/redirect ones). Gate + commit `test(membranes): pin phase 4.1 product-page structure and update behavioral contract`.

## Phase C exit gate
- [ ] Full sequential gate (incl. `pnpm build` then `pnpm website:build:cf`, never parallel). Membrane suite (9 spec files) green. Regression fence green (`product-slug.test.ts`, `sitemap.test.ts`, nav e2e). Strike-audit grep over the page tree + 3 locale files (excl. tests) → `STRIKE-AUDIT-CLEAN`. Trademark compliance test green; per-brand disclaimers still inside `CompatibilitySection`.
