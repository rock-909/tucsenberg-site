# Step 4.1 — Phase D: `/compatible/[brand]` Rebuild

> Part of `2026-05-16-step-4.1-master.md`; **master R1–R7 binding**. Depends on **merged Phase A**. Phase D OWNS `compatibleBrand.*` only (reads `trust.*`/`legal.*`). Per-task TDD + gates + commit conventions as Phase A, plus `node scripts/starter-checks.js client-boundary` (budget must stay unchanged — Phase D adds no client island; `BrandCompatibilityFilter` already registered). **Hard compliance (CLAUDE.md #3): top brand-notice disclaimer AND footer disclaimer, both test-locked.** R2: assert disclaimers via `data-testid="trademark-disclaimer"`+`data-variant` (or a page-side wrapper only if two same-variant could collide). **Regression fence: `generateMetadata` (path-aware canonical/hreflang en/es-never-zh, empty `{}` on unknown brand) + `sitemap.ts` untouched.** Brand iteration always via `getOemBrandFacts()` (3 real brands only).

**Render order:** top `TrademarkDisclaimer variant="brand-notice" brandName={brand.displayName}` → hero (overline + `<h1>{displayName}</h1>` + ICU `{brand}` description) → NarrativeSection "What 'compatible with {brand}' means here" → NarrativeSection "Confirm your installed setup" → stats line (`getBrandPathStats`) + existing `BrandCompatibilityFilter` (facets **category + material only**) → MaterialDecisionCard → CompatibilityProofBox → BatchControlsBlock → "Request a compatibility review" CTA → /quote + SlaCommitments stacked → footer `TrademarkDisclaimer variant="footer"`.

## Task D1 — `compatibleBrand.*` i18n (add `boundary`,`intake`,`stats`,`cta`; en/es/zh)
- [ ] Failing test `tests/unit/compatible-brand-i18n.test.ts`: each of `boundary.{eyebrow,title,body}`,`intake.{eyebrow,title,body}`,`stats.{summary,epdm,tpu}`,`cta.{eyebrow,title,body,action}` non-empty string in en/es/zh, no `[ES/ZH/EN-TODO]`; `stats.summary` uses `{paths}` ICU, not `6 documented`, no `228`.
- [ ] Add keys to all 3 locales appended after `results` (preserve order). EN per authored copy: `boundary.body` "Tucsenberg supplies aftermarket replacement membranes dimensioned to fit referenced {brand} diffuser models. {brand} is a third-party trademark; …A documented compatibility review against your installed part precedes every quote."; `stats.summary` "{paths} documented compatibility paths", `stats.epdm` "{epdm} EPDM", `stats.tpu` "{tpu} TPU"; `cta.body` "…we reply from sales@tucsenberg.com." Real ES + real ZH. Do not delete existing `compatibleBrand.disclaimer`/`results.fitStatus`/`confidence` (other tests reference shape; filter still renders fit/confidence badges).
- [ ] `content:check` + `i18n-message-contract` + `i18n-public-es-placeholder` green. Commit `feat(i18n): add compatible-brand narrative, stats and cta keys`.

## Task D2 — Real fact-driven stats line + frozen brand-fact wiring
- [ ] Read Next 16 async-server-component + `generateStaticParams` docs; re-read current `page.test.tsx` (H1 Sanitaire, /Xylem/ disclaimer, quote href `/quote?brand=sanitaire`, tube-tab filter, `notFound()`, full `generateMetadata` block — must not regress).
- [ ] Failing test additions: `data-testid="brand-stats"` shows `getBrandPathStats("sanitaire")` numbers 5/3/2, NOT "6 documented"/"228"; `generateStaticParams()` slugs sorted `["edi","sanitaire","ssi-aeration"]`.
- [ ] Impl `page.tsx`: import `getOemBrandFacts`/`getBrandPathStats`; `generateStaticParams` = `routing.locales.flatMap(l=>getOemBrandFacts().map(b=>({locale:l,brand:b.slug})))`; resolve `brandFact=getOemBrandFacts().find(b=>b.slug===brand); if(!brandFact) notFound();` `stats=getBrandPathStats(brandFact.id)`; keep `getBrandCompatibility(brand)` for rows; H1/ICU use `brandFact.displayName`; add `<p data-testid="brand-stats">{t("stats.summary",{paths:stats.paths})} · {t("stats.epdm",{epdm:stats.epdm})} · {t("stats.tpu",{tpu:stats.tpu})}</p>` under H1. **`generateMetadata` untouched.**
- [ ] Run page test (all incl. preserved metadata) + `type-check`. Commit `feat(compatible): drive brand stats from frozen fact layer`.

## Task D3 — Compose trust components + narrative sections
- [ ] Read Phase-A trust source (props/async; `NarrativeSection` sync `{eyebrow?,title,body?,cta?,children?,divider?}`). Failing test cases: top brand-notice disclaimer scoped to resolved brand (`data-variant="brand-notice"` + brand text); footer disclaimer present (`data-variant="footer"`); request-review CTA `href` contains `/quote` + `brand=<slug>`. (R2: assert via `data-testid="trademark-disclaimer"`+`data-variant`; only wrap page-side if two same-variant collide — they don't here, variants differ.)
- [ ] Impl: replace the lone disclaimer `<section>` with: brand-notice `TrademarkDisclaimer` → two `NarrativeSection`s (boundary/intake, pass `t(...,{brand:brandFact.displayName})`) → keep stats+`BrandCompatibilityFilter` → `<MaterialDecisionCard/>` → `<CompatibilityProofBox/>` → `<BatchControlsBlock/>` → CTA `NarrativeSection` with `cta`→`<Link href={`/quote?brand=${encodeURIComponent(brandFact.slug)}` as "/">` + `<SlaCommitments layout="stacked"/>` → footer `TrademarkDisclaimer variant="footer"`. Await async trust components per Phase-A usage. Keep `JsonLdGraphScript`; no hand-rolled schema. Run page test (notFound/metadata/filter/new composition all green). Commit `feat(compatible): compose trust blocks and narrative sections`.

## Task D4 — Facet reduction guard (category + material only)
- [ ] Guard test: exactly one `tablist` with 3 tabs (category All/Disc/Tube) + exactly one `combobox` (material); `queryByRole("tab",{name:/membrane class/i})`/`/mount style/i`/`combobox{name:/fit status/i}` all null. Likely passes immediately (filter already category+material) = intended §4 guard; if fails, trim `brand-compatibility-filter.tsx` to category+material only (no new facet code). Commit `test(compatible): lock facets to category and material only`.

## Task D5 — Client-boundary verification
- [ ] `node scripts/starter-checks.js client-boundary` + `pnpm test` → max unchanged (no new island; all trust comps server; `NarrativeSection` sync server). Only if a new boundary appeared: add to `allowedClientBoundaries` (sorted) + bump max by exactly N, re-run green. If no-op, fold verification into D6 (no empty commit).

## Task D6 — Behavioral contract + sitemap/canonical/hreflang non-regression + full gates
- [ ] `pnpm test src/app/__tests__/sitemap.test.ts "src/app/[locale]/compatible/[brand]/__tests__/page.test.tsx"` → `generateMetadata` (route canonical, en+es+x-default, never zh, robots index, empty `{}` unknown) + BC-023 (one OEM URL per brand, no zh) green; brand set still exactly 3 slugs.
- [ ] `tests/e2e/navigation.spec.ts` (BC-002): page still renders `<h1>{displayName}</h1>` → no spec change; run Playwright if env, else statically confirm spec only asserts H1+href; don't weaken.
- [ ] Append `### Step 4.1 — Phase D` to `behavioral-contracts.md` (R3): brand-notice + footer disclaimers (compliance), fact-driven stats, category+material facets only, trust composition; keep prior text intact. Full sequential Phase-D gate (incl. `build`, never parallel with cf). Commit `docs(compatible): record phase-d contract and verify gates`.

## Phase D exit
- All 6 tasks committed, each independently shippable. Full gate + client-boundary green. Compliance: brand-notice + footer disclaimers test-locked. Facets category+material only, locked. Every number from `getBrandPathStats`/`CATALOG_FACTS` (no 6/3, no 228 literal, no struck labels/timestamps/brands). i18n parity; sitemap/canonical/hreflang (en/es never zh) + `generateMetadata` unchanged; BC-002/BC-023 hold. Single `sales@tucsenberg.com`; CTA → `/quote?brand=`.
