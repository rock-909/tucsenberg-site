# Step 4.1 — Phase E: `/quote` Rebuild (wrap-only)

> Part of `2026-05-16-step-4.1-master.md`; **master R1–R7 binding (esp. R7 frozen RFQ surfaces)**. Depends on **merged Phase A**. Phase E OWNS `quote.*` only. Per-task TDD + gates + commit conventions as Phase A. **FROZEN — byte-unchanged by Phase E (wrap, do not rewrite): `src/app/api/quote/route.ts`, `src/lib/lead-pipeline/lead-schema.ts`, `quote-form.tsx`, `use-quote-form.ts`.** Phase E edits only `page.tsx`, `messages/**` (`quote.*`), `quote/__tests__/*`, `behavioral-contracts.md`. Suites that MUST stay green every gate: `src/app/api/quote/__tests__/quote-api.test.ts`, `src/app/[locale]/quote/__tests__/page.test.tsx`, `tests/e2e/navigation.spec.ts`. No new client island (budget unchanged). R2: disclaimers via `data-testid="trademark-disclaimer"`+`data-variant`.

## Task E0 — Phase-A probe + scope lock (no code)
- [ ] Verify `src/components/trust/` exports the 6 components with documented signatures (async; `NarrativeSection` sync). Verify `@/data/product-compatibility` `getOemBrandFacts`/`getFeaturedProductFacts`/`CATALOG_FACTS`; record real `CATALOG_FACTS.totalCompatibilityPaths` (17 — never hardcode; Phase E surfaces no count). Confirm prop contracts. Record exact client-boundary command + `maxClientBoundaries` (must NOT change). Inventory specs asserting old `/quote` structure (only `quote/__tests__/page.test.tsx` lines for hero/soft-entry/summary/prefill/context/submit/RFQ-POST/file/error/token-preserve + BC-026/BC-002). If Phase A absent → STOP.

## Task E1 — `quote.*` i18n expansion (en/es/zh, additive)
- [ ] Failing parity+content test `quote/__tests__/quote-i18n.test.ts`: identical `quote.*` key sets en/es/zh; new namespaces `intake`,`materialGuidance`,`whatHappensNext`,`proof`,`batch`,`assurances`,`legal` defined; no banned adjective in any `quote.*` value; no `\b\d+\s*(weeks?|days?|pcs?|pieces?)\b` numeric promise.
- [ ] Add EN keys (keep existing `softEntry`/`form`/`summary`/`success`): `intake{eyebrow,title,body}`, `materialGuidance{eyebrow,title,body}` (EPDM default / TPU triggered by oil/chemical/grease — not premium), `whatHappensNext{eyebrow,title}`, `proof{eyebrow,title,body}` ("…reviewed against documented dimensions and photos…Pricing is not released against a brand-name match alone."), `batch{eyebrow,title}`, `assurances{nonBinding:"Quotes are non-binding until confirmed by a purchase order.",privacy:"We use your details only to review compatibility and prepare your quote. We do not sell or share your information."}`, `legal{consent:"By submitting this request you agree to our {privacyLink}.",consentWithReviewTerms:"…{privacyLink} and {reviewTermsLink}.",privacyLinkLabel,reviewTermsLinkLabel}`. **R5:** both `consent` and `consentWithReviewTerms`+`reviewTermsLink*` exist (parity-safe) but only `consent` is rendered in Phase E. Real ES + real ZH (ICU placeholders unchanged).
- [ ] `quote-i18n.test.ts` + `content:check` green. Commit `feat(i18n): add quote page narrative and trust copy keys`.

## Task E2 — Hero + structured-intake framing (NarrativeSection wrap)
- [ ] Strengthen (not delete) the soft-entry test: H1 from `quote.hero.title` still renders; a section with `quote.intake.title` renders above the `quote-form` testid; existing `quote.softEntry.*` still above the form.
- [ ] Impl `page.tsx`: keep `JsonLdGraphScript`/`generateStaticParams`/`generateMetadata`/Suspense `QuoteFormSection` exactly. Replace inline hero+soft-entry `<section>`s with `<NarrativeSection eyebrow={t("intake.eyebrow")} title={t("hero.title")} body={t("intake.body")} divider/>` + a soft-entry block reusing existing styling (`t("softEntry.*")`). Keep container shell. Full `page.test.tsx` green. Gate (incl. `quote-api.test.ts`) + commit `feat(quote): frame hero and intake with narrative section`.

## Task E3 — Path-adaptive form copy (i18n-only; form logic frozen; strike bands)
- [ ] Confirm `quote-form.tsx` `quantity`/`shutdownDate` are plain optional fields (no band/table) — no code change. Add guard to `quote-i18n.test.ts`: `quote.form` no key `/band|moq|minimum|leadTimeTable/i`; quantity/shutdownDate hints no piece-count/week-band.
- [ ] Refine copy in 3 locales: `partNumbersHint` "Enter a Tucsenberg SKU, OEM part number, or model name — the more specific, the faster we confirm the path."; `quantityHint` "Approximate quantity for this order. Lead time and pricing are confirmed at quote against your reviewed part." (no week numbers); `shutdownDateHint` staged-delivery wording. Leave `fileUpload*`/`success.fileNotice` EXACTLY as shipped (frozen honest metadata-only, §9.7 — do not inflate to "5 files/20MB"). Gate + commit `feat(quote): path-adaptive form copy, strike quantity-band lead-time table`.

## Task E4 — MaterialDecisionCard (material-guidance, §7)
- [ ] Failing test (mock `@/components/trust` per E6 convention): `quote.materialGuidance.title` renders + card marker. Impl: after form section render `<NarrativeSection eyebrow title body><MaterialDecisionCard locale={locale as Locale} defaultMaterial="epdm"/></NarrativeSection>`. Gate + commit `feat(quote): add material decision card guidance`.

## Task E5 — Sticky summary copy lock (presentational, no rewrite)
- [ ] Strengthen the empty-summary test: after typing `partNumbers`+`fullName` the summary echoes them; summary shows `quote.summary.responseTimeValue` ("Within 2 business days") + `leadTimeValue` ("Confirmed during quote review") with NO week/day number. SummaryPanel is frozen-structural — test-only pin. Commit `test(quote): pin sticky rfq summary to real fields and non-numeric sla copy`.

## Task E6 — "What happens next" → SlaCommitments stacked (§1: exactly 3)
- [ ] Establish the top-of-file `vi.mock("@/components/trust", …)` lightweight async stubs convention (each renders a `data-testid` + echoes a recognizable prop, e.g. `SlaCommitments`→`data-testid="sla-commitments" data-layout`); comment that real components are exercised by Phase A's suite + Task E10 build.
- [ ] Failing test: `quote.whatHappensNext.title` + `sla-commitments` `data-layout="stacked"`. Impl: after material-guidance render `<NarrativeSection eyebrow title><SlaCommitments locale={locale as Locale} layout="stacked"/></NarrativeSection>`. Gate + commit `feat(quote): add what-happens-next sla commitments (stacked)`.

## Task E7 — CompatibilityProofBox + BatchControlsBlock (§9.5/§9.6/§3)
- [ ] Failing test: `quote.proof.title` + `compatibility-proof-box` testid; `quote.batch.title` + `batch-controls-block` testid. Impl after what-happens-next: `<NarrativeSection eyebrow title body><CompatibilityProofBox locale={locale as Locale}/></NarrativeSection>` + `<NarrativeSection eyebrow title><BatchControlsBlock locale={locale as Locale}/></NarrativeSection>`. No `extraChecks`. (§9.5/§9.6 "no CRR record / no tear-down" is enforced inside the frozen Phase-A component copy.) Gate + commit `feat(quote): add compatibility-proof and batch-controls blocks`.

## Task E8 — Assurances + CRR-terms gating DECISION (§9.1/§9.2/§9.8) — privacy-only (R5)
- [ ] **DECISION (binding, conservative):** render the privacy-only consent line (`quote.legal.consent` → existing privacy page only). Do NOT render the CRR-review-terms link and do NOT create a stub CRR-terms page in Phase E. Dual-link keys stay parity-safe-unused; Step 5 owns the CRR-terms page + the i18n-only switch. State this in the commit body + cross-phase notes.
- [ ] Resolve the canonical privacy href helper used elsewhere (`src/config/paths`/`single-site-links.ts`). Failing test: `assurances.nonBinding` + `assurances.privacy` text; consent line from `quote.legal.consent` with a link `href` == resolved privacy path; `quote.legal.reviewTermsLinkLabel` text NOT present.
- [ ] Impl after batch block: the two `assurances.*` lines + the consent line via `t.rich`/`next-intl` rich-text with `privacyLink`→site-relative `<Link>` (label `quote.legal.privacyLinkLabel`), matching the existing privacy-link pattern. Gate + commit `feat(quote): add non-binding/privacy assurances and privacy-only consent` (body: CRR-review-terms deferred to Step 5 per A5.c; no stub page; dual-link keys parity-safe).

## Task E9 — Inline + footer TrademarkDisclaimer + single-inbox lock + BC-026 note
- [ ] Single-inbox guard in `quote-i18n.test.ts`: no `quote.*` value matches `/quote@|quality@|legal@/i`; any email == `sales@tucsenberg.com`. (Strip offending keys in 3 locales if any.)
- [ ] Failing test: two `trademark-disclaimer` testids with `data-variant="inline"` and `="footer"` at page bottom. Impl: last sections `<TrademarkDisclaimer locale={locale as Locale} variant="inline"/>` then footer region `variant="footer"`.
- [ ] Append `### Step 4.1 — Phase E` to `behavioral-contracts.md` under BC-026 (R3): page now wraps the frozen RFQ form with Phase-A narrative/trust; form/backend behavior + BC-026 proof boundary UNCHANGED (RFQ chain/anti-abuse/Airtable-first proven only at route layer by `quote-api.test.ts`; composition by `page.test.tsx`); CRR-terms link deferred Step 5. **BC-026 stays Partial** (no Playwright RFQ smoke / no deployed-Airtable added). Run `quote/__tests__` + `quote-api.test.ts` + `navigation.spec.ts` green. Gate + commit `feat(quote): add inline/footer trademark disclaimers, lock single sales inbox`.

## Task E10 — Phase-E exit gate (no new code)
- [ ] Full sequential gate (incl. recorded client-boundary command — count UNCHANGED; `build` then `website:build:cf` never parallel); explicitly green: `quote-api.test.ts`, `page.test.tsx`, `quote-i18n.test.ts`, `navigation.spec.ts`.
- [ ] `pnpm dev` manual render proof `/en|/es|/zh /quote`: order intake NS → soft-entry → form+sticky summary → MaterialDecisionCard → stacked SLA (exactly 3) → ProofBox (no CRR-record/sample-CRR/tear-down) → BatchControls → assurances → privacy-only consent (links real privacy page, no CRR-terms link) → inline TM → footer TM; `?sku=`/`?brand=&model=` prefill still works; submit still Turnstile-gated; success shows honest file notice; no overflow 390/768/1280; no hardcoded English in new ES/ZH sections.
- [ ] **R7 freeze proof:** `git log --oneline -- src/app/api/quote src/lib/lead-pipeline "…/quote-form.tsx" "…/use-quote-form.ts"` shows NO Phase-E commit. If one does → STOP, revert, re-wrap.
- [ ] Update `DEVELOPMENT-LOG.md` Phase E complete (wrapped; RFQ frozen+non-regressed; single sales inbox; bands struck; CRR-terms → Step 5). Commit `docs: mark step 4.1 phase e (quote rebuild) complete`.

## Phase E exit
- All sections wrapped around the frozen form; RFQ pipeline byte-unchanged + green; single `sales@tucsenberg.com` (no quote@/quality@/legal@, no route-by-intent); quantity-band lead-time table struck; CRR-terms deferred Step 5 (privacy-only consent shipped, dual-link keys parity-safe-unused); BC-026 still Partial; client-boundary budget unchanged; full gate + both builds green.
