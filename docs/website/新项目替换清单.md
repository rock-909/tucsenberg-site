# 上线替换与证明清单

按顺序替换，不要跳步。

如果只想先看“哪些文件必须替换、哪些通常可保留、哪些不要先改”，先看 `replacement-surface-index.md`。如果正在改翻译 JSON，再看 `message-namespace-map.md`。

## 1. 品牌身份

先修改这些 canonical replacement surfaces：

- 公司名
- 网站名
- 域名
- 联系邮箱
- 联系电话
- 地址
- 社交链接
- 默认品牌色
- `src/config/single-site.ts` 里的公司身份、社交链接、SEO titleTemplate 和 description
- `src/config/single-site-seo.ts` 里的 sitemap、robots、crawl / indexing truth
- `src/config/single-site-navigation.ts` 和 `src/config/single-site-links.ts` 里的导航和链接入口
- `content/pages/{locale}/about.mdx` 里的公司介绍、证明、团队和信任资产
- `content/pages/{locale}/contact.mdx` 里的联系承诺、响应时间和表单说明
- `content/pages/{locale}/privacy.mdx` 和 `content/pages/{locale}/terms.mdx` 里的法务主体、联系方式和适用条款

## 2. 产品或服务信息

Tucsenberg 公开上线前，要把整组 catalog truth 都替换掉：

- `src/config/single-site-product-catalog.ts`：市场、系列、slug、分类关系和列表页说明。
- `src/constants/product-specs/**`：材料、规格、认证、MOQ、交期、供货能力、包装和市场级产品数据。
- `messages/{locale}/critical.json`：首屏、导航、产品列表和关键买家可见 UI 文案。
- `messages/{locale}/deferred.json`：延迟加载区块、表单辅助文案和次级产品说明。
- `public/images/**`：产品/服务图片、logo、OG 图、示例 SVG 和买家会看到的图像资产。

临时示例可以保留在受控开发阶段；公开上线不能把临时示例当成真实产品事实。

### Market slug 同步要求

`market slug` 会同时影响产品 URL、规格数据、列表文案和特殊市场展示。改一个 market slug 时，不要只改页面上看到的文字；必须同步检查这几处：

- `src/config/single-site-product-catalog.ts`：market slug、market.familySlugs、family.marketSlug。
- `src/constants/product-specs/{market-slug}.ts`：对应市场规格文件名和文件内 family slug。
- `src/constants/product-specs/market-spec-registry.ts`：registry key 和 import。
- `messages/{locale}/critical.json` 里的 `catalog.markets.*`。
- `messages/{locale}/critical.json` 里的 `catalog.families.*`。
- 如果改的是特殊市场，还要同步 `src/config/single-site-page-expression.ts`。

## 3. 页面组合和首页表达

优先修改 `src/config/single-site-page-expression.ts`：

- 首页 section 顺序
- CTA 目标
- 是否展示 FAQ
- 是否展示数据指标
- 是否展示产品/服务卡片

导航和按钮链接再检查 `src/config/single-site-navigation.ts` 与 `src/config/single-site-links.ts`。不要把页面组合写回组件里。

### Static public pages

Static public pages are defined in `src/config/pages.config.ts`.

To add a static public page, update one page definition first, then add the
page content/messages and route owner:

1. Add one entry to `src/config/pages.config.ts`.
2. Add `content/pages/{locale}/<slug>.mdx` when the page is MDX-driven, or add
   `messages/{locale}` keys when the page is message/component-driven.
3. Add the matching `src/app/[locale]/<route>/page.tsx` route owner.
4. Run `pnpm exec vitest run src/config/__tests__/pages-config.test.ts`.

Do not add dynamic product market pages or blog article pages to this registry.
Those have separate source contracts.

## 4. 页面正文

修改 `content/pages/{locale}/*.mdx`：

- About
- Capabilities
- Contact
- How it works
- Product / Service detail
- Privacy
- Terms

## 5. 翻译和 UI 文案

修改 `messages/{locale}/critical.json` 和 `messages/{locale}/deferred.json`。

先按 `message-namespace-map.md` 判断命名空间优先级：`home`、`catalog`、`contact`、`products`、`customProject`、`privacy`、`terms` 这类买家可见或业务事实文案必须替换；通用错误、主题、语言切换、加载状态这类文案不要一开始乱改。

## 6. 图片资产和图片交付策略

替换 `public/images/**` 中的示例图片，并在上线前选择图片交付策略。

必须完成：

- 替换 logo、favicon、OG 图、产品/服务图、案例图和页面内所有买家可见图片。
- 删除或替换旧模板示例 SVG、示例产品图和默认分享图，不把它们当成 Tucsenberg 真实资产。
- 上传前把真实图片导出到合适尺寸和格式，避免把未压缩大图直接放进 `public/images/**`。
- 检查首屏 hero、产品卡片和详情图的显示比例、alt 文案和移动端裁切。

图片交付策略三选一：

- current baseline：默认选择。Cloudflare 构建不启用 Cloudflare Image Optimization，不要求 Cloudflare Images、Transformations、Polish、Mirage、R2 或自定义 loader。
- Cloudflare Transformations：只适合客户已经确认 Cloudflare zone、plan/quota/billing，并且需要自动 resize 或 `format=auto` 的项目。
- Cloudflare Images：只适合图片资产很多、需要上传流程、统一 variants 或 CMS 图片管理的项目。

如果选择 Cloudflare Transformations 或 Cloudflare Images，必须单独写 Tucsenberg 图片交付设计和 proof，不能只删除 `next.config.ts` 里的 `images.unoptimized` 就说完成。

## 7. 部署配置

修改 Cloudflare、域名、邮件、Turnstile、Resend 或其他实际部署配置。

- Cloudflare analytics zone id, hostname, and server-only API token.
- Owner dashboard access key.
- Cloudflare Access policy for `/ops/*` when the site is deployed.

## 8. 质量证明

运行：

```bash
pnpm brand:check
pnpm content:check
node scripts/starter-checks.js content-readiness
node scripts/starter-checks.js client-boundary
pnpm component:check
pnpm website:check
pnpm website:build:cf
```

`starter-checks.js content-readiness` 只检查买家可见输入面有没有明显占位残留；`starter-checks.js client-boundary` 只检查源码里的 client component 边界预算。

然后阅读 `docs/website/quality-proof.md`，按当前项目补齐预览部署、买家可见内容、表单 canary、可观测性和上线前人工确认。

Do not delete products, ops, Storybook, or governance tests just to make the repo look smaller. Scope reduction requires a separate migration plan.

不要把旧模板示例资产、旧项目 proof 结果或旧项目第三方配置当成 Tucsenberg 已经完成的证明。
