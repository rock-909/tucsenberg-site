# 技术问题与决策

这里只记录仍影响当前行为、上线判断或产品真实性，且目前不能直接修复的事项。

## 待确认问题

### PDF 内容真实性与源文件

现状：`product-catalog.pdf`、`spec-sheet-tb-td.pdf`、`spec-sheet-tb-bw.pdf` 和 `supplier-checklist.pdf` 已暂停公开。前两份含未经确认的声明，后两份实际渲染严重裁切。当前只公开 `spec-sheet-tb-ag.pdf` 和 `spec-sheet-tb-fb.pdf`，其产品事实已与网页逐项核对，但仍没有可访问性标签，仓库也没有可靠源文件。

影响：现有两份公开 PDF 的辅助阅读体验仍不完整，不能把旧二进制当成后续修改源文件。

当前处置：不直接修改难以复核的二进制文件。需要恢复已暂停资料时，从已确认网页内容重新制作，并同时核对网页事实、可提取文本、标题、语言元数据、可访问性标签和 `X-Robots-Tag: noindex`。

重新评估条件：owner 提供可编辑源文件，或决定重新制作整套下载资料。

## 退役规则

问题已经修复、被正式规则覆盖，或 owner 已确认后，从本台账移除。过程材料、一次性命令日志和完成计划不在这里长期保留。
