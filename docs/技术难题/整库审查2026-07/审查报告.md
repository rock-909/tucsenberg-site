# 整库权威审查报告 2026-07

- **审查日期**:2026-07-13
- **基线**:main `772be61`(整库整改 2026-07 全部 14 个 PR #67–#80 合入之后)
- **方法**:10 路维度盲审(互不喂发现)+ 4 路文档子审计 + 跨车道去重 + 单源高危发现逐条对抗核验(全部 CONFIRMED,零误报)。审计全程只读,仓库零改动。
- **证据纪律**:每条发现带 file:line、严重度(P1/P2/P3)、置信度(执行确认=实跑命令/构建/计算;静态证据确凿=代码链闭合;推断=显式标注)。
- **覆盖车道**:①运行时真相(实跑双运行时+curl 逐页)②架构/耦合(madge+分层+新代码通读)③安全与依赖 ④表单/行为合约(BC 逐条对勘+156 测试实跑)⑤a11y(全组合对比度实算)⑥i18n(死键/逃逸/加语种演练)⑦性能(构建实测+sourcemap 字节归因)⑧SEO/结构化数据/内容真相 ⑨测试与门禁有效性(含变异实验)⑩文档真相 delta(设计/项目基础/技术难题/规则四路子审计)。

## 总评

**7.5 / 10**(上轮 Codex 外部审查为 6.5)。

基础工程质量高于平均:583 文件零循环依赖、分层干净、安全姿态正确(fail-closed、注入防线、零依赖漏洞)、四个事实面(MDX/常量/消息包/JSON-LD)无数字性矛盾、上一轮整改零复发。扣分集中在:询盘转化路径上的三个真缺陷、上线证明链失效、a11y 四个 P1、标题门面问题、以及"配置/文档声称 ≠ 实际行为"类残留。

**整改闭环确认**:#67–#80 声称的成果逐项复核属实——隐私退役彻底(三路独立确认零残留)、#79 性能声明实测属实、行为合约与实现一致(除两处漂移已列)、knip/truth-docs/no-js 门禁在岗、#74 修复的正文链接色达标。无复发、无整改引入的新伤。

---

## 一、上线前必须处理(P1)

### 1.1 标题双后缀 "… | Tucsenberg | Tucsenberg"(10–11 页)【三源互证,执行确认】
- 车道1 curl 实测 + 车道8 源码链 + 内容扫描逐页清单。
- 根因:内容源写死 "| Tucsenberg" 叠加 `titleTemplate`(`src/config/single-site.ts:189`)。
- 波及:5 个产品详情页(`src/constants/tucsenberg-product-meta.ts:9-37`)、`/request-quote`(`messages/profiles/b2b-lead/en/deferred.json:140`)、oem-wholesale / privacy / terms / warranty / flood-barrier-specifications(各自 mdx `:14` 的 seo.title)。
- 修法:源标题一律去品牌后缀,统一交模板;补"最终渲染标题"断言(现有 `market-metadata-live.test.ts:79-86` 只测模板叠加前的中间产物——把关盲区)。

### 1.2 确认邮件承诺"24 小时" vs 全站承诺"12 小时"【已核验】
- `messages/base/en/deferred.json:146-147`(emailTemplates,`src/emails/email-copy.ts:8` 真实读取)说 24 小时;全站 16 个文件一致承诺"标准品 12 小时报价"。买家表单上看到 12 小时、随后邮件里看到 24 小时,书面承诺自相矛盾。
- 另:`messages/en/deferred.json:465` 等 products.inquiry.success "within 24 hours" 为疑似死键,一并清理。

### 1.3 a11y 四个 P1(全部执行确认/实算;改动极小,可并一个 PR)
1. **输入框/复选框边框近乎隐形**:`--input`(`src/app/globals.css:150`)白底对比 1.06:1、卡片底 1.01:1(要求 ≥3:1);dark 同不达标。波及两表单全部输入控件。
2. **白字压浅品牌蓝 2.94:1**:移动抽屉 CTA(`src/components/layout/mobile-navigation.tsx:78`)、当前页高亮(`:54`,2.46:1)、`src/app/global-error.tsx:68,78`。改用已达标的 `--button-primary-bg`/`--primary-text`(7.34:1)。#74 修了正文链接,这几处是漏网,非复发。
3. **焦点环 light 模式 2.83:1**(`globals.css:151` `--ring`=brand-7):高对比分支已用 brand-10(`:630`),提为默认即可。
4. **首页产品卡显式移除键盘焦点且无替代**:`src/app/[locale]/page.tsx:159` `focus-visible:outline-none`【已核验】。

### 1.4 上线金丝雀已失效,守卫失明【已核验】
- `tests/e2e/smoke/post-deploy-form.spec.ts:91` 找 `/send message|submit/i`,真实按钮是 "Send Enquiry";`:103` 成功判定 `/success|thank you|sent/i` 与真实成功文案零交集。上线当天必失败。
- 守它的 `real-service-canary-boundary.test.ts` 只查字符串在场,不校验选择器对不对——守卫绿灯、被守物已死。
- 修法:选择器从消息包导入或 data-testid;守卫升级为"选择器 ⟷ 消息真相对账"。

### 1.5 "点提交→见成功"整条转化旅程无任何自动化门禁【已核验】
- CI e2e 仅两文件(`.github/workflows/ci.yml:156`);`contact-form-smoke.spec.ts` 零次 `.click()`(已核验)。唯一点击提交的就是 1.4 那个烂 canary。
- 本地 Playwright 已配 Turnstile 测试密钥(`playwright.config.ts:195-197`),补一条 test-mode 用例入 CI 即可。

### 1.6 切正式域名的双重时点风险(上线清单硬项)
- `wrangler.jsonc:45-47` 生产 vars 仍是 `example.invalid` 占位(车道3 F3)。
- `src/config/single-site.ts:52-54` baseUrl 兜底是 workers.dev 预览域,`isBaseUrlConfigured`(`src/config/paths/site-config.ts:42`)只拦 example.com/localhost【已核验】——忘设 `NEXT_PUBLIC_SITE_URL` 时 canonical/sitemap/JSON-LD 全部静默指向 workers.dev,任何检查不报错。
- 修法:production 下 `isBaseUrlConfigured` 同时拒绝 workers.dev;写入 `docs/项目基础/上线验证.md` 强制项。

### 1.7 文档 P1(误导施工/编辑丢失)
- `docs/design/页面模式.md`、`网格系统.md` 整体停留在视觉改版前:footer 深灰带(实际白底,`globals.css:239-244`)、卡片"禁边框用阴影"(现实完全反转,阴影全为 0、`.surface-card` 恰用边框)——照文档施工会做反。
- `.claude/rules/content.md:30,42` 把文案编辑面指向**生成的**兼容文件 `messages/{locale}/*.json`【已核验】——照做会被 `messages:sync` 覆盖丢改。真相是物理包 `messages/base/`、`messages/profiles/`。
- `docs/项目基础/文档清单.md:16` "登记闭环完成"为假(10 个 tracked 文件漏登记);`替换边界.md:22` 的验证命令指向不存在的测试文件。

---

## 二、应修(P2)

### 转化路径(建议同一批优先处理)
| # | 发现 | 证据 | 来源 |
|---|---|---|---|
| 2.1 | **Turnstile 令牌消费后不重置**:失败重试/二次提交必被拒,卡约 5 分钟。两表单均中 | `src/lib/forms/use-lead-form-submission.ts:114-127` 无重置;全仓无 `widget.reset()` 生产调用 | 车道2+4 双盲互证 |
| 2.2 | **RFQ 客户端限 4000 字、服务端收 2000 字**:长询盘(最高价值买家)必失败且提示"检查字段"无自救线索【已核验】 | `request-quote-form-fields.tsx:10` vs `validation-limits.ts:30`;limit-consistency 测试不覆盖 message | 车道4 |
| 2.3 | RFQ 错误语义混同(验证失败/503/500 全显示"检查字段",服务端 details 被丢弃);`?interest=` 超 200 字符时买家陷入不可见字段死局 | `request-quote-form.tsx:21-30,38-44` | 车道4 |
| 2.4 | **买家多段落留言在邮件里被压成一行**【已核验】:发送层二次消毒 `sanitizePlainText` 吃掉换行,与 schema 层刻意保留换行的真相冲突;Airtable 正常,两渠道不一致 | `src/lib/resend-utils.ts:49,140-142` vs `lead-schema.ts:61-62` | 车道3 |
| 2.5 | **电话字段三层断链**:配置开关是坏的——翻开后 UI 渲染、客户端发送、服务端校验,但管道构造 `leadInput` 不含 phone,买家电话被静默吞掉;同时死文案 "WhatsApp or phone number" 随页面下发 | `contact-form-config.ts:137-144`、`submit-canonical-contact.ts:315-325`、`lead-schema.ts:143-167` | 车道1+2 互证;方向待业主裁决(见四) |
| 2.6 | contact 双层校验各持真相:外层放行内层拒绝时误报 500 | `contact-field-validators.ts:92-106` vs `lead-schema.ts:158-160` | 车道4 |

### 门禁与测试基建
| # | 发现 | 证据 | 来源 |
|---|---|---|---|
| 2.7 | eslint-disable 卫生门可被**行尾注释**完全绕过(变异实验执行确认) | `scripts/quality/checks/eslint-disable.js:176-187` | 车道9 |
| 2.8 | 测试基建无条件吞掉全部 console.error,无"意外 error 即失败"机制 | `src/test/setup.console.ts:16-44` | 车道9 |
| 2.9 | `@/lib/env` 被全局 mock,其自测在测 mock 自己;production-config 校验同样测假件 | `src/test/setup.env.ts:64-184`、`env.test.ts:5-8` | 车道9 |
| 2.10 | validate-production-config 与 `pnpm audit` 无 CI 兜底(仅本地钩子,`RUN_FAST_PUSH=1` 可整跳) | `ci.yml` 全文无;`lefthook.yml` | 车道9 |
| 2.11 | 11 个 e2e spec(约 2500 行)零门禁引用,静默腐化(含产品页→联系页交接、SEO 验证等真实旅程) | `playwright.config.ts:31-36` testMatch 不含 | 车道9 |
| 2.12 | 负空间守卫成建制(断言已删名字保持不存在),违反自家 Gate Discipline | `tucsenberg-site-contract.test.ts:179-195`、`interactive-islands-usage.test.ts:4-8` 等 | 车道9+文档子审计互证 |
| 2.13 | 自证快照测试钉死 4 个死配置,其中 `SINGLE_SITE_ABOUT_STATS_ITEMS` 引用不存在的翻译键、labelKey 语义错配 | `single-site-page-expression.ts:115-155` + 其 `__tests__` | 车道2 |

### i18n / 结构
| # | 发现 | 证据 | 来源 |
|---|---|---|---|
| 2.14 | 死翻译键约 240 个(全库 771 键的 ~30%),`content:check` 无使用面检查是根因;`language` 25 死键还随每页下发客户端 | 车道6 F1(逐簇 grep) | 车道6 |
| 2.15 | 8 处 `t(key as …)` 类型逃逸打穿键校验 + 两套静默英文回退机制(漏译不报错) | 车道6 F2/F3 | 车道6 |
| 2.16 | 首页消息键仍是 starter 建站语义(`cloudflareFoundation`/`multilingual` 下挂产品文案)【已核验】,加语种前必须改名 | `messages/profiles/catalog/en/critical.json:85,109`、`single-site-page-expression.ts:41-74` | 车道2 |

### 性能
| # | 发现 | 证据 | 来源 |
|---|---|---|---|
| 2.17 | 首页为 12px 入场动画付 28.6 KB gz motion 运行时;换纯 CSS(仓库已有先例)首页首载 214→186 KB | sourcemap 归因,执行确认 | 车道7 |
| 2.18 | contact 是全站唯一动态路由,根因仅是服务端读 searchParams 渲染一条提示;挪到客户端 Suspense 即转全静态(官方文档模式,已对照安装包文档) | `contact-page-sections.tsx:245`;`.meta` 扫描执行确认 | 车道7 |
| 2.19 | Worker 包内 zod/env 校验图谱被 4 个 API 路由各复制一份(~210 KB gz,占限额 7%)——上游打包行为,记入限额监控:每加一个 zod 路由 +70 KB | `handler.mjs.meta.json` | 车道7 |

### SEO / a11y 余项
| # | 发现 | 证据 | 来源 |
|---|---|---|---|
| 2.20 | 面包屑 JSON-LD 用 `/en` 前缀 URL(会被 307),与 canonical 及另一套面包屑互相矛盾【已核验】 | `catalog-breadcrumb-jsonld.ts:16` | 车道8 |
| 2.21 | 15/16 页面无 og:image(资产存在且 1200×630 合规,只是没接)【已核验】;B2B 分享场景(LinkedIn/WhatsApp)无卡片图 | `seo-metadata.ts` 仅 3 分支带 image | 车道8 |
| 2.22 | dark 模式 `--muted-foreground` 对卡片/muted 面 3.88:1;根因是 light 把 muted 直接等于正文色"修对比度"而 dark 没同步(应与 Q3 主蓝双套一起收敛) | `globals.css:337` 等,实算 | 车道5 |
| 2.23 | 每封询盘邮件标题白字压 `#1e9df1` 头部 2.94:1;`static-theme-colors.ts` 快照滞后于 #74(未收录 `--primary-text`) | `EmailLayout.tsx:90`、`static-theme-colors.ts:10` | 车道5 |
| 2.24 | contact 表单 `aria-describedby` 引用从不存在的错误元素(读屏听不到字段级错误);主题切换器无选中态语义;页脚 hover、Turnstile 提示、ghost hover 等对比度不达标点 | `contact-form-fields.tsx:151,170`、`theme-switcher.tsx:96-111` 等 | 车道5 |

### 文档 P2(择要;完整清单见各车道原始报告)
- 设计文档 16 条 A 类(A3-A16):按钮尺寸、GridFrame 幽灵引用、H2 32/24 双轨、设计系统说明指向 4 个幽灵路径等。
- 项目基础 5 条:图标路径、3 个不存在的工具文件、GSE"活跃例外"实际零活跃、monitoring 指向不存在的证明文件。
- 技术难题/注释 10 条:inquiry 路由"产品页抽屉"幻影注释、newsletter"博客订阅"注释、3 处 Server Actions 幻影、整改执行文档自称"待执行"、性能记录断链、"this starter" 注释残留。
- 规则文件 4 条:structured-data.md 注入点描述过期(照做会双重注入)、security.md cookie 清理幻影、conventions.md import pin 措辞、i18n.md 守护不存在目录。
- CLAUDE.md 与 AGENTS.md 大段逐字重复(维护漂移风险)。

---

## 三、记账(P3,随常规迭代清理)

- **安全**:IPv6 /64 旋转可绕限流(有 Turnstile 兜底);蜜罐命中返回可区分错误自曝陷阱;csp-report 双日志放大噪音;`SECURITY_MODES` 六开关五个死且被测试钉死、`isSecurityHeadersEnabled` 三分支同一表达式;`verifyTurnstile` 死包装;`RATE_LIMIT_PEPPER_PREVIOUS`/KV_* 死环境面;`DEPLOYMENT_PLATFORM` 误配面无校验;Next 升 16.2.10。
- **表单**:双表单成功后行为不一致(RFQ 给参考号 contact 不给);校验顺序语义相反;Turnstile action 半配置(env 陷阱);`/api/subscribe` 孤儿(见四);RFQ 无 no-JS 降级说明;错误文案 tooLong 说 1000 实为 2000;contact 表单错误键映射靠英文文案字符串路由 + 2 个潜伏缺键。
- **结构**:三态模型类型层可表示非法状态(建议 discriminatedUnion)+ `resolveProductIdentity` 静默重分类;产品目录三层别名壳;specialty 死分支;`catalog-product` 臂零生产入口(注释需改)。
- **i18n**:SVG/Canvas 产品图 ~20 段图内英文、2 处硬编码 aria-label、确认邮件链路锁死英文、`seo-metadata` locale 恒等 en、`localePrefix:"never"` 是加语种第一道墙、copyright 按语种硬编码含死 zh、globals.css 残留 zh 规则、JSON-LD `defaultValue` 无效 API 用法(误导)。
- **SEO**:`PrivacyPolicy`/`TermsOfService` 无效 schema 类型(schema.org 双 404 执行确认);Article author 用 Person 装组织;Organization 缺 email/address/@id;ProductGroup 单变体自引用;oem FAQ 双真相;`/request-quote` 无 JSON-LD 且规则描述的 layout 注入点不存在;meta keywords 死机制 ~100 行;`url-generator.ts` 死 SEO 机制 −54 行;零值事实敞口;`PRODUCT_STANDARDS` 占位表;"12 小时"散写 16 文件(建议提为 site fact 插值);contact meta description 质量差;规格指南近孤页。
- **门禁**:brand:check 大小写盲区(执行确认);knip 对"仅测试引用"失明(需登记);truth-docs 措辞类锁营销句式(加语种时全要双改);CI "Lead proof" step 纯重复(且漏了真正的强证明 lead-pipeline-real);playwright `/en` 基路径遗产双份 ~50 行;CI 三种报告产物无人消费。
- **内容事实**:验货机构口径、管坝 MOQ、TB-CP 保修定位(见四);hero proof 键名撒谎(`est`/`countries` 装 12h/3年文案);`annualCapacity` 字段语义误用;孤儿 quotation-template.pdf。
- **死资产/死代码**:~90 行死颜色 token;MDX 死管线三件套(死管线链接色还不达标——启用即回退);React Email 死模板 5 文件+2 依赖;`useTurnstile`/`CONTACT_SUBJECTS` 等死代码簇;og-image.jpg/svg、about-og.jpg、showcase-hero.svg、next.svg、Figtree woff2 等死资产;`--nav-h` 死 token;`optimizePackageImports` 指向不存在依赖;tailwind-merge 8.5 KB/页(可选,有回归风险)。
- **运行时噪音**:构建日志 bailout "Error"(官方模式预期,记为已知);esbuild -0 警告(上游);middleware→proxy 废弃警告(业主已裁决搁置,升 Next 大版本前必做)。
- **待部署后实测**:PDF noindex 响应头在 Cloudflare Static Assets 下是否生效;CSP report-to 真实浏览器报文;`/en/contact` 307 行为。

---

## 四、待业主裁决项

1. **Radix Themes 试点的价签**:全站每页 68 KB gz 阻塞 CSS(占 CSS 69%)只服务 8 个试点包装器,contact 页另 +18.5 KB JS。续付还是走退役流程(涉 `docs/决策记录/UI基础方案.md` 边界)。这是全站最大单项性能收益。
2. **电话/WhatsApp 字段**:修通全链还是全链拆掉(2.5)。
3. **`/api/subscribe`**:退役,还是合约标注"headless 为未来博客保留"(三源确认孤儿)。
4. **验货机构口径**:SGS/BV vs SGS/TÜV/QIMA,哪个是真。
5. **管坝 MOQ**:"10 米起订"与 5 米单段的矛盾,真实口径。
6. **背书确认**:ASTM G154 / EN 13501 测试报告是否真实存在;security@tucsenberg.com 是否存在。
7. **邮件模板双实现**:删 React Email 死套(5 文件+2 依赖)还是让它成为唯一渲染器。
8. **i18n 简化三连**(加语种成本减半):critical/deferred 拆分退役、`messages/en/` 兼容层退役、表单配置引擎收敛(~250 行)。
9. **设计阶梯**:H2 32px(SectionHead)与 24px(.text-section)双轨,哪个是最终真相。

## 五、门禁升级建议(结构性防复发)

1. 最终渲染 `<title>` 断言(堵 1.1 类"测中间产物"盲区)。
2. 金丝雀选择器 ⟷ 消息包真相对账(堵 1.4 类"守卫看不见被守物已死")。
3. test-mode 转化旅程 e2e 入 CI(堵 1.5)。
4. `content:check` 增加翻译键使用面检查(堵 2.14 根因)。
5. `setRequestLocale` 全页架构测试(漏一处全站转动态,规则自述后果却无检查)。
6. 文档登记闭环校验(tracked docs ⊆ 清单)+ GSE 注册表反向校验 + 替换边界路径存在性校验(堵"字符串级把关查不出路径失效"整类)。
7. eslint-disable 行内扫描修复(2.7)。
8. console.error 收集断言模式(2.8)。
9. CI 加 preview 模式 validate-production-config + 每周定时 `pnpm audit`(2.10)。
10. `--primary-text` 映射与对比度换算断言入 token 契约测试。
11. 负空间守卫分类清理:买家可见残留类保留,纯内部负空间整批删除(2.12)。
12. brand:check 统一小写匹配。

## 六、跨车道互证与核验记录

- **三源互证**:标题双后缀(车道1 执行确认+车道8 源码链+内容扫描清单);`/api/subscribe` 孤儿(车道2/4/技术难题子审计)。
- **双源互证**:Turnstile 不重置(车道2/4 盲审独立命中,证据链一致);电话字段(车道1 现象+车道2 结构);负空间守卫(车道9+技术难题子审计)。
- **主会话对抗核验(全部 CONFIRMED)**:24h 邮件文案、金丝雀选择器、CI e2e 覆盖、RFQ 4000/2000、邮件压平、面包屑前缀、og:image 缺失、workers.dev 兜底、首页键语义、首页卡片焦点、content.md 编辑面、`--input` token 值。
- **执行确认类**(车道自证,抽样复核无误):全对比度计算、构建/包体全部数字、schema.org 双 404、变异实验(eslint 绕过、brand 大小写)、`pnpm audit` 零漏洞、2414 测试全绿。

## 七、通过项(实证干净的面)

运行时(双运行时逐页 200、SSR 内容真实、API 2-27ms fail-closed、Worker 2.0/3 MiB);架构(零循环、分层方向干净、新代码零类型作弊、判别器 leaf 模式正确);安全(五路由校验无绕过、Turnstile 链完整、限流原子、CSP/头收紧、PII 不落日志、邮件/Airtable 注入防线、依赖零漏洞);表单(双通道降级有执行级证明、防身份走私、提交锁、归因链自洽);a11y 基础(skip link、landmark、标题层级、reduced-motion 完整、Radix 语义未破坏、表单 label/aria-live 纪律);i18n(JSX 零裸露英文、19 错误码一一对应、setRequestLocale 覆盖完整、hreflang/sitemap 自动扩展);性能(字体零成本、懒岛纪律、18/19 全静态、#79 收益属实);SEO(sitemap/robots 三层一致、canonical 无污染、JSON-LD 拒绝造假、内链零断链、E-E-A-T 诚实定位);门禁(client-boundary 真 AST、registry 真相推导、release manifest 三方对账、部署链完整、lead-pipeline-real 模范测试);内容(核心产品参数全站一致、公司事实四处逐字一致、下载零断链)。

---

## 八、Codex 独立二审对勘(2026-07-13)

Codex 对同一基线做了独立盲审(78/100,No-Go 判定,与本报告 7.5/10 及"上线前必修"框架高度一致)。对勘结果:

### 8.1 Codex 新增、本报告漏掉的发现(主会话已逐条核验坐实)

| # | 发现 | 核验证据 | 处置 |
|---|---|---|---|
| C1 | **P2:Resend 返回 2xx 但无 message ID 时仍标记成功**,ID 被置为 `"unknown"`,`ownerNotified: true` 无凭据支撑;若同时 Airtable 失败,存在真实丢单风险 | `src/lib/email/resend-http-client.ts:112-124`(任何 2xx 即成功)+ `src/lib/resend-core.tsx:135,181,231`(`result.data?.id \|\| "unknown"`)【已核验】 | **并入第一批转化路径修复**:成功收紧为 `response.ok && data.id 非空`,删除 "unknown" 哨兵态 |
| C2 | **P2:全部 6 个公开 PDF 无结构标签(Tagged: no),其中 TB-BW 规格书可提取文本仅 ~45 词**(对照 TB-AG 474 词),supplier-checklist 文本截断错序 | 主会话 pdftotext 复测:tb-bw=45 词、tb-ag=474 词【执行确认】 | **上线前处理**:重新导出 TB-BW 与 supplier-checklist(需业主侧源文件);全量打标签列 P2 排期。这是二进制资产盲区——本报告 10 路均未查 PDF 内部 |
| C3 | **P3:`NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` 被生产门禁与部署工作流强制要求,但全仓无任何 Server Action** | `production-config.js:301` 强制 + `cloudflare-deploy.yml:141,176,198` 三处传入 + src 零 `'use server'`【已核验】 | 删除该配置面三件套(env schema 项/强制检查/workflow secret),真引入 Server Action 时再连测试一起加回 |
| C4 | **P3:quotation-template.pdf(3.27 MB,public 最大单文件之一)被架构测试钉死存在**,而全站无任何入口——测试保护的是过去的文件清单,违反 Gate Discipline | `tucsenberg-site-contract.test.ts:59` 钉死 + 3,266,628 字节【已核验】;本报告 P3 只发现"孤儿",Codex 补上了"测试让死资产无法清理"这一层 | 升级 2.12/负空间守卫清理的具体案例:删 PDF(入废纸篓)+ 删测试清单项,或先给买家入口再守护 |

### 8.2 对勘裁决:本报告一处结论被 Codex 纠正

- **上线验证.md 的"三个 placeholder blocker"是过期陈述**。本报告车道10 项目基础子审计(B7)称"当前为真";Codex 指出检查器已具名放行表单 placeholder 字段的 your@email/Your company。主会话核验:`content-readiness.js:193-197` 确有 `FORM_PLACEHOLDER_ALLOWED_RULE_IDS` 放行逻辑(带完整理由注释),`上线验证.md:122-125` 仍声称三个 blocker——**文档说谎,Codex 对、我方子审计误读**。归入文档 P2 修正清单(改 上线验证.md)。

### 8.3 Codex 佐证本报告的项(第三方独立确认)

占位域名双风险(=1.6,Codex 定为唯一 P1 阻断)、邮件压平换行(=2.4,并补充"多行全链路测试实际绕过了 ResendService、错误行为被测试钉死"的证据层)、电话字段死分支(=2.5,Codex 倾向整体拆除)、middleware→proxy(=P3 已裁决搁置项)、test-only 假 API(useTurnstile/getEmailStats 等,=车道4 Q-2 + 车道9 knip 盲区的合流,Codex 补充 getEmailStats 返回虚构零值统计)。

### 8.4 Codex 未覆盖、本报告独有的面(两报告互补性证明)

标题双后缀(1.1)、24h/12h 承诺矛盾(1.2)、a11y token 级四 P1(1.3;Codex 的 axe 自动检查测不到边框/焦点环/非默认态)、金丝雀失效与转化旅程零门禁(1.4/1.5)、Turnstile 令牌不重置(2.1)、RFQ 4000/2000(2.2)、Radix Themes 68KB 价签(四.1)、死翻译键 240 个、加语种就绪度清单、全部文档真相 delta。

### 8.5 合并后的第一批修复清单(最终版)

一(1.1-1.7 全部)+ 2.1/2.2/2.4 + **C1(Resend 空 ID)** + **C2(TB-BW/checklist PDF 重导出)** + 8.2(上线验证.md 修正)。其余排期不变。

---

*各车道完整原始报告见本轮会话记录;本报告为去重合并后的权威版本(含 Codex 二审对勘)。修复排期建议:第一批=8.5 合并清单;第二批=门禁升级(五)+ 测试基建 P2;第三批=业主裁决项落地 + P3 清理。*
