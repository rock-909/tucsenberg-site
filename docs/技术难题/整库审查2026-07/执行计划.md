# 整库审查 2026-07 修复执行计划(v6,经两轮 Codex 计划评审+业主长期技术裁决修订)

> **For agentic workers:** 若当前工具环境提供 superpowers:subagent-driven-development / superpowers:executing-plans,按其流程逐任务执行;否则用普通计划执行流程,保持测试优先、完成前验证。步骤用 checkbox(`- [ ]`)跟踪。

- **状态**:Codex 验收口径对齐（2026-07-14）：**不能**宣称整份规划结束 / M1 完成条件全绿 / 公开上线通过。可确认「M1 十一项代码 PR 已合入 `5b4cd8b`，本地+preview 技术链通过」。R1.6 公开 URL 就绪契约已按 Codex P2 收紧。审查报告/执行计划/交接已纳入本目录版本控制。**当前执行面是 M3 全部未完成任务**；2026-07-15 更新 C1/D1、通用询盘、多语言边界和业主延期范围，**不代表这些 M3 项已实施**。
- **基线**:原 main `772be61`;全部发现与 file:line 证据见同目录 `审查报告.md`(含第八节 Codex 对勘);本计划保留 v2/v3 的两轮评审修正，并在 v4-v6 回写业主长期技术裁决与当前执行范围。
- **业主裁决**(2026-07-13 业主已定,不可推翻):R'1 Radix Themes 退役 / R'2 WhatsApp / 电话作为选填联系方式 / R'3 subscribe 退役 / R'4 移除验货机构名称举例(不点名,保留"支持买家指定第三方验货") / R'5 管坝 MOQ 真实口径业主后补(该条挂起) / R'6 ASTM/EN 报告不存在→删标准声明;security@ 不建→security.txt 改 sales@ / R'7 删 React Email 死套 / R'8 i18n 简化三连采纳 / R'9 标题阶梯统一 24px。**2026-07-15 补充裁决**:R'1 范围确认为退役 `@radix-ui/themes` 运行时/全局 CSS/Theme 专属治理，保留 Radix Primitives、Radix-style 色阶方法与本地 UI Registry/Playbook;R'10 `motion` 作为**路由局部的高级动效能力受控保留**，CSS/状态属性仍是常规动效默认层，禁止 Motion 回流根布局或扩张为全站装饰系统;R'11 全站采用**一套固定、低摩擦的通用询盘表单**:仅姓名和邮箱必填，WhatsApp / 电话与需求描述选填，不显示公司、主题、产品下拉、数量、尺寸、国家、港口、预算、文件上传或多步骤字段。`/contact` 与 `/request-quote` 页面均保留，但复用同一表单、同一 `/api/inquiry`、同一验证/邮件/Airtable/成功错误链;产品与估算器背景由页面自动带入，不要求买家重复填写。12 小时口径改为“12 小时内回复;资料充分时直接报价，否则只补问必要信息”，不再对空白简表单无条件承诺报价;R'12 **本轮明确延期且只延期五类业主事项**:正式域名/切换、全部 PDF 处理、公开电话与产品照片的提供或改门裁决、管坝 MOQ、法律/联系页人工签字。五类保持未完成且只阻塞 M2，**不得阻塞 M3**；除处置账中已有证据的“不做/未来功能”外，其余 B/C/D 任务全部执行。

**Goal:** 完成全部仓库内可执行的 B/C/D 任务，只保留 R'12 明列的五类业主事项未完成；不得用假域名、假电话、假照片、假 MOQ、假签字或降低 strict gate 来制造绿灯。当前执行完成 ≠ 公开上线已证明，M2 必须等业主重新启动。

**Architecture:** 三个里程碑。M1=发布阻断代码修复(11 个代码 PR 已合入，R0 业主件延期);M2=公开上线(运维动作,当前暂停);M3=当前工程执行面(33 个 PR:B 组 7 + C 组 9 + D 组 17)。代码 PR 共 44 个,以本清单为准;里程碑完成以验收链判定,不以 PR 数量判定。**M3 可以全部完成而 M2 仍保持未开始；两者不得互相伪装。**

**Tech Stack:** Next.js 16.2.7 / React 19.2.7 / TS 6 strict / Tailwind 4 / next-intl 4 / Vitest / Playwright / Cloudflare OpenNext。

## Global Constraints

1. **PR 正式流程**(2026-07-15 业主确认,不可用更短口头版替代):
   worktree 分支 → push → **最新 head SHA** CI 全绿 → 标记 **READY_FOR_ACCEPTANCE** → **停止并等待验收** → 验收通过后标记 **ACCEPTED** → **仅业主批准合并**。
   - Cursor/agent **不得自行合并**任何 PR。
   - **CI 全绿 ≠ 验收通过**;未标 READY_FOR_ACCEPTANCE 不得提请验收,未标 ACCEPTED 不得请业主合并。
   - **有前后依赖的任务必须串行**:后续 PR 原则上等前置 PR **验收通过并合并进 main** 后再基于新 main 开工/rebase。
   - **无依赖的独立任务**可并行推进到各自的 READY_FOR_ACCEPTANCE,但仍各自停等验收与业主合并,互不代合并。
   - **M3 全部 PR 合并后**另做一次整库集成验收;单 PR ACCEPTED 不代替里程碑集成验收。
   - commitlint 小写 conventional subject。
2. **合并前硬规则**:每个 PR 进入 READY_FOR_ACCEPTANCE / 业主合并前必须 ①rebase 最新 main ②重跑受影响的生成文件(如 messages:sync)③重跑该 PR 最小验证 ④等**最新 head SHA** 的 CI 重新变绿。禁止拿旧 base 上的绿 CI 连续合并整波——尤其 R3.B2(console.error 规则)这类改全局测试语义的 PR 合入后,同波其余 PR 必须重验。
3. **生成文件冲突规则(新增)**:compat 层退役(R3.C5c)之前,凡修改消息物理包并跑 `pnpm messages:sync` 的 PR **彼此串行**(它们必然共同改写 `messages/en/*.json` 生成文件);调度矩阵同时标"源文件冲突"和"生成文件冲突"。
4. 删除文件一律移入废纸篓(macOS Trash),禁 rm/git clean;永久删除先问业主。
5. `pnpm build` 与 `pnpm website:build:cf` 绝不并行;用最小验证集。
6. Next.js API 动手前读 `node_modules/next/dist/docs/` 精确文档文件。
7. 用户可见文本走翻译键;改物理包后跑 `pnpm messages:sync`,绝不直接编辑 `messages/en/`(该层退役后此条自动失效)。
8. **门禁设计原则(新增)**:新守卫断言**行为或计算结果**(对比度比值、prerender 清单、selector 可解析性),不断言源码形状;确需源码约定检查的,在测试注释里注明"约定门"性质与豁免路径。谁说谎修谁;禁钉快照、禁守负空间。
9. 每 PR 描述引用审查报告发现编号;背景子代理"完成"声明必须 git/gh 实况核验。
10. 执行者按 file:line 与给定代码执行,禁止顺手扩大改动面;发现计划与代码现实不符时,停下来记录再走,不得临场降级验收标准。
11. **持续执行范围(R'12)**:Cursor/agent 不得因域名、PDF、公开电话/产品照片裁决、管坝 MOQ 或法律签字未到位而停下 M3；只跳过真正依赖该输入的那一步，记录 `DEFERRED_BY_OWNER` 后继续下一项。不得自行补值、改 gate 或把未完成勾成完成。
12. **其余任务必须做**:B/C/D 中未勾选任务均为当前执行项，不再以 ROI、利用率低、"可以以后做"或需要再次确认作为跳过理由。处置账中明确标为上游问题、已有证据的不做项或未来新增语种等新功能，不属于本轮任务，不得反向扩 scope。
13. **非延期外部阻塞不拖全局**:Airtable `WhatsApp / Phone` 列等不在 R'12 延期清单内，仍是必须完成的真实前置；若当前执行环境无账号权限，精确记录 blocker 并继续所有不依赖它的并行任务，不能假装完成，也不能让单个账号动作冻结整份计划。

---

# 里程碑 1:发布阻断修复(Release lane,11 个代码 PR + R0 业主件)

只含上线必要项:P1/P2 正确性、Lead 主链、转化路径、a11y 硬伤、无依据声明、域名护栏、必要 E2E。**长期重构一律不在此列。**

## M1 调度

- 波 1(并行):R1.1 ∥ R1.3 ∥ R1.4 ∥ R1.6 ∥ R1.8
- 波 2(并行,等各自依赖):R1.5 ∥ R1.7 ∥ R1.2(消息链首位空出后)
- 波 3:R1.10(等 R1.7:同改 `src/lib/__tests__/resend.test.ts`)
- **消息物理包串行链**(生成文件冲突,Global 3):R1.1 → R1.2 → R1.9 → R1.11,后者在前者合并后 rebase+重跑 sync 再送审。
- R0(业主件)与全部并行,不阻塞任何 PR,但阻塞 M2。

## M1 状态口径(代码已完成，公开上线输入未完成)

11 个代码 PR 已全部合并，最新 main 的本地质量链、production build、OpenNext build 与 production wrangler dry-run 已形成技术证据；R0 业主资料按 R'12 延期，因此**只能宣称 M1 代码 lane 完成，不能宣称 M1 全部输入就绪或 M2 可启动**。deployed smoke、真实 canary、真实收件与签字仍只属于 M2，不能提前打勾。

### Task R1.1: 标题双后缀修复 + 双层守卫

**Files:**
- Modify: `src/constants/tucsenberg-product-meta.ts:9,16,22-23,29-30,36-37`
- Modify: `content/pages/en/oem-wholesale.mdx:14`、`privacy.mdx:14`、`terms.mdx:14`、`warranty.mdx:14`、`flood-barrier-specifications.mdx:14`
- Modify: `messages/profiles/b2b-lead/en/deferred.json:140` → `pnpm messages:sync`
- Modify: `src/app/[locale]/products/[market]/__tests__/market-metadata-live.test.ts:79-86`
- Test: `src/app/__tests__/source-title-suffix.test.ts`(新建,约定门)+ `tests/e2e/no-js-html-contract.spec.ts` 增渲染断言(行为门)

- [x] **Step 1(约定门,注明性质)**:新建源标题扫描测试——遍历产品 meta、MDX frontmatter seo.title、消息 metadata.title,断言不匹配 `/\|\s*Tucsenberg\s*$/u`。测试文件头注释:"约定门:后缀唯一来源是 titleTemplate;渲染级真相由 no-js-html-contract 的 title 断言把守"。跑之预期 FAIL(11 处命中)。

```ts
import { readFileSync, readdirSync } from "node:fs";
import { describe, expect, it } from "vitest";
import { TUCSENBERG_PRODUCT_META } from "@/constants/tucsenberg-product-meta";

const BRAND_SUFFIX = /\|\s*Tucsenberg\s*$/u;

describe("source titles never carry the brand suffix (template owns it)", () => {
  it("product meta titles", () => {
    for (const meta of Object.values(TUCSENBERG_PRODUCT_META)) {
      expect(meta.title).not.toMatch(BRAND_SUFFIX);
    }
  });
  it("mdx seo.title frontmatter", () => {
    for (const file of readdirSync("content/pages/en")) {
      const src = readFileSync(`content/pages/en/${file}`, "utf8");
      const m = src.match(/seo:\s*[\s\S]*?title:\s*['"](.+?)['"]/u);
      if (m) expect(m[1], file).not.toMatch(BRAND_SUFFIX);
    }
  });
  it("message metadata titles", () => {
    const pack = JSON.parse(
      readFileSync("messages/profiles/b2b-lead/en/deferred.json", "utf8"),
    );
    expect(pack.requestQuote.metadata.title).not.toMatch(BRAND_SUFFIX);
  });
});
```

(导出名以源文件真实导出为准调整 import,不改断言语义。)
- [x] **Step 2**:逐处删除源标题尾部 ` | Tucsenberg`;requestQuote 改后 `pnpm messages:sync`;market-metadata-live 断言值同步去后缀。测试 PASS。
- [x] **Step 3(行为门)**:`no-js-html-contract.spec.ts` 增:

```ts
test("rendered <title> carries exactly one brand suffix", async ({ page }) => {
  await page.goto("/products/aluminum-flood-gates");
  const title = await page.title();
  expect(title).toMatch(/\| Tucsenberg$/u);
  expect(title).not.toMatch(/Tucsenberg\s*\|\s*Tucsenberg/u);
});
```

- [x] **Step 4**:`pnpm test` + 本地 e2e 绿;commit `fix: remove hardcoded brand suffix from source titles`,push,CI 绿,停。

### Task R1.2: 邮件承诺 24h→12h + 关联死键清理

- [x] `messages/base/en/deferred.json:146-147` 改:`preview` = `"We received your enquiry — quote within 12 hours for standard items."`;`receivedMessage` = `"Thank you for reaching out. We have received your enquiry and will reply with a quote within 12 hours for standard items (within 48 hours for custom work)."`
- [x] `grep -rn "24 hours" messages/ src/` 清零;删无消费者的 `products.inquiry.success` 键(catalog deferred.json:47);`pnpm messages:sync` + `pnpm content:check`。
- [x] `pnpm test`(email-copy 断言按新文案更新);commit `fix: align confirmation email sla copy with site-wide 12-hour promise`,push,CI 绿,停。

### Task R1.3: a11y 四 P1(token 级)+ 对比度计算门

**Files:** `src/app/globals.css:150,151,340`;`src/components/layout/mobile-navigation.tsx:54,78`;`src/app/global-error.tsx:68,78`;`src/app/[locale]/page.tsx:159`;`tests/architecture/design-token-contract.test.ts`。

- [x] **Step 1**:scratchpad 对比度脚本(OKLCH→sRGB→WCAG,审查已有公式)选值:light `--input` 取 neutral 阶首个对白底与卡片底均 ≥3:1 的档;dark `--input` 同法;light `--ring` 改 `var(--brand-10)`(高对比分支 `globals.css:630` 现值,提为默认)。实算比值写进 PR。
- [x] **Step 2**:mobile-navigation.tsx:78 → `bg-[var(--button-primary-bg)] text-[var(--button-primary-fg)]`(hover 用现有 hover token);`:54` 选中项文字 → `text-[var(--primary-text)]`;global-error 两按钮同法;page.tsx:159 删 `focus-visible:outline-none`。
- [x] **Step 2b(自 D5a 拉入的三个买家可见速修,均为一行 token/类名)**:页脚链接 hover `Footer.tsx:36` → `hover:text-[var(--primary-text)]`;Turnstile 不可用提示 `turnstile.tsx:83` + `lazy-turnstile.tsx:153` `text-destructive` → `text-[var(--error-foreground)]`;邮件头部色 `static-theme-colors.ts:10` 用于头部处改 `#005993`(brand-11 sRGB,白字 7.3:1)。
- [x] **Step 3(行为门,替代 v1 的 token 名钉死)**:design-token-contract.test 增**计算型断言**——测试内用同一 WCAG 公式解析 globals.css 实际值并断言比值:`--input` vs `--background`/`--card` ≥3、`--ring` vs 两底 ≥3、`--button-primary-fg` vs `--button-primary-bg` ≥4.5、`--primary-text` vs `--background` ≥4.5(light+dark 各跑)。未来换色值只要比值达标即通过,不钉 token 名。
- [x] **Step 4**:`pnpm test` + `pnpm lint:check` + dev 目检;commit `fix: raise input border, focus ring and mobile cta contrast to wcag aa`,push,CI 绿,停。

### Task R1.4: 金丝雀选择器绑定消息真相(结构性修法)

**Files:** Create `tests/e2e/smoke/canary-selectors.ts`;Modify `tests/e2e/smoke/post-deploy-form.spec.ts:71,90-92,102-104`;Modify `tests/architecture/real-service-canary-boundary.test.ts`;Test `tests/unit/canary-selectors.test.ts`(新建)。

- [x] **Step 1**:新建 selector builder(可单测的真实函数,消灭"硬编码 regex 漂移"这一类):

```ts
import deferred from "../../../messages/profiles/b2b-lead/en/deferred.json";

export function buildCanarySelectors() {
  const submitLabel = deferred.contact.form.submit;
  const successText = deferred.contact.form.success;
  if (!submitLabel || !successText) {
    throw new Error("canary selectors: message truth missing");
  }
  return { submitLabel, successPrefix: successText.slice(0, 40) };
}
```

(键路径以 JSON 实际结构为准——"Send Enquiry" 在 deferred.json:62 附近,成功文案 :70 附近。)
- [x] **Step 2**:单测断言 builder 返回非空字符串(**不钉具体文案值**——真值由消息包持有,journey e2e 会用它真实驱动页面;钉值只会让改文案的人双改)。
- [x] **Step 3**:canary spec 改用 builder:`getByRole("button", { name: selectors.submitLabel })`、`getByText(selectors.successPrefix)`;`:71` `/en/contact` → `/contact`。
- [x] **Step 4**:boundary 守卫删除旧"字符串在场"断言,**不新增任何源码形状断言**——builder 被 canary 与 journey(R1.5)双消费、有单测、TS import 构建期验证,失效模式已被行为层覆盖;若 boundary 测试因此只剩空壳,连文件一起退役(负空间不留)。`pnpm exec playwright test --list` 可解析;commit `fix: bind post-deploy canary selectors to message truth via testable builder`,push,CI 绿,停。

### Task R1.5: 转化旅程 e2e 入 CI

**Files:** Create `tests/e2e/contact-submit-journey.spec.ts`;Modify `.github/workflows/ci.yml:156`。

- [x] **Step 1**:新 spec(Turnstile test-key 已配,`playwright.config.ts:195-197`;label 定位与成功包络形状以 `src/lib/forms/lead-response.ts` 真实字段为准):

```ts
import { expect, test } from "@playwright/test";
import { buildCanarySelectors } from "./smoke/canary-selectors";

test("buyer fills contact form, clicks submit, sees success", async ({ page }) => {
  const selectors = buildCanarySelectors();
  await page.route("**/api/contact", (route) =>
    route.fulfill({
      status: 200,
      contentType: "application/json",
      body: JSON.stringify({ success: true, data: { referenceId: "e2e-ref-1" } }),
    }),
  );
  await page.goto("/contact");
  await page.getByLabel(/name/iu).fill("E2E Buyer");
  await page.getByLabel(/email/iu).fill("buyer@example.com");
  await page.getByLabel(/subject/iu).fill("Journey check");
  await page.getByLabel(/message/iu).fill("Ten characters minimum message.");
  const submit = page.getByRole("button", { name: selectors.submitLabel });
  await expect(submit).toBeEnabled({ timeout: 15_000 });
  await submit.click();
  await expect(page.getByText(selectors.successPrefix)).toBeVisible();
});
```

- [x] **Step 2**:本地 PASS(spec 问题修 spec;发现产品真 bug 单独上报不夹带);ci.yml:156 追加;commit `test: add browser-level contact submit journey to ci`,push,CI 绿,停。
- 依赖:R1.4 的 builder(同波开发时先各自分支,R1.5 rebase R1.4 合并后送审;或将 builder 提为 R1.4/R1.5 共同前置小 commit)。

### Task R1.6: 公开 URL 就绪判断收口 + 上线文档修正

**Files:** Modify `src/config/paths/site-config.ts:32-43`;Test 该文件现有测试;Modify `docs/项目基础/上线验证.md`。

- [x] **Step 1 失败测试**(必须包含**仓库当前真实占位值**):

```ts
const REJECTED = [
  "https://tucsenberg-site-production.example.invalid", // wrangler.jsonc 当前值
  "https://example.com",
  "https://sub.example.org",
  "http://localhost:3000",
  "http://127.0.0.1:8787",
];
const REJECTED_IN_PRODUCTION = ["https://tucsenberg-site-preview.faints-pudgier-9r.workers.dev"];
const ACCEPTED = ["https://tucsenberg.com", "https://www.tucsenberg.com"];
```

- [x] **Step 2 实现**:`isBaseUrlConfigured` 重写为模式清单驱动(覆盖 `.example`(含 example.invalid/com/org/net)、`localhost`、`127.0.0.1`、production 下 `workers.dev`)。**收口优先级**:①首选提取无运行时依赖的纯共享模块(如 `scripts/quality/public-url-readiness.mjs`),让 `isBaseUrlConfigured` 与 production-config 的 URL 判定共同调用——先验证 TS 侧可 import 且 dependency-cruiser 分层规则放行;②若构建/分层边界不允许,退回"双实现 + 契约测试锁同一 fixture 清单",并在**两处代码注释**写明"为何存在两份 + 契约测试位置"。
- [x] **Step 3 文档**:上线验证.md ①删"三个 placeholder blocker"过期段(改述:已由 content-readiness `FORM_PLACEHOLDER_ALLOWED_RULE_IDS` 具名放行,非 blocker);②切域名必跑链**如实写全**(不是只写 release:verify):`PUBLIC_LAUNCH_STRICT=true APP_ENV=production node scripts/starter-checks.js validate-production-config` + production build + OpenNext build + production wrangler dry-run + deployed smoke + 真实 lead canary + Resend 收件人工确认;并注明 `release:verify ≠ public launch proof`(项目已有此声明,引用之)。
- [x] **Step 4**:`pnpm test` + `node scripts/quality/checks/current-truth-docs.js`;commit `fix: consolidate public url readiness and correct launch runbook`,push,CI 绿,停。

### Task R1.7: Resend 空 ID 不得算成功

**Files:** `src/lib/email/resend-http-client.ts:112-124`;`src/lib/resend-core.tsx:135,181,231`;`src/lib/__tests__/resend.test.ts:718-730`。

- [x] **Step 1**:改测试——2xx 无 `id` → 断言失败结果(替换"保留 unknown"断言)。预期 FAIL。
- [x] **Step 2**:http-client `response.ok` 分支后:

```ts
const data = getSuccessData(responsePayload);
if (!data || typeof data.id !== "string" || data.id.length === 0) {
  return {
    data: null,
    error: { message: "Resend success response is missing a message id" },
  };
}
return { data, error: null };
```

- [x] **Step 3**:resend-core 三处 `result.data?.id || "unknown"` → `result.data.id`(错误分支已保证非空;可空类型收紧而非再兜底);grep `"unknown"` 哨兵清零。
- [x] **Step 4**:`pnpm vitest run src/lib/__tests__/resend.test.ts src/lib/lead-pipeline/__tests__/process-lead.test.ts` PASS;commit `fix: require resend message id before marking owner notified`,push,CI 绿,停。

### Task R1.8: Turnstile 令牌提交后重置

**Files:** `src/lib/forms/use-lead-form-submission.ts:113-127`;`src/components/security/turnstile.tsx`;`src/components/forms/lazy-turnstile.tsx:24-51`;`src/components/forms/use-contact-form.ts`;`src/app/[locale]/request-quote/request-quote-form.tsx`;Test `src/lib/forms/__tests__/use-lead-form-submission.test.tsx`。

**Interfaces:** Produces: 内核 `registerTurnstileReset(fn: () => void): () => void`——**返回注销函数**,组件 effect cleanup 时调用,防重挂载后内核持有指向旧 widget ref 的僵尸回调;不变量"每次 submit 结算(成败皆然)后 token 清空且 widget reset"。

- [x] **Step 1 失败测试**:mock reset 回调,成功/失败两路径均断言 token→null 且回调调用一次。
- [x] **Step 2**:内核加 `turnstileResetRef` + `registerTurnstileReset`;submit `finally` 中 `setTurnstileToken(null); turnstileResetRef.current?.();`。
- [x] **Step 3**:turnstile.tsx 用 `@marsidev/react-turnstile` ref(动手前读其 types 确认 `reset()` 签名),新增 prop `onReadyRef?: (reset: () => void) => void` 回传 `() => ref.current?.reset()`;LazyTurnstile 透传;两表单接线到 `registerTurnstileReset`。内核 `:15` 注释改为如实描述。
- [x] **Step 4**:`pnpm test` + dev 手测(mock 失败后 widget 重新出票);commit `fix: reset turnstile token and widget after every submit settlement`,push,CI 绿,停。

### Task R1.9: RFQ 限长单真相(从真实文案推导,不引入新魔数)

**Files:** `src/app/[locale]/request-quote/request-quote-form-fields.tsx:10` 及该页 copy 构造处;`src/lib/lead-pipeline/__tests__/lead-field-limit-consistency.test.ts:24-40`;messages 中 `errors.requirements.tooLong`。

- [x] **Step 1 护栏测试**:断言"RFQ 页构造的 requirements(真实 source 行 + `\n` + 满长 message)长度 ≤ `MAX_LEAD_REQUIREMENTS_LENGTH`"——用真实 copy 构造,不用假设常数。预期 FAIL。
- [x] **Step 2 实现**:message 的 `maxLength` 由真实 payload 组成推导。**事实纠正(Codex 二审)**:`request-quote-payload.ts:48-70` 中 `requirements = copy.source + "\n" + message`,而 interest 是**独立的 buyerInterest 字段,不进 requirements**。公式:`messageMaxLength = MAX_LEAD_REQUIREMENTS_LENGTH - copy.payload.source.length - 1`(减号后为换行符;无 message 时 requirements 仅 source 行,无多余换行)。interest 由 buyerInterest 自身上限(200)单独约束,客户端截断在 D6a,不混入本计算。
- [x] **Step 3**:tooLong 文案数字改真(定位实际键,值含 "1000");`pnpm messages:sync`。
- [x] **Step 4**:`pnpm test`;commit `fix: derive rfq message limit from server-side requirements truth`,push,CI 绿,停。

### Task R1.10: 邮件多行压平修复 + 真链路测试

**Files:** `src/lib/resend-utils.ts:49,140-142`;`src/lib/__tests__/resend.test.ts:438-468,619-650`;`src/lib/lead-pipeline/__tests__/multiline-lead-fields.test.ts`。

- [x] **Step 1**:多行链路测试改为穿过真实 `ResendService`(仅 mock HTTP 传输层),含 `\n` 的 message/requirements 断言 provider payload 换行语义保留(html 按 `runtime-email-content.ts:59-70` 实际多行渲染形态断言)。预期 FAIL。
- [x] **Step 2**:resend-utils message/requirements 改 `sanitizeMultilineText`(消毒权归 schema 边界,此处仅双保险);单行字段不动。
- [x] **Step 3**:更新 :438-468/:619-650 旧断言为"换行保留";相关三测试文件 PASS;commit `fix: preserve buyer multiline text through the email channel`,push,CI 绿,停。

### Task R1.11: 无依据声明与业务口径修正(R'4/R'6,发布正确性——虚假声明不能上线)

**Files:** `content/pages/en/about.mdx:38`;`content/pages/en/oem-wholesale.mdx:81`;机构名对应消息物理包键(compat critical.json:418,440 反查);`src/constants/tucsenberg-product-page-abs-flood-barriers.ts:32,91`;`src/constants/tucsenberg-product-page-flood-tube-dams.ts:31,93` 及相关消息键;`public/.well-known/security.txt:4`。

- [x] **Step 1(R'4)**:删全部机构名举例("SGS/BV"、"SGS, TÜV, QIMA"),改不点名表述如 `"third-party inspection by any agency of your choice is welcome"`(按各处上下文语气微调,不新增任何机构名)。
- [x] **Step 2(R'6)**:删 "ASTM G154-2016" 引用(改 "UV-stabilised ABS" 类不引标准的材质陈述);删 "EN 13501 B1"(改 "flame-retardant fabric" 类)。验收:`grep -rn "ASTM\|EN 13501\|SGS\|TÜV\|QIMA\|\bBV\b" content/ messages/ src/constants/` 清零(BV 注意误匹配,人工复核)。
- [x] **Step 3**:security.txt Contact → `mailto:sales@tucsenberg.com`。
- [x] **Step 4**:`pnpm messages:sync` + `pnpm content:check` + `pnpm test`;commit `fix: remove unsubstantiated standards claims and agency name-dropping`,push,CI 绿,停。
- **R'5 管坝 MOQ 挂起**:业主口径到位后单独小 commit(`tucsenberg-product-page-flood-tube-dams.ts:116` + 消息键),属 M1 收尾件。

### Task R0(业主明确延期,不进入当前执行队列;继续阻塞 M2)

> **2026-07-15 R'12**:下列五类事项先不做，勾选保持未完成，Cursor/agent 不得反复请求确认，也不得修改 strict gate 迁就缺口。它们只暂停 M2，不阻塞任何 B/C/D 仓库任务。

- [ ] **正式域名与切换时点**：不写假域名，不做 production cutover。
- [ ] **全部 PDF 工作**：包括 TB-BW 规格书、供应商清单重导出、其余公开 PDF 的结构标签/可访问性处理与相关人工验收。
- [ ] **公开联系电话与产品实拍照片**：本轮既不提供，也不做“改 strict gate 绕过”的裁决。C2 通用询盘里的买家选填 WhatsApp / 电话不是公开联系电话。
- [ ] **管坝 MOQ 真实口径**：不改相关产品事实、消息键或 SEO 文案，等待业主给出真实值。
- [ ] **法律/联系页人工审核签字**：不伪造 `PUBLIC_LAUNCH_LEGAL_CONTENT_REVIEWED` 或任何签字证据。
- [x] 已裁决:R'2 WhatsApp / 电话选填 + R'11 单一低摩擦通用询盘(按 M3.C2+D6a-e 完整实施,不能只做半条链)/ R'4 / R'6 / security@ 不建。

---

# 里程碑 2:公开上线(当前暂停,等待业主重新启动)

> **R'12 当前指令**:Cursor/agent 不执行 production cutover、不尝试绕过 strict gate，也不把 preview/本地 proof 写成公开上线。下列步骤保留为未来恢复清单；仓库内不依赖 R0 的准备和测试仍可在各自 B/C/D 任务中完成。

前置:M1 代码 lane 证据仍有效 + R0 闭环。M2 独占全部"真实上线证明"。恢复后执行(记录进 `docs/项目基础/上线验证.md`):

1. 写入正式域名:`wrangler.jsonc` production vars(替换 example.invalid)+ `NEXT_PUBLIC_SITE_URL`;production secrets(Upstash/pepper/Turnstile/Resend/Airtable)在 Cloudflare/GitHub 逐项核对。
2. `PUBLIC_LAUNCH_STRICT=true APP_ENV=production node scripts/starter-checks.js validate-production-config` 通过。
3. production workflow 部署(strict 门全过)→ deployed smoke(顺带实测三项部署态挂账:PDF noindex 响应头、CSP report-to 报文、/en/contact 307)→ **真实 lead canary**(R1.4 修复版)→ Resend 收件人工确认 → Airtable 记录核对。
4. 业主签字 + 证据(canary 记录 ID、smoke 输出)归档。
5. **索引事实(按当前实现如实陈述)**:没有独立索引开关——`APP_ENV=production` 同时翻转 X-Robots-Tag、metadata index、robots.txt(next.config.ts:161-167 / seo-metadata.ts / robots.ts)。**production 部署即开放索引**,业主必须在部署前完成内容就绪确认;"先部署后放索引"当前做不到,如确有此需求需先加独立开关(暂不加,少一个开关)。

---

# 里程碑 3:当前工程执行面(Hardening lane,33 个 PR:B 7 + C 9 + D 17)

M1 代码 PR 已合并，M3 立即按调度持续执行。M2 因 R'12 暂停不影响本里程碑；**B 组先行**，遇到 R0 依赖只标记精确延期点并继续其他任务。

## M3 调度

| 组 | 波 1(并行) | 波 2 | 波 3 |
|---|---|---|---|
| B(门禁) | B1 ∥ B2 ∥ B3 ∥ B4 ∥ B6 | B5(等 B4:ci.yml)∥ B7(等 B1:eslint-disable.js;等 B6:current-truth-docs.js) | — |
| C(裁决) | C1 ∥ C3 ∥ C4 | C2(等 C3:先移除 newsletter union)∥ C6(等 C1:共享 UI/设计文档)∥ C5a(等 B7:先有真实使用门)→C5b→C5c | C7(等 C1+C6 稳定文档;等 C5c:rules 文件;等 D6e:通用询盘最终真相) |
| D(清理) | D2 ∥ D3a ∥ D4a | D3b ∥ D4b ∥ D6a→D6b→D6c→D6d→D6e(等 C2,询盘链内部串行) | D1(等 C1+C6:共享 UI/动效真相)∥ D3c ∥ D4c ∥ D5b ∥ D5a(等 D6a:统一字段错误面)∥ D7a(等 B7+C5c+D6e) → D7b 收尾(page.tsx+messages 汇聚) |

(消息物理包串行链规则同 Global 3,直至 C5c 合并后失效。执行中发现未列冲突:后动者等先动者,回写本表。)

### B 组:门禁与测试基建(7 PR)

**Task B1: eslint-disable 行内绕过修复** — `scripts/quality/checks/eslint-disable.js:176-187` 修 `extractDirectiveText` 不丢非行首命中(`ESLINT_DIRECTIVE_PATTERN.exec(rawLine)` 已存在);失败测试:行尾 disable 必产 finding;`pnpm lint:check` 全仓过(新报=真逃逸,按 GSE 登记或修)。commit `fix: detect trailing-comment eslint-disable directives`。

**Task B2: console.error 收集断言** — `src/test/setup.console.ts:16-44` error 改收集,`afterEach` 非白名单即 throw;先全量跑出噪音清单:真问题修组件、框架噪音入白名单带理由注释(允许两 commit:先机制+白名单,后修真问题)。commit `test: fail on unexpected console.error instead of swallowing`。

**Task B3: env 真实合约(修订版——不测"缺生产 secrets 抛错",那不是 env 层的职责)** — 两层分治:①`src/lib/__tests__/env.real-contract.test.ts`:`vi.unmock` + importActual,测**类型/非法格式/布尔转换/Cloudflare runtime 读取**,并断言真实导出键集 = 全局 mock 键集(防 mock 漂移);②production 组合必填测试进 `tests/unit/scripts/validate-production-config` 系:用**完整 fixture**(而非 Vitest 假件路径)测缺 Upstash/pepper/Resend/Airtable 时 fail-closed。commit `test: real env parsing contract and production combo fixtures`。

**Task B4: CI 补门(修订版——如实命名)** — ①ci.yml quality job 加 step,**命名 "preview config smoke"**:`APP_ENV=preview node scripts/starter-checks.js validate-production-config`(preview 模式跳过 runtime 契约,`production-config.js:246-255`——它只是冒烟,不是 production gate,step 名与注释如实说);②production 契约的 PR 循环覆盖靠 B3-② 的 fixture 单测(不要求 PR CI 拿真实 secrets);③strict gate 保持在 production workflow。④新建 `.github/workflows/weekly-audit.yml`:周一 cron `pnpm audit --prod --audit-level high`,失败开 issue。commit `ci: add preview config smoke and weekly dependency audit`。

**Task B5: 孤儿 e2e 处置(修订版——先覆盖映射再裁决)** — 执行时复核为 12 个零门禁引用 spec(原审查漏计 1 个),**逐个建覆盖映射表进 PR 描述**:覆盖的用户行为 / 是否有等价覆盖(unit/integration/在役 e2e)/ 是否依赖退役路径 / 处置(nightly 保活 | 改写 | 废纸篓)。已知倾向:`product-family-contact-handoff` 与 `seo-validation` 修复后编入 nightly(无 nightly 则建 CI_DAILY workflow);其余按映射表裁决,**不许只按 CI 引用数删除**。同 PR:ci.yml "Lead API family proof" step 改为 `pnpm vitest run tests/integration/api/lead-pipeline-real.test.ts`(删与 pnpm test 重复的 4 文件);删 playwright.config:53-77 与 global-setup:24-48 的 `/en` 基路径死遗产(~50 行)。commit `test: coverage-mapped e2e disposition and ci proof lane dedupe`。

**Task B6: 负空间守卫清理 + 死资产** — 分类表进 PR:保留=买家可见残留守卫;删除=纯内部负空间(`tucsenberg-site-contract.test.ts:179-195` StarterProfileId 断言、impeccable 目录断言、proof-lane 退役文件名断言、truth-docs 死名单 `current-truth-docs.js:26-34` 中的内部项、`interactive-islands-usage.test.ts:4-8`)。quotation-template.pdf(3.27MB)入废纸篓 + `TARGET_DOWNLOADS`(contract test :59)删条目。`pnpm test` + truth-docs 绿。commit `test: drop negative-space guards, keep buyer-visible residue checks`。

**Task B7: 门禁升级余项(修订版——行为门优先)** —
- 翻译键使用面检查进 content:check(双向 diff,动态键前缀白名单带注释;本 PR 只上检查+现状白名单,删键在 C5a)。
- **setRequestLocale 改行为门**:不扫源码字符串;新建 `scripts/quality/checks/prerender-static.js`,在 CI cloudflare-build job 的 `pnpm build` 之后运行,解析 `.next` 产物路由清单(prerender manifest / .meta),断言全部 `[locale]` 路由为 static/PPR 壳,**显式豁免清单**初始含 contact(D2 合并后从豁免删除)。漏 setRequestLocale 导致转动态会被此门当场抓住——抓的是真实失效模式,不是函数调用字符串。
- truth-docs 三连:tracked docs ⊆ 文档清单;文档内反引号 `src/`、`tests/` 路径存在性;GSE 反向校验(注册 ID 必须有消费)。同 PR 修被立即揭出的账:10 个漏登记文件补行、`替换边界.md:22` 断链测试路径改真、GSE 表清空注明"当前无活跃例外"。
- brand.js 大小写归一(`content.toLowerCase()` + 小写 needle;测试:全大写/全小写变体均命中)。
commit `feat: behavior-level gates for prerender, key usage and doc liveness`。

### C 组:裁决落地(9 PR)

**Task C1: Radix Themes 原子退役(R'1,2026-07-15 再确认)** — 退役范围是 `@radix-ui/themes` 运行时、全局 CSS、Theme context 与 Themes 专属治理分支;**不退役** `@radix-ui/react-*` Primitives、Radix-style 1–12 色阶方法、项目 token、本地 `src/components/ui/*` 入口、Registry 和 Playbook 治理系统。不再机械“重写并保留 8 个名字”，先按执行时真实消费面分为保留语义能力与删除重复层。
- [ ] **前置真相与基线**:用 `rg` 重查 8 个包装器的 production/story/test 消费面;记录首页、contact 初始/聚焦/错误/成功/disabled、no-JS static fallback、light/dark、mobile/desktop 截图与键盘行为;保存 fresh build CSS 原始体积。若消费面已与本计划不同，先回写计划，不按旧清单盲删。
- [ ] **更简等价形态**:`input.tsx`/`textarea.tsx` 收口为原生控件+Tailwind/项目 token;`status-callout.tsx` 保留语义 wrapper 与 live-region 契约。`radix-theme.tsx` 直接删除，不改成空包装器。若当前消费面仍与 2026-07-15 复核一致:`contact-form-control.tsx` 合并到通用 Input/Textarea;`contact-form-shell.tsx` 与 `data-card.tsx` 的单一/重复能力迁移到已有 `Card`;`badge.tsx` 若仍无 production 消费者，先形成“零调用者+无独立行为+本地替代入口仍可发现”的退役证明，再同步删除 source/story/test/Registry 条目;证明不足就保留本地实现与真实条目，不为凑删减数字硬删。未来有真实需求时按本地 wrapper 流程恢复。仅对**保留的公共组件**维持 props 契约;被删的仓库内部层必须在同 PR 迁完全部调用者。
- [ ] **运行时退役**:删 `src/app/globals.css` 的 `@radix-ui/themes/styles.css` 导入与 Themes-only token bridge;`package.json`/lockfile 移除 `@radix-ui/themes`;执行 `rg` 确认生产源码零 Themes import、零 `.rt-*` 依赖。构建验收以“vendor Themes CSS 确实消失+记录 fresh before/after bytes”为准，不把某个旧 chunk 名或固定字节数写成永久断言。
- [ ] **治理原子反转**:Registry/Playbook **保留但必须改真，不是“本 PR 不动”**。删 Themes approved-wrapper allowlist 和 `radix-theme-pilot-*` 特殊路径;component governance 改为生产 UI 全面禁止 `@radix-ui/themes`，仍允许本地 UI wrapper 内使用 Radix Primitives;Registry `radixLayer` 收窄到 `primitive|local`，删 `themes|mixed`;`themeBoundary` 若复核确认只为 Themes 服务，同 PR 从 Registry/schema/index/truth helper 删除，不留全为 `none` 的死元数据。PR 必须附 retirement proof:被删项零活跃消费者、保留能力仍有明确本地入口、Registry/Playbook 对 AI 与人工的可发现性不低于当前。这是对 Registry/Playbook 的等强或更强治理，不是退役治理系统。
- [ ] **稳定文档同 PR 反转**:同步 `AGENTS.md`、`.claude/rules/ui.md`、`src/components/ui/README.md`、`docs/决策记录/UI基础方案.md`、`docs/决策记录/Radix联系表单试点.md`、`docs/design/{组件使用手册,组件治理,组件索引,设计真相,色彩系统}.md`。联系表单试点 ADR 保留为 historical experiment，明确记录“技术可行，但实测全局成本与双设计系统不值得长期续付”，不再保留 `stable takeover` 的当前决策语气。
- [ ] **验收**:把现有“必须存在 `data-ui-pilot=radix-themes-*`”的供应商实现断言改为用户行为/语义契约;`pnpm component:check` + focused tests + `pnpm test` + `pnpm build` + 视觉差异/键盘/focus/autofill/no-JS 对比全绿。commit `refactor: retire radix themes runtime and simplify local ui`。

**Task C2: 通用询盘数据契约 + WhatsApp 基础(R'2/R'11)** — 本 PR 先建立唯一的后端询盘真相，D6a-D6e 再按同一契约收口两个现有前端和退役旧链。**不能**新建配置驱动万能表单，也不能先把 Contact 独立体系补强后再拆。
**Hard prerequisite(合并前置,不是提醒)**:Airtable base 先建列 `WhatsApp / Phone` 并验证真实写入;该列**不在 R'12 延期清单内**。当前执行环境无 Airtable 权限时记录精确 blocker，先继续 C1/C4/C5/C6/B/D 非询盘任务，不能假装列已存在，也不能让此账号动作冻结全计划。(风险边界:即便列缺失,Airtable 失败按 BC-012A 只降级不丢单——email-first——但仍按列先行执行,不赌降级。)
- [ ] **固定买家字段契约**:唯一可见字段为 `fullName`(必填)、`email`(必填)、`phone`(选填,标签 `WhatsApp / phone (optional)`)、`message`(选填,标签 `What do you need? (optional)`)。不接受 company/subject/product selector/quantity/dimensions/country/port/budget/file upload/multi-step 作为通用表单字段;以后确有需求另做行为裁决，不给本轮留假开关。
- [ ] **上下文不转嫁给买家**:保留并规范 `catalogProductId`(仅服务端目录验证后作为产品身份)、`buyerInterest`(描述,不是身份)、估算器可见预填、source page 与 UTM/click-id 归因。页面自动带入这些上下文;不得要求买家重复选产品或填写独立规格表。
- [ ] **后端单真相基础**:`src/lib/lead-pipeline/lead-schema.ts` 与 `process-lead.ts` 收口为 canonical inquiry input/processing path;Contact 与 RFQ 的临时适配器在 D6e 退役前只能把旧 payload 映射进该契约，不能继续各自定义业务规则。统一 owner email 与 Airtable 数据形状，`phone`/`message` 均允许缺省;删除 company/subject/quantity 对通用 Web 询盘的必需语义。
- [ ] **全链失败测试**:姓名/邮箱缺失 fail;phone/message 为空可成功;填写 phone 时 API→canonical inquiry→owner email→Airtable 完整穿透;产品页上下文只能使用目录验证后的 product id;通用入口无产品上下文也可成功;UTM/click-id 不丢。
- [ ] **文案契约**:成功与页面说明统一为“12 小时内回复;资料充分时直接报价，否则只补问必要信息”。不得用两个必填字段+选填描述的简表单承诺无条件准确报价。commit `refactor: establish the canonical low-friction inquiry contract`,push,最新 SHA CI 全绿 → READY_FOR_ACCEPTANCE;**等业主 Airtable 列回执且验收 ACCEPTED 后**由业主合并。

**Task C3: subscribe 退役(R'3)** — 自内向外删:`src/app/api/subscribe/`(废纸篓)→ lead-schema newsletter 段(:253-256)→ process-lead newsletter 分支 → rate-limit preset → 消息键;行为合约.md 删 newsletter 行、BC-012A 收窄双路由;每步 type-check,knip/depcruise 兜底。commit `feat: retire orphan subscribe endpoint and newsletter pipeline`。

**Task C4: React Email 死套 + 假 API + 无用 secret(R'7 + Codex C3/P3-9)** — 废纸篓:`src/emails/{ConfirmationEmail,ContactFormEmail,ProductInquiryEmail,EmailLayout,EmailField}.tsx` + 测试(保留 email-copy.ts/theme.ts);package.json 删 `react-email`、`@react-email/ui`;删 `useTurnstile`(turnstile.tsx:211-266)、`getEmailStats/getEmailConfig/checkConnection`(resend-core.tsx:242-280)及 only-test 用例;删 `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` 三件套(env.ts:32,175 / production-config.js:301 / cloudflare-deploy.yml:141,176,198);`server-action-utils.ts` 文件头改真。PR 描述提醒业主删 GitHub secret。逐簇 type-check+knip;test+build。commit `chore: remove dead react-email templates, fake apis and unused server action key`。

**Task C5a: 死翻译键清理** — 必须等待 B7 的真实使用面检查。逐簇把键分为“当前运行消费 / 已批准且已排期能力 / Starter 残留或零消费者”三类，只删除第三类(~240 键候选,簇清单见审查报告车道6 F1);不能用“当前只有英文”作为删除 locale routing、跨语种 parity 或未来加语种基础契约的理由。动态键、邮件、SEO、Schema、MDX、Storybook 和测试工具均纳入消费面;`language` 当前无语言切换器消费者，可移出 `client-messages.ts:17-24`，相关旧文案仅在证零消费后删除，未来真实语言切换器随功能重新增加。每簇 content:check+test;白名单同步收缩。commit `chore: remove dead translation keys uncovered by usage gate`。

**Task C5b: critical/deferred 合并** — 每个 ownership pack/locale 合并为单个 `messages.json`，保留固定 `base -> b2b-lead -> catalog` 归属图;改 message-pack-loader、static-split-messages(两处“语种→包”映射合一)、load-messages、sync 脚本与 check 的 type 循环。**客户端体积仍由 namespace pick 控制**，不得因文件合并把完整消息图下发到所有客户端路由;所有 configured locales 继续保持 leaf-key parity。`.claude/rules/i18n.md:117-123` 与 `docs/项目基础/消息文案.md` 同步重写。commit `refactor: merge critical/deferred message split into single pack files`。

**Task C5c: compat 层退役** — `next-intl.d.ts` 直接从默认 locale 的三个物理 pack 合成 key 类型;storybook-messages、测试和工具全部改读物理 pack/唯一 composition helper，删重复实现。废纸篓 `messages/en/`、sync-message-compat.ts、`messages:sync` script 和**仅验证生成副本新鲜度**的 compat 段;保留并适配 physical-pack shape、configured-locale parity、动态键白名单和缺键 fail-fast 门禁。`rg` 清全部 `messages/en\b` 消费;`.claude/rules/{content,i18n}.md`、`docs/项目基础/{内容,消息文案,生命周期}.md` 改为新语种只需 locale config + 三个物理 pack + 对应 MDX/SEO/runtime proof，不再生成副本。全量 `pnpm website:check`+build。commit `refactor: retire generated message compat layer`。

**Task C6: 设计真相统一(R'9)+ 设计文档重写** — `section-head.tsx:29` 对齐 `.text-section` 阶(24px/md:28px),目检 contact/FAQ;按审查报告 A1–A16 重写 `页面模式.md`(footer=neutral 白底、卡片=surface-card 边框制、按钮 40/48、H2=24 标准)、`网格系统.md`(收缩为 HeroGuideOverlay 现状)、`设计系统说明.md`(4 幽灵路径改真)、`组件治理.md` 示例、`Storybook覆盖范围.md:28`、`色彩系统.md:9`、`动效原则.md:69`(对齐 300ms)、`可迁移设计资产*.md:3`、`文档清单.md`(补 4 行 + 去 current-reference 标签)。truth-docs 绿。commit `docs: rewrite design pattern docs to post-redesign reality`。

**Task C7: 文档/注释真相修正批(修订版——AGENTS.md 方向反转)** — 代码注释:inquiry route:1-4(drawer 幻影)、turnstile.ts:5、submit-canonical:357-363、security.ts:41,64,229 + footer-links.ts:17("this starter");项目基础:替换边界.md 图标路径/3 个不存在文件、monitoring.md:6-7、架构树.md:28;技术难题:整库整改执行文档补完成态、门禁机械遵守三份加 Historical banner 入机器清单、性能记录断链、路由模式基线 vs 技术栈归因口径统一;规则:security.md:162-163、conventions.md:91-92、i18n.md:34(structured-data.md 已随 D3b 同 PR 修正,本任务不碰,依赖解除)。**AGENTS.md(修订)**:保持**自包含跨工具入口**(AGENTS.md:159 明文),不建立 AGENTS→CLAUDE 依赖;消除重复的方式是"CLAUDE.md 引用 AGENTS.md 共享段"或双向内容一致性核对(修 drift 即可),方向不许反。truth-docs 绿。commit `docs: fix remaining doc-vs-reality drift from 2026-07 audit`。

### D 组:P3 清理与性能(17 PR)

**Task D1: Motion 受控保留与长期边界(R'10,2026-07-15 业主裁决)** — 取消“当前利用率低→立即退役”的预设结论，不新建 `useInViewOnce`/IntersectionObserver 只为复刻现有能力，不删 `motion` 依赖。长期定位:`motion` 是 CSS/状态属性之上的**高级动效引擎**，服务多元素编排、enter/exit、动态布局、媒体切换、SVG/产品配置状态等 CSS 明显更差的真实需求;不用于普通 hover/focus/menu/dialog/status 或全站装饰 reveal。
- [ ] **当前边界证明**:用 fresh build 记录首页 Motion chunk 与代表性非首页路由;验证 Motion 运行时仍只由真实使用路由承担，不回流 `src/app/[locale]/layout.tsx`。记录当次 chunk/bytes 而不把某个 hash 或瞬时字节数写成永久门禁。
- [ ] **保留策略**:现有首页 `BreathingReveal` 作为开发阶段路由局部试点暂留，冻结为“不默认扩张”，不得因已安装 Motion 就免证据用到新路由。`motion/react` import 收口在 `src/components/motion/*` 或有独立 proof 的 feature-local advanced component;首屏主张内容始终服务端渲染且默认可见;`prefers-reduced-motion` 和 no-JS 信息完整性是硬契约。
- [ ] **小清理不扩建**:以执行时 `rg` 为准，删仅测试引用的死 motion token/变体(当前候选:`lightBreathingStaggerChildren`)，收窄 `getInstantTransition` 这类已被 reduced-motion 早返回使得参数恒定的假灵活性;不新建 shared micro-motion 框架。
- [ ] **状态型后续裁决**:等产品原理图、计算器、图库/灯箱等真实产品表达面稳定后再复审，不按日期或提交数硬判:若届时 Motion 仍只服务通用首页 reveal，再开完整退役证明;若出现 CSS 明显更差的真实高级使用面，则正式确认路由局部长期保留并继续按增量测量。
- [ ] **验收**:首页视觉/reduced-motion/no-JS 行为不回归;代表性非首页路由不承担 Motion 运行时;`pnpm type-check` + focused tests + `pnpm build` 全绿;更新 `docs/design/动效治理.md`与相关当前真相，保持“CSS first / Motion advanced-only”。commit `chore: retain motion as a route-scoped advanced capability`。

**Task D2: contact 转全静态** — 读安装版 use-search-params.md Prerendering 段;`contact-page-sections.tsx:245` 删 `await searchParams`,family 提示移入 client island 的 `useSearchParams()`;验收:contact.meta 不再 postponed、静态壳含 contact-form-column 与 faq-section(no-js spec 补断言);B7 的 prerender-static 豁免清单删 contact;handoff 行为用 B5 保活 e2e 复验。commit `perf: make contact page fully static`。

**Task D3a: canonical/metadata/OG 修正** — `catalog-breadcrumb-jsonld.ts:16` canonicalBase=baseUrl(删 /en 前缀),与 seo-metadata canonical 共用 helper;og:image 公共层兜底(`createStaticPageSeoDefaults` + `static-mdx-page.tsx:38-40` includeImage);contact.mdx:14 描述改为与 R'11 一致的响应口径，例如 `'Contact Tucsenberg — factory-direct flood barriers from China. Reply within 12 hours; pricing follows when project details are sufficient.'`，不得恢复无条件 12 小时报价承诺。commit `fix: canonical breadcrumb base and og image coverage`。

**Task D3b: JSON-LD/schema 修正(代码与规则文档同 PR)** — PrivacyPolicy→WebPage(privacy/page.tsx:70);terms 删无效 additionalType(:69);Article author→Organization(structured-data-generators.ts:136-143);Organization 补 email/address/foundingDate("2021",值取 single-site.ts:167-175);`@id` 图谱(`#organization`/`#website`,节点引用);request-quote 补 JsonLdGraphScript;ProductGroup→单 Product(market-jsonld.ts:57-70,description 用 lead 段 :28-45);oem FAQ 双真相收敛(正文 ## FAQ 删,frontmatter faq 渲染区块,照 [market]/page.tsx:285-307 模式);删无效 defaultValue 传参改 `??`。**同 PR 改 `.claude/rules/structured-data.md:27`** 注入点描述与新图谱一致(代码与规则文档同步落地,C7 不再依赖本任务)。schema.org validator 手动过(截图进 PR)。commit `fix: json-ld schema types, entity graph and faq single truth`。

**Task D3c: 死 SEO 机制 + 内链** — 删 meta keywords 全链(~100 行);删 url-generator.ts + generateLocalizedMetadata alternates(−54 行,先 grep 零独立调用方);删 SEO_INTERPOLATION_MAP/LAYER1_FACTS 零值键;删 PRODUCT_STANDARDS 占位表 + site-types.ts:142 类型收紧 TB 字面量;规格指南内链(products 页、ABS 页、页脚各一条)。**R'5 管坝 MOQ 已明确延期，本 PR 不触碰 MOQ 事实或相关文案。**commit `chore: remove dead seo mechanisms and add guide internal links`。

**Task D4a: 安全行为批** — 蜜罐命中改同形 200 假成功(referenceId 打服务端可辨前缀,删 shouldBeEmpty 字段泄露);csp-report 双日志合一(isSuspicious 才 error);SECURITY_MODES 收缩 `{ cspReportOnly }` + isSecurityHeadersEnabled 合并 + _testMode 删 + 死字段断言删;删纯转发 verifyTurnstile 包装。`RATE_LIMIT_PEPPER_PREVIOUS`/`KV_*` 删除前必须同时证明生产代码、Cloudflare/GitHub secrets、部署文档和密钥轮换流程均无活跃消费；若仍承担平滑轮换，先把轮换能力迁到唯一真相再删旧字段，不能把安全能力误当死代码。commit `fix: security behavior cleanup batch`。

**Task D4b: 限流与平台护栏** — IPv6 /64 归一(rate-limit-key-strategies.ts:105-108,用 ip-range.ts 的 ipv6ToBigInt 高 64 位;测试同 /64 同桶、异 /64 异桶);production-config 加 `DEPLOYMENT_PLATFORM === "cloudflare"` 生产断言。commit `fix: normalize ipv6 rate-limit keys and assert deployment platform`。

**Task D4c: 框架升级(独立 PR)** — Next 16.2.7→16.2.10 + `@opennextjs/cloudflare`→1.20.1;`pnpm build` → `pnpm website:build:cf` 串行双绿 + wrangler dry-run 体积对比进 PR;middleware→proxy 仍不迁(业主裁决搁置,升大版本前必做,PR 描述重申)。commit `chore: bump next to 16.2.10 and opennext to 1.20.1`。

**Task D5a: a11y 正确性批(行为已定,无执行者裁量;三个买家可见速修已拉入 R1.3 Step 2b)** — dark `--muted-foreground`→neutral-9 复算 ≥4.5 + light muted 层级恢复(neutral-9,~7:1,与 Q3 主蓝收敛一起);`static-theme-colors.ts` 收录 `--primary-text`、删 4 死键(头部色已在 R1.3 改);**aria-describedby:在 D6a 的通用询盘四字段上实现字段级错误渲染**(服务端 details 按字段 key 拆到输入下方 + `aria-invalid`,ErrorDisplay 保留总览——此为定案,不许降级为"删属性");只有姓名/邮箱显示必填标记，WhatsApp / 电话和描述明确标选填;theme-switcher `aria-pressed`+`role="group"`;ghost hover 文字保持 foreground;删 `aria-haspopup="dialog"`;删 3 处冗余 aria-label;NAVIGATION_ARIA/Footer/breadcrumb aria-label 入 accessibility 命名空间。对比度复算数字进 PR。commit `fix: a11y correctness batch with field-level form errors`。

**Task D5b: 死样式/死管线清理** — 死 token ~90 行(审查 Q1 清单;`--shadow-card-active` 若 R1.3 未用则删,`--nav-h` 删);MDX 死管线三件套废纸篓(mdx-components/mdx-loader/importers,先证零生产引用,vitest alias 调整);Figtree woff2 死资产;`optimizePackageImports` 的 `@radix-ui/react-icons` 残留删。commit `chore: remove dead style tokens and dead mdx pipeline`。

**Task D6a: 固定通用询盘表单 + 双页面复用(R'11)** — 新建一个固定字段的 `InquiryForm`，不是 schema/config 驱动的 universal form engine。`/contact` 与 `/request-quote` 保留各自页面内容、SEO 和联系/报价说明，但嵌入同一表单并提交同一 `/api/inquiry`;产品页 CTA 与估算器继续把目录验证产品、`interest`、`config` 上下文自动带入，其中估算器摘要必须对买家可见且可编辑。表单只渲染姓名/邮箱/WhatsApp-or-phone/描述四框，前两项 required、后两项 optional;删除 Contact 的 company/subject 可见字段，不新增产品选择器。统一 decode 为字段 details/安全/服务器三分类;`?interest=` 客户端限长;no-JS 输出静态表单说明、公开邮箱与安全验证不可用原因，不渲染一个看似可提交却永远失败的假表单。测试覆盖两个页面字段完全一致、移动端/autofill/键盘、空 optional 成功、产品上下文可见、无上下文通用询盘成功。commit `feat: use one low-friction inquiry form across contact and request quote`。

**Task D6b: 单一 `/api/inquiry` + 验证单真相** — `/api/inquiry` 成为唯一网站询盘写入口;废纸篓 `/api/contact` route，删除 Contact 独立限流 preset/错误响应分支。边界只验证一次并调用 `processValidatedInquiry(input)`，不再让 `processLead()` 二次解析造成 VALIDATION_ERROR→500;Zod issue→消息键使用结构化 issue params，不读英文错误文案。姓名/邮箱必填;phone/message 缺省合法;蜜罐、Turnstile、服务端限流、同形安全响应和 attribution 保留。旧 `/api/contact` 无公开第三方合同，不留永久双写适配器;若部署日志证明仍有真实外部调用，先做有截止日期的旧 payload adapter 或明确 410，不能用不兼容 payload 的裸重定向，更不能永久保留第二套业务实现。commit `refactor: make api inquiry the single validated lead write path`。

**Task D6c: 产品上下文单真相 + CTA 接线** — 不让买家选择内部 `productInquiryKind`。已知产品页由统一 CTA helper 传 `catalogProductId`，服务器必须对目录校验后才赋产品身份;无有效产品 ID 即 general inquiry;`buyerInterest` 与估算器摘要只进入描述，不得伪造产品身份。收口 `resolveProductIdentity` 与产品目录 facade，删除 `SPECIALTY_MARKET_SLUG` 等死分支;统一所有产品页当前不一致的 plain `/request-quote`、`?interest=` 和 estimator handoff，保留可回放的 source page/UTM/click-id。类型用 catalog-context/general-context 判别联合表达服务器已验证状态，不把三态控制字段暴露成买家表单项。commit `refactor: derive inquiry product context from validated page handoff`。

**Task D6d: 统一成功态、Turnstile 与响应承诺** — 两页面使用同一 success/error UI，成功显示 referenceId 并重置四个表单字段和 Turnstile;删除客户端 5 分钟冷却(服务端真限流保留);Turnstile action 收口到一个 leaf constant，删 `NEXT_PUBLIC_TURNSTILE_ACTION` env 兜底、`enableTurnstile` 死开关和重复 action。分析事件仍可按页面来源记录 contact/request-quote/product/estimator，但不得分叉提交业务。所有“标准品 12 小时报价”表述按 R'11 改为“12 小时内回复;资料充分时直接报价，否则只补问必要信息”，成功态不得暗示空描述也能直接得到准确报价。commit `fix: unify inquiry success, turnstile and response expectations`。

**Task D6e: 退役重复 Contact 表单栈与配置引擎(R'8/R'11)** — D6a-D6d 行为稳定后，废纸篓 Contact 独立表单组件/hooks/static fallback/config-schema-builder/submit-canonical-contact/only-test 文件和旧邮件/Airtable转换分支;`CONTACT_FORM_CONFIG`、checkbox 假分支、恒空 whitelist、schemaVersion、CONTACT_SUBJECTS、company/subject/quantity 通用 Web 询盘字段及两套成功/错误类型全部清零。合并 `contact.form` 与 `requestQuote.form` 重复键为一个受治理的 inquiry namespace，页面标题/说明仍各自归页面 owner;更新行为合约、组件 Registry/Storybook、E2E、架构文档和 Contact/RFQ 文案。验收:`rg` 证明只有一个可见表单实现、一个 API 写入口、一个 schema owner、一个 email/Airtable inquiry path;`pnpm component:check` + `pnpm test` + contact/RFQ/product-handoff focused e2e + `pnpm build` 全绿。commit `refactor: retire the duplicate contact form stack and config engine`。

**Task D7a: 退役组件级静默英文副本，保留系统级 locale 恢复** — 等 B7+C5c+D6e 后执行。8 处 `t(key as …)` 改字面量联合(照 theme-switcher labelKey 范式);删 Footer translateWithFallback 与 getContactCopy 硬编码英文副本，必需键统一用 `readRequiredMessagePath` 缺键即抛，防未来 locale 页面悄悄混入英文。**不得误删**三类正确恢复:无效 locale→默认 locale 的 `coerceLocale`、缓存加载失败→同 locale 物理源重试、next-intl context 外的 global-error 固定英文例外。所有 configured locales 继续 key parity;若未来业务批准“部分翻译上线”，必须另建集中、可观测的 fallback 策略，不能把英文副本散回组件。SVG/Canvas 图内文案 props 化(~20 段,"TB-BW" 型号豁免)+ 2 处硬编码 aria-label(与 D5a 对齐勿重复);全量 e2e 证明当前英文无缺键。commit `refactor: remove component-level english fallbacks without weakening locale recovery`。

**Task D7b: 键语义与残留收尾** — 首页键改名(problems→productLines,item 键改产品语义,cloudflareFoundation 等同法;连改 single-site-page-expression.ts:41-74 + page.tsx:92-117 as-const 数组);hero proof 键改名(est/countries→quoteSla/warranty);copyright 入消息包删 zh 死分支;globals.css zh 规则删(:821,848,git 历史可找回);emailTemplates.runtimeDefaultLocale 死键删;LOCALES_CONFIG 三死字段删。content:check+test+build。commit `refactor: rename starter-era message keys to product semantics`。

---

## 显式处置账(全部发现的非"修复"处置,防"任务做完=上线已证明"的错觉)

| 处置 | 项 |
|---|---|
| 业主接受/裁决 | middleware→proxy 搁置(升 Next 大版本前必做);security@ 不建;机构名/标准声明删除;R'1 `@radix-ui/themes` 原子退役但保留 Radix Primitives/Registry/Playbook;R'10 `motion` 按路由局部高级能力受控保留，不执行原 D1 的无条件退役;R'11 Contact/RFQ 收口为姓名+邮箱必填、WhatsApp/电话+描述选填的一套通用询盘，两个页面保留但不保留两套表单/接口/处理链 |
| **R'12 业主明确延期(只阻塞 M2)** | 正式域名/切换;全部 PDF 处理;公开电话与产品照片的提供或改门裁决;管坝 MOQ;法律/联系页人工签字。保持未完成，不重复询问，不允许假值或降 gate |
| **非延期外部前置(必须完成)** | Airtable `WhatsApp / Phone` 列与真实写入验证；无账号权限时记录精确 blocker，但继续全部无关任务 |
| 挂起单独跟进 | Worker zod ×4 重复(上游打包,记 `docs/技术难题/` 监控:每加 zod 路由 +70 KB gz);tailwind-merge 换 clsx(收益 8 KB/风险回归,不做);加语种本体(邮件 locale 管道、路由开关——未来"加语种 PR",本轮只做 D7 前置件);部署态实测(PDF noindex 头、CSP report-to 报文、/en/contact 307)→ M2 的 deployed smoke 时顺带 |
| 不做(有据) | 构建 bailout 日志(官方模式,已记已知);esbuild -0 警告(上游) |
| **当前必须完成** | B/C/D 全部未勾选任务，包括 D5a 全部 a11y、C5 消息结构、D7 locale 边界、统一询盘、Radix Themes 退役、Motion 边界证明及其余代码/文档/测试清理；不得把 R'12 扩大成新的延期清单 |

## Self-Review 记录(v6 增补,2026-07-15)

当前执行范围回写:①R'12 将域名、全部 PDF、公开电话/产品照片裁决、管坝 MOQ、法律签字明确移出当前执行队列，只阻塞 M2;M3 不得因此停工，也不得用假值或改 gate 伪造 readiness。②除处置账已有证据的“不做/上游/未来功能”外，B/C/D 全部任务必须执行，D5a a11y 不再属于可延期项。③Airtable WhatsApp 列不在延期清单内，仍是 C2/D6 的真实前置;无权限只阻塞该链，不阻塞全局。④C5a 等 B7 后按消费类型删键，C5b/c 保留 namespace payload 控制、physical-pack parity 与未来加语种基础;D7a 只退役组件级英文副本，保留 invalid-locale 默认、同-locale source retry 和 global-error 例外。⑤M2 标记暂停，M3 改为当前执行面;历史 Self-Review 中“上线前最后考虑/P2 延期”口径以本 v6 为准。⑥**PR 验收链正式化**(Codex 验收指出 Global 1 口头版过短):最新 SHA CI 全绿 → READY_FOR_ACCEPTANCE → 停等验收 → ACCEPTED → 仅业主合并;Cursor 不得自合并;CI 绿≠验收通过;有依赖串行等前置 ACCEPTED+合并;独立任务可并行到 READY_FOR_ACCEPTANCE;M3 全部合并后再做整库集成验收。

## Self-Review 记录(v5 增补,2026-07-15)

业主通用询盘裁决回写:①R'11 固定可见字段为姓名/邮箱必填、WhatsApp / 电话/描述选填，明确不加 company、subject、产品下拉、数量尺寸、国家港口、预算、上传或多步骤;这不是“先做最小版以后再打开配置”，而是当前真实产品契约。②`/contact` 与 `/request-quote` 页面因公司联系信息和高意向报价内容各自保留，但表单实现、`/api/inquiry`、验证、Turnstile、邮件、Airtable、成功错误语义只保留一套;自动产品/估算器/归因上下文不增加买家字段。③C2 改为 canonical inquiry 后端契约与 WhatsApp 基础，C3 先退役 newsletter union;D6a-e 改成前端复用→单 API/单验证→产品上下文→成功/安全语义→旧 Contact 栈退役的串行链，防止并行制造两套新真相。④D5a 只给姓名/邮箱必填语义，C7/D7a-b 等待 D6e 的最终消息与文档真相。⑤简表单与“无条件 12 小时报价”矛盾已消除，统一为 12 小时内回复，资料充分才直接报价。PR 总数不变(C2 与 D6 五项均为原任务重写)，M3=33、全计划=44。

## Self-Review 记录(v4 增补,2026-07-15)

业主长期技术裁决回写:①C1 从“重写 8 包装器但不动 Registry/Playbook”改为代码+依赖+治理+稳定文档的**原子反转**;Registry/Playbook 系统保留，但 Themes-only 允许状态、已删组件条目与过期使用指引必须同 PR 改真，否则 `component:check` 与长期架构仍冲突。②明确不把 Radix Themes 退役扩大为 Radix 全退，Primitives、色阶方法与本地 UI 入口保留。③D1 取消“利用率低即退役”结论，改为 Motion 路由局部受控保留+状态型复审;不为了复刻一个 viewport reveal 先自建 `useInViewOnce`，也不允许 Motion 回流根布局或成为简单动效默认层。④M3 PR 总数不变(D1 仍作为边界证明+小清理 PR)，但调度改为 C1→C6/C7、C1+C6→D1，避免 `AGENTS.md`/`.claude/rules/ui.md`/设计文档并行冲突和短暂假真相。

## Self-Review 记录(v3 增补)

> **历史记录，执行范围已被 v6 覆盖：** 当时对 Codex 二审的处置为:①M1/M2 完成条件解耦(M1=代码与输入就绪:合并+本地链+双构建+dry-run+R0;M2 独占 deployed smoke/真实 canary/收件/签字)②R0 补齐 strict gate 全部业主输入(公开电话、产品照片、法律审核签字;secrets 核对归 M2)③索引事实如实陈述(production 部署即开放索引,无独立开关,选方案 A 不加开关)④R1.9 事实纠正(interest 是独立 buyerInterest 字段不进 requirements,公式改 `MAX - source.length - 1`)⑤PR 数改真(M3=33:B7+C9+D17,总 44)⑥structured-data.md 随 D3b 同 PR,C7 依赖解除。次要项:R1.6 首选共享纯模块、退路契约测试带注释;R1.4 删除文案值钉死与源码形状门;R1.8 register 返回注销函数;R1.7 错误文案改"success response is missing a message id"。当时曾把“4 个 PDF、D5a 余项”列为待裁决；**现在执行必须以 v6 为准：全部 PDF 属 R'12 延期，D5a 全部纳入当前 M3，不得延期。**三个买家可见速修仍已拉入 R1.3 Step 2b。

## Self-Review 记录(v2)

- **对 Codex 14 项修正的处置**:①PR 数改口径(11+29≈40,以清单为准,里程碑验收不数 PR)②三里程碑拆 lane ③A6→R1.6 全模式清单+真实占位值 fixture+双实现契约测试 ④strict proof 链写全 ⑤C2 补 process-lead.ts 邮件映射 ⑥Airtable 列改 hard prerequisite ⑦B3 两层分治 ⑧B4 如实改名 preview smoke+fixture 契约 ⑨生成文件冲突入调度(Global 3+M1 串行链)⑩合并前 rebase+重验硬规则(Global 2)⑪C7 AGENTS 方向反转 ⑫新门禁改行为门(R1.3 计算比值/B7 prerender 清单/R1.4 可测 builder;R1.1 源扫描保留但标注约定门+渲染门双层)⑬C1 "待点头"删除 ⑭Goal 语义改"处置齐全≠全部修复"。超大 PR 拆分:D3→3、D4→3、D5→2、D6→5、C5→3;A9→R1.9 改真实文案推导。B5 改覆盖映射制。顶部 skill 依赖软化。
- **覆盖对账**:审查报告一(1.1-1.7→R1.1-R1.6+R0)、二(2.1→R1.8,2.2→R1.9,2.3→D6a,2.4→R1.10,2.5→C2,2.6→D6b,2.7-2.13→B1-B7,2.14-2.16→B7/C5a/D7b,2.17→D1 受控保留证明,2.18-2.19→D2/处置账,2.20-2.24→D3a-b/D5a)、三 P3(→D3c/D4a-c/D5b/D6c-e/D7a-b/B5-B6/处置账)、四裁决(R'1→C1…R'9→C6,R'10→D1,R'11→C2+D6a-e+D5a/C7/D7 消费面)、五门禁 12 条(→R1.1/R1.3/R1.4/R1.5/B1-B7)、八 Codex(C1→R1.7,C2→R0,C3→C4,C4→B6,8.2→R1.6)。无无主项。
- **占位符扫描**:无 TBD;原"aria 修复可降级"的裁量已删(D5a 定案字段级渲染)。
- **类型一致性**:`registerTurnstileReset`(R1.8→D6a/D6d)、`processValidatedInquiry`(D6b 定义/唯一 API 引用)、catalog/general inquiry context(D6c 定义→C2/D6a 消费)、`buildCanarySelectors`(R1.4→R1.5)、`MAX_LEAD_REQUIREMENTS_LENGTH`(R1.9→C2/D6a 描述上限同源)一致。
