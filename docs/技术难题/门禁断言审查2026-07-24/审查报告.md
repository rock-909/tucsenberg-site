# 门禁断言审查 2026-07-24（批次 E）

审查对象：`tests/architecture/` 39 个文件、6414 行，其中 302 条写死字符串的断言
（`toContain("...")` 形式），分布在 82 个用例块里。

判据来自 `AGENTS.md` 的 Gate Discipline：

> 门禁是为意图服务的启发式，不是法律。当检查逼着文档或代码陈述假话时，改检查而不是改
> 陈述。不要把时点快照钉成必须断言。新守卫要保护活的真相，不要保护过去重构留下的负空间
> （例如断言已删除的名字保持不存在）。

## 结论

302 条里：

| 类别 | 条数 | 处置 |
| --- | ---: | --- |
| 1. 真架构边界，保留 | 约 160 | 不动 |
| 2. 负空间守卫，违反 Gate Discipline | 33 | 删除或改为正向断言 |
| 3. 文档内容断言（要求某文档原样写着某句话） | 约 85 | 需业主裁决，见下 |
| 4. 文案锁（把用户可见文案冻结在测试里） | 约 12 | 改为断言事实，不断言措辞 |
| 5. 逐字重复的门禁 | 8（×2 处） | 合并 |

第 2、4、5 类是明确的缺陷，建议修。第 3 类里有一部分是业主 2026-07 明确保护的
Registry / Playbook 治理系统，不能单方面动，单列裁决。

## 类 2：负空间守卫（33 条，已逐条验证）

共同特征：断言"某个已经删掉的名字/文案/步骤保持不存在"。这类守卫保护的是过去，不是
现在。它们永远为真，因此永远不会失败，也就永远不会发现任何问题；同时它们把一次性的
重构结果变成了永久的措辞约束。

| 位置 | 断言内容 | 验证结果 |
| --- | --- | --- |
| `ci-workflow-contract.test.ts:159-163` | CI 和 lefthook 里不许出现 `website:lighthouse` / `lhci` / `performance-test` | 已确认三处配置里本来就没有 |
| `mdx-manifest-runtime-contract.test.ts:74-75` | 三个清单文件不许出现 `profile-fixture` / `showcase-full` | starter 概念已退役（`AGENTS.md` 有记载） |
| `proof-lane-labels.test.ts:36-37` | 上线验证文档不许出现 `` Default `company-site` first-pass `` / `Profile proof lanes:` | starter 遗留文案 |
| `proof-lane-labels.test.ts:100-101` | 维护入口不许出现 `project-workflow.md` / `website-production-workflow.md` | 文档已删 |
| `env-boundary.test.ts:190,218` | 源码不许出现 `getPublicRuntimeEnvNumber` | `src/` 中 0 处引用，助手已删 |
| `env-example-parity.test.ts:388` | 不许出现 `docs/website/env 设置.md` | 文档已删 |
| `i18n-locale-truth-parity.test.ts:43` | 配置文件不许出现中文注释 `只需在此处修改 locales 数组` | 锁的是一句**代码注释**；写成"在这里改 locales 数组"照样通过，零行为价值 |
| `tucsenberg-site-contract.test.ts:648,649,653,658` | 不许出现 `NEXT_PUBLIC_SUPPORTED_LOCALES=en,zh` / `Showcase Website Starter` / `/messages/zh/` / `preview.example.com` | 均为 starter 残留 |
| `contact-page-boundary.test.ts:103-104` | 联系页路由不许出现 `ContactFormStaticFallback` / `contact-form-static-fallback` | `src/` 中 0 处引用，组件已删 |
| `ui-component-playbook.test.ts:61,89-92` | 手册"缺失包装器"段不许列 `Checkbox` / `Select` / `RadioGroup` / `Dialog` | 四个包装器都已存在于 `src/components/ui/`；断言的是"过去的活干完了别退回去" |
| `ui-component-index.test.ts:133` | 索引不许出现 `Generated by` | 生成器已退役 |
| `locale-source-boundary.test.ts:48` | 不许出现 `typeof LOCALES_CONFIG.locales` | 旧写法 |
| `vitest-mdx-alias-contract.test.ts:17` | 不许出现旧正则 `find: /\.mdx$/` | 已换新正则，同一用例已正向断言新写法 |
| `zod-schema-boundary.test.ts:44,47` | 不许出现 `./setup.zod` / `globally mocks \`zod\`` | 已删的 setup 文件与已改的规则措辞 |
| `design-token-contract.test.ts:330-331` | CSS 不许出现 `next/font` / `Geist` | 字体已迁到 `next/font/local` |

**建议**：整类删除。其中有真实意图的两条，改为正向断言：

- Lighthouse 不接入 CI —— 改为解析 CI workflow 的步骤清单，断言没有性能步骤，而不是对
  整个文件做字符串匹配（现在在注释里提一句 `lhci` 就会误报）。
- 清单只含目录内容 —— 改为断言生成的清单里每一项都能解析到 `content/` 下的真实文件，
  而不是断言两个退役名字缺席。

### 三条容易误判的，已确认要保留

- `component-governance.test.ts:461-462`（`TooltipPortal` / `PopoverPortal` 不许导出）——
  守的是包装器的对外接口，且用的是解析出的导出清单而非字符串匹配。真边界。
- `contact-page-boundary.test.ts:106-107`（路由不许出现 `InquiryFormStaticFallback`）——
  该组件在 `src/` 中 6 处存在，同文件另一个用例正向断言它必须出现在 sections 里。这是
  "谁该引用它"的分层边界，不是负空间。
- `tucsenberg-site-contract.test.ts:630-631`（不许配置 `images.unsplash.com` /
  `via.placeholder.com` 远程图片域）—— 活的配置安全边界。

## 类 3：文档内容断言（约 85 条，需业主裁决）

要求某个 Markdown 文档原样包含某句话。和刚在 PR #160 里从 `AGENTS.md` / `CLAUDE.md`
移除的是同一类手段。

分布：

| 文件 | 条数 | 被断言的文档 |
| --- | ---: | --- |
| `ui-component-playbook.test.ts` | 33 | `docs/design/组件使用手册.md` |
| `email-copy-boundary.test.ts` | 10 | 三份替换指引文档 |
| `component-governance.test.ts` | 8 | 组件使用手册（与上面逐字重复） |
| `proof-lane-labels.test.ts` | 8 | 上线验证、性能记录、四份 Lighthouse 记录 |
| `ui-component-index.test.ts` | 5 | 组件索引 |
| `static-public-pages-contract.test.ts` | 5 | 静态页说明文档 |
| `message-packs-contract.test.ts` | 5 | messages README、替换顺序 |
| 其余 6 个文件 | 11 | 各自的说明文档 |

这类断言分两种，**处置不同**：

**3a. 治理契约类（业主已保护，不单方面动）**
`AGENTS.md` 写着"在获批的退役证明之前，不得删除、归档或收缩 Registry / Playbook"。
组件使用手册和组件索引的断言（41 条）属于这个系统。它们的问题是实现方式——用整句字符串
匹配，改个标点就挂——而不是意图。

建议：不删，改断言粒度。把"手册必须原样包含这 33 句话"换成"手册必须覆盖这些**主题**"
（章节标题存在、表格列齐全），措辞自由。

**3b. 纯复述类（建议删）**
其余约 44 条断言的是"文档里写了代码在做的事"。文档写错不会让网站出问题，代码写错才会；
而这些断言只检查文档，不检查代码。典型：`proof-lane-labels.test.ts:85-87` 要求四份
Lighthouse 记录各自包含 `Historical starter proof` 等三句英文原文。

## 类 4：文案锁（约 12 条）

`tucsenberg-site-contract.test.ts:796-846` 把联系页和询价页的用户可见文案冻结在测试里，
包括这样一整段：

> `**Email**: sales@tucsenberg.com — we reply within 12 hours. If the details are
> sufficient, the reply includes a quote. Otherwise, we ask only for the missing
> essentials. You'll hear from a person, not a sequence.`

改一个逗号，CI 就挂。

**意图是真的**：邮箱地址要对、承诺的回复时效要真实、不能残留 starter 占位文案。
**实现是错的**：它冻结的是措辞，不是事实。

建议改为断言事实：

- 邮箱地址等于配置里的唯一真相源（现在测试里是第二份硬编码副本，同一事实两处维护）；
- 地址等于配置里的唯一真相源（同上）；
- 承诺的回复时效在页面文案、成功提示、Turnstile 兜底三处一致（现在是各写各的硬编码）；
- 页面不含 starter 占位标记（这条保留，属于类 2 里少数值得保留的负向断言：它防的是
  新引入，不是守护旧删除）。

## 类 5：逐字重复的门禁（8 条 × 2 处）

`component-governance.test.ts:610-624` 与 `ui-component-playbook.test.ts:20-34` 对同一份
组件使用手册断言同样 8 个字符串。两处任何一处改了，另一处就成了假门禁。

建议：`ui-component-playbook.test.ts` 保留，`component-governance.test.ts` 里删除重复段，
只留文件存在性检查。

## 修复分批建议

| 批 | 内容 | 条数 | 风险 |
| --- | --- | ---: | --- |
| E1 | 删除类 2 负空间守卫，两条有真意图的改正向断言 | 33 | 低 |
| E2 | 合并类 5 重复门禁 | 8 | 低 |
| E3 | 类 4 文案锁改为事实断言（含把邮箱/地址收敛到唯一真相源） | 12 | 中，动到联系页与配置 |
| E4 | 类 3b 纯复述类文档断言删除 | 约 44 | 中，需逐条确认没有别的门禁依赖 |
| E5 | 类 3a 治理契约断言改粒度（保留意图，放开措辞） | 41 | 高，触及业主保护的 Registry / Playbook 系统，需先裁决 |

E1、E2 可直接做。E3 需要确认联系页文案的唯一真相源放在哪。E4 建议合并到 E1 之后单独一轮。
E5 必须先有业主裁决。

## E1 / E2 执行结果（2026-07-24）

`toContain("...")` 断言 302 → 260。39 个架构测试文件全绿，`pnpm test` 286 文件 / 2355 条
全绿，lint 与 type-check 通过。

### 四条改成正向断言

| 原断言 | 改后 |
| --- | --- |
| CI 与 lefthook 文本里不许出现 `lighthouse` / `lhci`（5 条字符串匹配） | 解析两份 YAML，收集所有真正会执行的 `run:` 命令，断言其中没有 lighthouse。注释里提一句不再误报 |
| 三个文件不许出现 `profile-fixture` / `showcase-full` | 遍历生成清单的每一条，断言 `source` 是 `active-content`、路径在 `/content/` 下、且文件真实存在 |
| 手册"缺失包装器"段不许列 Checkbox / Select / RadioGroup / Dialog | 解析该段实际列出的包装器名，断言它们在 `src/components/ui/` 下确实还不存在 |
| `.env.example` 含 `=en` 且不含 `=en,zh`（靠一正一反凑精确） | 一条精确行匹配 `/^NEXT_PUBLIC_SUPPORTED_LOCALES=en$/m` |

四条都做了变异检验：人为制造违规后测试确实失败，改回后恢复绿色。这一步是必要的——把恒真
断言换成另一条恒真断言，等于什么也没做。

### 两处审查时的归类被推翻

- `.env.example` 的 `=en,zh` 否定断言**不是**负空间。`=en` 是 `=en,zh` 的子串，正向断言
  单独存在时形同虚设，那条否定断言正是它精确性的来源。改为一条精确行匹配，两条合一。
- `docs/design/组件使用手册.md` 的"缺失包装器"段**当前根本没有列表**，四条否定断言是对空
  内容做匹配，比审查时判断的更空洞。

### 未做

E4、E5 未动。

## E3 执行结果（2026-07-24）

审查时把这一批记为"文案锁 12 条"。执行时发现问题比记录的严重一档：**被冻结的不是措辞，
而是三个没有一致性保障的业务事实。**

| 事实 | 真相源 | 实际副本数 | 原有保障 |
| --- | --- | ---: | --- |
| 联系邮箱 | `src/config/single-site.ts` | 6（含 1 处代码硬编码） | 无 |
| 注册地址 | `TUCSENBERG_REGISTERED_ADDRESS` | 4 | 无 |
| 回复时效 | 无真相源 | 59 处表述 | 无 |

原测试冻结的是其中**一份副本**（`content/pages/en/contact.mdx` 的一整段 231 字符营销
文案）。真正的失效模式——业主改了配置里的邮箱，另外 5 处保持旧值——这条测试完全测不出来，
因为它根本不看配置。改一个逗号却会让 CI 挂。

`src/components/security/turnstile-rescue-line.tsx` 里的邮箱硬编码是**有意的**，文件顶部
写明理由（避免把服务端 env 解析拉进客户端边界）。所以这里不改代码结构，改的是加一道
一致性断言。

### 改法

删除整段文案冻结，换成三条跨表面一致性断言：

1. `src`、`content`、`messages` 下出现的每一个 `@tucsenberg.com` 邮箱，必须等于
   `getPublicContactEmail()` 返回的配置值；
2. 任何提到注册地址的文件，必须完整包含 `TUCSENBERG_REGISTERED_ADDRESS`；
3. 全站"回复时效"表述只允许存在一个数值（当前 59 处全是 12 小时）。

联系页本身改为断言事实：包含配置邮箱、包含地址常量、有 RFQ 链接、无占位文案。占位文案检测
从原来的两个死字符串换成通用模式（`TODO`、`TBD`、`lorem ipsum`、`replace with your` 等）。

顺带删除 `CUSTOM_QUOTE_HOURS` 负空间断言（该常量在仓库中已不存在，属 E1 漏网）。

### 验证

四条变异检验全部按预期失败：

| 变异 | 失败的断言 |
| --- | --- |
| 改掉 turnstile 组件里的邮箱副本 | 邮箱一致性 |
| 改掉 `terms.mdx` 里的地址 | 地址一致性 |
| 某产品页回复时效改成 24 小时 | 回复时效一致性（报出 `12,24`） |
| 联系页塞入 `TODO: replace with your...` | 联系页事实断言 |

`pnpm test` 286 文件 / 2358 条全绿，lint 通过。

## 证据

- 断言抽取脚本与全量清单：本轮临时产物，未入库；抽取规则为
  `expect(<主体>).[not.]toContain("<字面量>")`，覆盖 295/302 条，剩余 7 条为跨行写法，
  已人工核对，不改变分类结论。
- 逐条验证命令记录见本目录 `证据.md`。

## E4 / E5 执行结果（2026-07-25）

E5 的前置裁决已经在 PR #163 完成：`AGENTS.md` 里"未经批准的退役证明不得删除 Registry /
Playbook"那条冻结条款已经删掉，Registry / Playbook 不再是不可动的保护区。

`toContain("...")` 断言在这 10 个文件里 122 → 33，净减 89 条。`pnpm test` 289 文件 /
2367 条全绿，lint 与 `pnpm component:check` 通过。

### E5：治理契约改粒度（组件使用手册 33 条 + 组件索引 5 条）

原来是 38 条整句字符串匹配——手册里改一个标点就挂，但手册删掉半篇内容照样绿。换成四条
能失败的断言：

| 新断言 | 失效模式 |
| --- | --- |
| 手册必须有这 10 个章节标题 | 删掉/改名任一章节 → 红。段落里的措辞随便改 |
| "New primitive checklist" 段必须点名 registry 的每一个字段（字段名从 registry JSON 读出来） | registry 加字段而清单没跟 → 红 |
| `docs/README.md` 与手册里引用的每个 `.md` 必须能解析到真实文件 | 文档改名留下死链 → 红 |
| 组件索引被手册和 `设计.md` 引用，且索引文件真实存在 | 引用断了 → 红 |

原来那条"索引每一行必须逐格等于 registry"的断言是真边界，原样保留。

删掉的：手册"自称 maintained mirror"、"@radix-ui/themes 已退役"等纯复述——后者的事实
已经由 `component-governance-check` 直接扫源码强制执行，测手册怎么写属于重复。

### E4：纯复述类文档断言（8 个文件）

删掉整块的是"文档里写了代码在做的事"，代码那一半各自都有真断言看着：

- `email-copy-boundary` 10 条（三份替换指引文档）——同文件的 message-pack 键断言才是真证明
- `message-packs-contract` 6 条（README + 两份指引）——下面的组合结果比对才是真证明
- `proof-lane-labels` 14 条（四份 Lighthouse 记录各含三句英文原文等）
- `static-public-pages-contract` 5 条 + 一条负空间（`blogArticle` 这个 PageType 早已不存在）
- `design-token-contract` 4 条（设计真相 / 手册各含某句话）——上面的 CSS 变量断言才是真证明
- `real-service-canary-boundary` 5 条、`i18n-locale-truth-parity` 4 条

一条顺手改成事实断言：`zod-schema-boundary` 原来断言 `.claude/rules/testing.md` 里写着
"Vitest uses real zod by default"，外加一条已删文件的负空间。改成扫 `src/test/` 下所有
共享 setup 文件，断言没有 `vi.mock("zod")`。原有的扫描只走 `*.test.ts`，setup 文件里
藏一个 mock 会让全部 schema 拒绝测试静悄悄变绿——这是真漏洞，不是复述。

### 变异检验（6 条，全部按预期变红）

| 变异 | 变红的断言 |
| --- | --- |
| 手册 `## Card rule` 改名 | 章节覆盖 |
| registry 加一个 `a11yNotes` 字段 | 清单字段覆盖 |
| `docs/README.md` 指向不存在的文档 | 引用解析 |
| `设计.md` 改成引用 `组件目录.md` | 索引引用 |
| `src/test/setup.base-mocks.ts` 里加 `vi.mock("zod")` | 共享 setup 真 zod |
| 手册 backlog 列上已发货的 `Badge` | 缺失包装器（原有断言，确认改写后仍有效）|

## 工具链逐行扫（2026-07-25）

E1–E5 只覆盖了断言本身。这一轮把**门禁工具自己**逐行读了一遍：`scripts/quality/checks/*`、
`scripts/*.js`、`.github/workflows/*`、`lefthook.yml`、`semgrep.yml`、`eslint.config.mjs`，
以及 26 个工具类测试文件。找的是同一类毛病，只是发生在更上游：**检查跑了、也绿了，但它
什么都没查**。

### 已修（每条都做过变异检验：先把被保护的东西弄坏，确认断言变红）

| # | 位置 | 原来的行为 | 变异证明 |
| --- | --- | --- | --- |
| 1 | `content-readiness` | 配置文件解析失败时返回 `[]`，整份文件跳过残留扫描 | 塞进解析器不认的语法 → 原来绿，现在红 |
| 2 | `eslint-disable` | 扫到 0 个文件也算通过 | 改扫描根 → 原来绿，现在红 |
| 3 | `content-slugs` | 默认模式在本仓库什么都不断言 | 已挂 `--strict-frontmatter` |
| 4 | `content-slugs` 严格模式 | 扫到 0 个文件仍返回"全部通过" | 改内容根目录名 → exit 0 变 exit 1 |
| 5 | `prerender-static` | 静默跳过三分之一检查（当前构建不产模板壳） | 输出改为显式说明跳过了什么 |
| 6 | `proof-lane-contract` | 冻结 6 条命令原文 | 改成断言"这条命令指向的脚本/文件真的存在" |
| 7 | 多处 | 16 条负空间断言守着本仓库历史上从没出现过的名字 | 删除 |
| 8 | `current-truth-docs` | 3 个日期化审查目录写死，同一事实要声明三遍 | 改成按目录名判定 |
| 9 | `component-governance` vs `client-boundary` | 两个门禁用两种方式判 `"use client"`，会互相打架 | 统一走 AST 指令序言 + 平价测试 |
| 10 | `react-doctor-reconcile` | 两次 `npx` 下载没有超时；CI 里这步是 `continue-on-error`，卡住不会快速失败 | 超时 10 分钟，改为报错 |
| 11 | `semgrep.yml` | `nextjs-unsafe-server-action` 规则从未命中过任何文件 | 删除（`unsafe-eval-usage` 已覆盖同一场景） |
| 12 | `semgrep.yml` / `eslint.config.mjs` | 3 条豁免指向不存在的目录，将来在那里建文件会自动免检 | 删除 |
| 13 | `config-exact-paths-exist` 测试 | 自身跳过所有 `目录/**` 形式的豁免，正是第 12 条能活下来的原因 | 修好后立刻抓出那 3 条 |
| 14 | `ci-workflow-contract` 测试 | 手抄 16 条 semgrep 规则 ID | 改成断言"没有规则低于 CI 实际扫描的严重级别" |
| 15 | `eslint.config.mjs` | jest 导入禁令被后面两个块整条关掉，`import from "@jest/globals"` lint 全绿 | 禁令挪到最后一个块 |
| 16 | `eslint.config.mjs` | `scripts/**` 关掉了 `no-dupe-keys` 和 `no-undef`——门禁脚本里写重复的配置键或打错变量名都不报 | 打开后全树干净 |

### semgrep 规则实测

用探针文件逐条打靶，15 条规则里 15 条能命中（含自闭合与带子元素两种
`dangerouslySetInnerHTML` 写法）；第 16 条打不中，已删。

### 读完确认没问题的

`production-config`、`client-boundary`、`content-manifest`、`cloudflare-smoke`、
`cloudflare-static-asset-headers`、`cloudflare-official-compare`、`release-verify`、
`message-key-usage`（323 个键、零豁免、双向陈旧检查）、`message-key-consumers`、
`translations`、`component-governance`、`public-url-readiness`、4 个 workflow、
12 个 lefthook 钩子，以及 26 个工具类测试文件（没有一个是空断言）。

### 留在原地的

- `REQUIRED_TRUTH_FILES`（43 条硬编码路径）和 `CURRENT_TRUTH_COMMAND_DOCS` 仍是手工
  维护的文件系统镜像。会漂，但漂了会红，不会静默放行——优先级低于以上 16 条。
- `package.json` 里 React Doctor 用 `react-doctor@latest`：不改代码也可能换行为。这是
  业主层面的工具选型问题，不是门禁缺陷。
