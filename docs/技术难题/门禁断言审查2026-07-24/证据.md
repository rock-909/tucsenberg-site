# 门禁断言审查 2026-07-24 — 验证记录

本轮所有分类结论的验证命令与结果。日期：2026-07-24，基线 `main@2b1c385`。

## 规模

```bash
$ ls tests/architecture/*.ts | wc -l
39
$ wc -l tests/architecture/*.ts | tail -1
6414 total
$ grep -rnE 'toContain\("' tests/architecture | wc -l
302
```

按主体归组后共 82 个用例块；抽取脚本覆盖 295 条，7 条跨行写法人工核对。

否定断言 113 条，正向 182 条。

## 类 2 逐条验证

断言"某名字不存在"的守卫，逐个检查该名字在源码里是否真的已经消失。若已消失，该断言守的
是过去而非现在。

```bash
$ for n in ContactFormStaticFallback InquiryFormStaticFallback \
           getPublicRuntimeEnvNumber TooltipPortal PopoverPortal; do
    echo "$n -> $(grep -rl "$n" src/ | wc -l) 个文件"
  done
ContactFormStaticFallback  -> 0 个文件
InquiryFormStaticFallback  -> 6 个文件
getPublicRuntimeEnvNumber  -> 0 个文件
TooltipPortal              -> 0 个文件
PopoverPortal              -> 0 个文件
```

结论：

- `ContactFormStaticFallback`、`getPublicRuntimeEnvNumber` 已删 → 对应断言是负空间守卫。
- `InquiryFormStaticFallback` 仍在用（6 处）→ `contact-page-boundary.test.ts:106-107`
  是分层边界（路由不引用、sections 引用），**保留**。
- `TooltipPortal` / `PopoverPortal` 在 `src/` 中不出现，但
  `component-governance.test.ts:456-463` 断言的是包装器解析出的**导出清单**，守的是对外
  接口而非文件文本，**保留**。

## playbook "缺失包装器"断言

```bash
$ for w in checkbox select radio-group dialog; do
    ls src/components/ui/$w.tsx >/dev/null 2>&1 && echo "$w.tsx 存在" || echo "$w.tsx 缺失"
  done
checkbox.tsx 存在
select.tsx 存在
radio-group.tsx 存在
dialog.tsx 存在
```

四个包装器都已落地，而 `ui-component-playbook.test.ts:89-92` 断言它们不出现在手册的
"Missing wrappers"段落里 → 守的是"已完成的工作别退回去"，是负空间。

## Lighthouse 断言

```bash
$ grep -rn "lighthouse\|lhci" .github/workflows/ lefthook.yml
(无输出)
```

`ci-workflow-contract.test.ts:159-163` 的五条否定断言当前恒为真，守的是一次过去的移除。
同用例第 165 行还要求 `lighthouserc.js` 原样包含一句中文说明，属类 3。

## 逐字重复门禁

`component-governance.test.ts:610-624` 与 `ui-component-playbook.test.ts:20-34` 对
`docs/design/组件使用手册.md` 断言同样 8 个字符串：

```
"short human/agent component selection playbook"
"Project-level"
".claude/rules/ui.md"
"full maintained wrapper inventory"
"组件索引.md"
"Registry and Playbook are retained"
"AI discoverability and machine governance will not get weaker"
"Do not delete, archive, or shrink"
```

两处逐字相同，人工比对确认。

## 文案锁

`tucsenberg-site-contract.test.ts:828-847` 用例 `uses the approved Tucsenberg contact
page copy` 对 `content/pages/en/contact.mdx` 做整句断言，最长一条为 231 字符的完整营销
段落（含邮箱、回复时效、承诺措辞）。同文件 796-826 另有回复时效的三处独立硬编码
（页面文案、成功提示、Turnstile 兜底），彼此之间没有一致性断言。

## 未做的事

- 未执行任何修复。
- 未运行完整测试套件（本轮只读分析）。
- 类 3 的 85 条按被断言文档归类，未逐条判断"删/改粒度"；该判断留到对应修复批次。
