# 根指令文件（CLAUDE.md + AGENTS.md）精简执行文档

> **2026-07-11 状态：本文档已由审计会话亲自执行完毕**，分支 `pr/root-instruction-slim`（commit b334ac6，基于 main 60c3831，在 `.claude/worktrees/root-slim` 隔离 worktree 完成）。本地验证全绿：website-config-runtime-boundary + component-governance 21/21 通过、brand:check 通过、truth-docs 仅剩已知基线 6 条（PR-B 范围）。**给执行者的唯一动作**：PR-I 里"owner 决策 3 的 CLAUDE.md 措辞修正"一项**跳过**（本 PR 已改），其余照旧；若推送时该分支已在远端，直接开 PR 走 CI 即可。
>
> 给执行者：本 PR 建议排在 PR 序列**末尾**（PR-I 之后）。CLAUDE.md 与 AGENTS.md 是近似双胞胎（仅 Rule Loading / Rule Routing 一节有意分叉），**所有编辑必须两个文件同步做**，分叉节除外。

## 背景与方向

2026-07-11 对根指令文件本身做了审查。方向：主流实践认为 CLAUDE.md 每一行都在每次会话中消耗注意力，内容应只装三类东西——不可发现的事实、改变 Agent 行为的判断层规则、指向细则的路由。当前 195 行中约 50 行是与 `.claude/rules/ui.md` 的逐句重复或一眼可见的目录清单，目标瘦到 ~135 行，并补一段本次两轮审计的根因原则。

## 硬约束：被测试钉死的字符串（改前必读）

以下断言同时检查 CLAUDE.md **和** AGENTS.md 两个物理文件的原文：

1. `tests/architecture/component-governance.test.ts:19-20,:661-664` — 必须原句保留：
   `Do not delete, archive, or shrink Registry / Playbook until a later approved retirement proof explicitly authorizes it and confirms equal-or-stronger AI discoverability and machine governance.`
   且不得出现 `until replacement proof exists`。
2. `tests/architecture/website-config-runtime-boundary.test.ts:73-100` — 必须保留：
   - `**tucsenberg-site** - derived English B2B website for Tucsenberg flood barrier products.`
   - `It is not a generic starter anymore.`
3. 同文件 :103-120 — Structure 节必须保留这 6 行原文（含缩进空格）：
   - `config/             # Runtime config and starter replacement surfaces`
   - `test/               # Source-level test helpers`
   - `- base/                       # Base physical message packs`
   - `- profiles/b2b-lead/          # Fixed lead-form message ownership layer`
   - `- profiles/catalog/           # Fixed catalog message ownership layer`
   - `- en/                       # Generated compatibility message JSON`（以测试实际断言的空格数为准，执行时对照测试源码）
4. `scripts/quality/checks/current-truth-docs.js` — 两文件属 `ROOT_INSTRUCTION_COMMAND_DOCS`，命令引用会被校验、退役措辞会被禁词扫描；`scripts/quality/checks/brand.js` 也扫这两个文件。
5. `<!-- BEGIN:nextjs-agent-rules -->` 注释块是外部同步标记，整块原样保留。

## 编辑清单（按节）

### 1. Project 节 — 修正"永久单语"假话（必须）

`The site is permanently the single English catalog site.` 与 owner 2026-07-11 裁决（多语言框架必须维持、后续加语种）冲突。该句**未被任何测试锁定**（已验证），改为：

```
The site currently ships English only; the i18n framework stays in place and
more locales will be added later.
```

注意：若 PR-I 已先行修过这句，保留 PR-I 的措辞，不要来回改。

### 2. Structure 节 — 删一眼可见的目录行

保留硬约束第 3 条的 6 行钉死内容 + 语义不可发现的行（`app/[locale]`、`app/api`、`config/single-site*.ts`、`i18n/`、`lib/`、messages 三层、content 两行）。删除自明目录行：`components/`、`constants/`、`emails/`、`hooks/`、`types/`，以及整个 `tests/` 块（目录名自明）。净减约 12 行。删完立刻跑硬约束第 2、3 条对应测试确认没误删钉死行。

### 3. UI Foundation 节 — 收敛为指针（16 行 → ~6 行）

该节几乎逐句复刻 `.claude/rules/ui.md:64-84`（已核对：pilot-only、禁直接 import `@radix-ui/themes`、禁 `.rt-*`、禁接管 hero/叙事结构，全部在 ui.md 里有）。替换为：

```
## UI Foundation

Hybrid / pilot-first UI foundation — decision record: `docs/决策记录/UI基础方案.md`.
Radix Primitives are the default for complex interactions; Tailwind owns layout,
responsive structure, and brand expression; runtime color truth stays in
`src/app/globals.css`. Radix Themes is pilot-only via approved
`src/components/ui/*` wrappers — hard boundaries in `.claude/rules/ui.md`.
```

### 4. AI-assisted Frontend System 节 — 保钉死句 + 收敛（~20 行 → ~9 行）

替换为（stop-line 一字不动）：

```
## AI-assisted Frontend System

Governance truth: `docs/design/组件使用手册.md`, `docs/design/设计真相.md`,
`.claude/rules/ui.md`, and the component governance tests.
`src/components/ui/*` is the formal project UI entry; tests and
`pnpm component:check` are the hard gate. Historical specs and plans under
`docs/superpowers/**` are background only unless a stable doc promotes the
same rule.

Do not delete, archive, or shrink Registry / Playbook until a later approved retirement proof explicitly authorizes it and confirms equal-or-stronger AI discoverability and machine governance.
```

被删的三条定位（Storybook MCP 仅内部知识源、shadcn 仅外部参考、stories 提供 owner 可见状态）：先检查 `.claude/rules/ui.md` 是否已有等价表述，没有的补进 ui.md（各一行），有则直接删。规则细节的家在 rules 层，不在根文件。

### 5. 新增：Gate Discipline 节（本次两轮审计的根因原则）

放在 "Correctness and Bug-Fix Discipline" 之后：

```
## Gate Discipline

Gates, tests, and checks are heuristics serving an intent, not laws. When a
check forces docs or code to state something false, fix the check, not the
statement. Never pin point-in-time snapshots (commit hashes, check output
counts, push status) as required assertions. New guards must protect live
truth, not the negative space of past refactors (e.g. asserting that deleted
names stay absent).
```

这是唯一的净增量（~7 行），直接约束 Agent 未来怎么写测试和检查——正是本仓库两轮审计反复出现的病根。

### 6. 不动清单

Correctness and Bug-Fix Discipline、Ponytail、Reference Sources（nextjs 本地 docs 指令块）、Commands + 最小验证清单、Rule Loading / Rule Routing、Communication、Constraints——全部保留原样。这些是高价值行为层，正是根文件该装的东西。

### 7. AGENTS.md 同步

上述 1-5 全部同样应用到 AGENTS.md；两文件各自的 Rule Loading（CLAUDE.md）/ Rule Routing（AGENTS.md）节保持现有分叉。

## 双文件关系（owner 已裁决，2026-07-11）

**裁决：双文件同步维持。** CLAUDE.md 与 AGENTS.md 保持两个近似文件，本次同步瘦身，仅 Rule Loading / Rule Routing 一节有意分叉，两个架构测试不动。

备考：Next.js 官方 AI agents 指南（`node_modules/next/dist/docs/01-app/02-guides/ai-agents.md`）推荐 CLAUDE.md 只写一行 `@AGENTS.md` 做单一真相，owner 已知情并否决（暂不采用，因需连带改 website-config-runtime-boundary 与 component-governance 两个测试的断言方式）。执行者不要按官方指南把 CLAUDE.md 缩成 import。

同指南要求的其余配置已就位、无需动作：两文件的 `<!-- BEGIN:nextjs-agent-rules -->` 块都在（:66-72），Next.js 16.2.7 ≥ 16.2.0 要求，`node_modules/next/dist/docs/` 文档包随装。

## 验证

```bash
pnpm exec vitest run tests/architecture/website-config-runtime-boundary.test.ts tests/architecture/component-governance.test.ts
node scripts/starter-checks.js truth-docs
pnpm brand:check
```

PR 描述用商业语言：给 AI 的项目说明书做了瘦身——删掉与细则文件重复的内容、修正"永久只做英文"的过时表述（多语言路线已确认）、新增一条"检查规则必须服务真实意图"的工作原则。
