# Surfaces

This is the file-level replacement index. Use `维护入口.md` for the current maintenance entry. `替换顺序.md` remains available only for historical traceability.

## Labels

- `must-replace`: replace or confirm before client launch.
- `review-or-tune`: starter structure may stay, but review for the selected project.
- `do-not-edit-first`: facade/generated/runtime mechanism; change upstream truth instead.

## Replacement groups

| Group | Priority | Files | Proof |
| --- | --- | --- | --- |
| brand identity | `must-replace` | `src/config/single-site.ts` | `pnpm brand:check` |
| SEO / crawl | `must-replace` | `src/config/single-site-seo.ts` | `pnpm content:check` |
| navigation / links | `must-replace` | `src/config/single-site-navigation.ts`, `src/config/single-site-links.ts` | `pnpm exec vitest run tests/architecture/static-public-pages-contract.test.ts` |
| page expression | `review-or-tune` | `src/config/single-site-page-expression.ts` | `pnpm exec vitest run src/config/__tests__/single-site-page-expression.test.ts` |
| product catalog | `must-replace` | Current Tucsenberg product-page runtime truth: `src/constants/tucsenberg-product-pages.ts`. Product list and route catalog: `src/config/single-site-product-catalog.ts` | product page contract tests for current Tucsenberg pages |
| content behavior | `review-or-tune` | `content/config/content.json` | `pnpm content:check` |
| page content | `review-or-tune` | `content/pages/{locale}/*.mdx` | `pnpm content:check` |
| contact form behavior | `must-replace` | UI and field ownership in `src/components/forms/**`; production browser write path in `src/app/api/inquiry/route.ts`; Lead-family Turnstile policy in `src/lib/security/lead-turnstile.ts`; lead processing in `src/lib/lead-pipeline/process-lead.ts`; production email body in `src/lib/email/runtime-email-content.ts` | `pnpm exec vitest run src/app/api/inquiry/__tests__/route.test.ts src/components/forms/__tests__/inquiry-form.test.tsx src/lib/security/__tests__/lead-turnstile.test.ts tests/architecture/lead-write-endpoint.test.ts tests/e2e/contact-submit-journey.spec.ts tests/e2e/product-interest-rfq-handoff.spec.ts` |
| UI messages | `review-or-tune` | `messages/base/**`, `messages/profiles/b2b-lead/**`, `messages/profiles/catalog/**` | `pnpm content:check` |
| email copy | `must-replace` | `messages/base/{locale}/messages.json` `emailTemplates`, `src/emails/email-copy.ts`, `src/lib/email/runtime-email-content.ts`, `src/lib/resend-utils.ts` | `pnpm exec vitest run tests/architecture/email-copy-boundary.test.ts tests/architecture/email-runtime-boundary.test.ts src/emails/__tests__/email-copy-source.test.ts src/emails/__tests__/theme.test.ts src/lib/__tests__/resend.test.ts` |
| assets | `review-or-tune` | `public/images/**`, `src/app/apple-icon.png`, `src/app/icon.png` | content/readiness plus visual review |
| public security disclosure | `must-replace` | `public/security-policy.txt` | public launch replacement review |
| deployment runtime | `must-replace` | `.env.example`, `.dev.vars.example`, `wrangler.jsonc`, `.github/workflows/**` | `PUBLIC_LAUNCH_STRICT=true APP_ENV=production node scripts/starter-checks.js validate-production-config` |

## Root tooling surfaces

Some root files support local collaboration and review. They are not buyer
content or runtime configuration:

- `.mcp.example.json`: safe developer integration example; replace or remove it
  if the site does not use MCP tooling.
- `semgrep.yml`: site security scan config used by `.github/workflows/**`. It
  is current while the Semgrep CI job exists; remove or rewrite both together
  if the project chooses another security scan.

## Catalog truth

The site is English-only today. The i18n framework stays in place for future
locales. Current Tucsenberg product detail pages and their JSON-LD use
`src/constants/tucsenberg-product-page-*.ts`, assembled by
`src/constants/tucsenberg-product-pages.ts`, as runtime truth.
`src/config/single-site-product-catalog.ts` provides the product list and route
catalog truth.

## Do not edit first

- `src/config/paths/site-config.ts`: runtime/validation facade; edit `src/config/single-site.ts` first.
- `src/constants/product-catalog.ts`: query facade; edit current product page truth in `src/constants/tucsenberg-product-pages.ts` or product list/route catalog in `src/config/single-site-product-catalog.ts` first.
- `src/lib/content-manifest.generated.ts`: generated.
- `src/app/**/page.tsx`: route owner; do not hardcode brand, product facts, or long page copy there.

Regenerate content artifacts with:

```bash
node scripts/starter-checks.js content-manifest
node scripts/starter-checks.js content-manifest --check
```

Minimum default replacement proof:

```bash
pnpm brand:check
pnpm content:check
node scripts/starter-checks.js translations
node scripts/starter-checks.js content-readiness
pnpm exec vitest run tests/architecture/website-config-runtime-boundary.test.ts
```
