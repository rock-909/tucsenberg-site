# Contracts

User-visible behavior contracts. If one changes, update tests and proof in the same branch.

## Navigation

| ID | Promise | Proof |
| --- | --- | --- |
| BC-001 | Tucsenberg Phase 1 pages load and show current page headings. | `tests/e2e/tucsenberg-site-smoke.spec.ts` |
| BC-002 | Header/mobile menu use the current Tucsenberg IA: Products, OEM/Wholesale, Guides, About, RFQ, and Contact. | layout/navigation component tests and `tests/e2e/tucsenberg-site-smoke.spec.ts` |
| BC-003 | Current public site is English-only: `html[lang]` is `en`, Chinese links are not exposed, and `/zh` returns 404. | `tests/e2e/tucsenberg-site-smoke.spec.ts` |
| BC-004 | Root serves the default English site without a locale prefix. | `tests/e2e/tucsenberg-site-smoke.spec.ts` |
| BC-005 | Unknown routes return the current not-found boundary and retired `/zh` routes stay 404. | catch-all route tests, Cloudflare smoke tests, and `tests/e2e/tucsenberg-site-smoke.spec.ts` |
| BC-006 | Mobile menu opens, renders current navigation items, and closes through the component interaction path. | mobile navigation component tests |

## Conversion

| ID | Promise | Proof |
| --- | --- | --- |
| BC-007 | Buyer can submit contact inquiry through `/api/contact`. Local/test-mode proof covers browser form -> `/api/contact` request only; it is not real Turnstile/Airtable/Resend proof. Real lead-service proof stays in post-deploy smoke / real-service canary. | `tests/e2e/contact-form-smoke.spec.ts`, `tests/e2e/smoke/post-deploy-form.spec.ts`, contact route tests |
| BC-008 | Contact form validates required fields and email before submission. Privacy is expressed as a submit-adjacent statement (translation key `contact.form.privacyNotice`), not a consent checkbox: submission is never gated by a privacy tick, and there is no `acceptPrivacy` field. | contact form validation tests, contact route tests, `tests/e2e/contact-form-smoke.spec.ts`, `tests/e2e/no-js-html-contract.spec.ts` |
| BC-009 | Product family handoff opens Contact with validated context on the current catalog site. | `tests/e2e/product-family-contact-handoff.spec.ts`, market/contact route tests |
| BC-010 | Contact form renders in English for the current public locale. | `tests/e2e/tucsenberg-site-smoke.spec.ts`, contact form tests |
| BC-011 | Contact and inquiry enforce rate limiting. | contact route tests, `tests/integration/api/lead-family-protection.test.ts` |
| BC-012 | Contact and inquiry share stable Turnstile errors: required/rejected return 400, unavailable returns 503. | contact route tests, lead-family protection tests, lead Turnstile unit tests |
| BC-012A | Lead delivery for contact and inquiry is parallel owner email + Airtable with either-channel success (email-best-effort); both channels failing returns failure. The Airtable write is bounded by a request budget: a hung CRM cannot block the buyer response beyond the budget — on timeout it degrades to a non-blocking, logged failure and success stays email-driven. | src/lib/lead-pipeline/__tests__/process-lead.test.ts, tests/integration/api/lead-pipeline-real.test.ts |
| BC-013 | Buyer can submit the RFQ page through the product inquiry lead path. The RFQ page sends an explicit **general RFQ** (`productInquiryKind: "general-rfq"`) with no per-product identity; any product-line hint is carried as `buyerInterest` free text, and the buyer message is packaged into the inquiry `requirements`. The page does not pretend to upload files and never claims a product slug/name it did not choose. | `src/app/[locale]/request-quote/__tests__/request-quote-form.test.tsx`, `src/app/[locale]/request-quote/__tests__/request-quote-payload.test.ts`, `/api/inquiry` route tests, real-service canary after deploy |
| BC-013A | A product inquiry is one of three server-defined states, never a client-trusted product slug: **catalog product** (`catalogProductId` validated server-side against the product registry; the display name is resolved from the registry, not the client), **general RFQ** (no per-product identity), and **buyer interest** (`buyerInterest` free text kept for description only, never used as product attribution). A general RFQ that smuggles a `catalogProductId`, or a catalog inquiry whose id is not in the registry, is rejected before lead processing. | `src/app/api/inquiry/__tests__/route.test.ts`, `src/lib/lead-pipeline/__tests__/lead-schema.test.ts` |
| BC-024 | Public lead routes do not require a replay key. Duplicated leads are acceptable starter behavior; dropped leads are not. Contact Server Action compatibility now follows the same no-key behavior. | tests/integration/api/lead-family-protection.test.ts |

`/products/[market]` is part of the current Tucsenberg catalog contract.

### Privacy and consent (owner decision, 2026-07)

Both buyer lead forms (Contact and Request a Quote) express privacy the same
way: a short privacy statement rendered next to the submit button, sourced from
a translation key (`contact.form.privacyNotice` and
`requestQuote.form.privacyNotice`, identical wording), shown through the shared
`FormPrivacyNotice` component. There is no privacy consent checkbox on either
form, and submission is never gated by one — the only submit gate stays the
Turnstile token (plus the contact honeypot and page-freshness guards).

The site does not run marketing email, so there is no marketing-consent field
anywhere: `marketingConsent` has been retired end-to-end (form, zod schemas,
`/api/contact` and `/api/inquiry`, lead pipeline, the Airtable "Marketing
Consent" column write, and the owner email templates). Proof: contact/RFQ form
and payload tests, lead-schema tests, Airtable create-operation tests, email
render tests, and the two E2E specs above.

## Content and SEO

- Public pages expose correct English metadata, canonical, x-default alternate,
  and JSON-LD where applicable.
- `content/pages/{locale}/*.mdx` owns page body and page-level SEO input.
- `src/config/single-site-seo.ts` owns sitemap/robots/crawl policy.
- UI copy belongs to message packs, not MDX.

Proof:

```bash
pnpm content:check
node scripts/starter-checks.js translations
CI=1 pnpm exec playwright test tests/e2e/tucsenberg-site-smoke.spec.ts --project=chromium
```

## Lead pipeline details

Public lead entry points:

- `src/app/api/contact/route.ts` -> `contact`
- `src/app/api/inquiry/route.ts` -> `product` (three server-defined identity states: catalog product / general RFQ / buyer interest — see BC-013A)

They may share `src/lib/lead-pipeline/process-lead.ts`, but each route keeps its own input shape, Turnstile action, validation messages, and response contract.

The inquiry product identity is the source of truth for the owner email and
Airtable record: the display `Product Name` is resolved server-side (catalog
registry label, or a fixed general-RFQ label), `Product Slug` holds the
validated `catalogProductId` and is empty for a general RFQ, and `buyerInterest`
is folded into the description text only.

## Lead pipeline policy

The canonical policy, approved by the owner on 2026-07-07, is
`email-first-storage-optional`, implemented
by `LEAD_DELIVERY_POLICY` in `src/lib/lead-pipeline/process-lead.ts`: contact
and inquiry lead delivery starts owner email and Airtable persistence in
parallel, either channel succeeding is user-facing success, and both channels
failing returns failure. The design record is
`docs/superpowers/specs/2026-07-06-lead-pipeline-parallel-delivery-design.md`.
Rationale: CRM degradation must not lose buyer submissions; Airtable gaps are
backfilled from the owner email using the logged `referenceId`.

The Airtable write is time-boxed by a request budget
(`AIRTABLE_REQUEST_TIMEOUT_MS` in `src/lib/airtable/service.ts`, 8000ms) applied
both at the Airtable SDK (`requestTimeout`) and as an in-process race in
`process-lead.ts`. This keeps `email-first` honest under a hung CRM: a buyer can
never wait minutes for Airtable. A contact or inquiry budget timeout degrades
to a non-blocking, logged failure and success stays email-driven.

Focused proof:

```bash
pnpm exec vitest run tests/integration/api/lead-family-contract.test.ts tests/integration/api/lead-family-protection.test.ts src/app/api/inquiry/__tests__/route.test.ts
```

## Update rule

When changing contact, inquiry, health, behavioral-contract, or smoke-test behavior, update:

1. affected contract text;
2. test path;
3. proof boundary;
4. status in the same branch.

Historical long contract list is available in git history before docs IA simplification.
