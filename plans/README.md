# Implementation Plans

Generated by the improve skill (deep whole-repo quality audit) on 2026-07-01
at commit `338df844`. Plans 001–008 from a previous cycle were intentionally
retired and deleted; numbering continues monotonically at 009. Do not restore
the old plans.

Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 009  | Delete dead filesystem content pipeline (~1,060 lines + 5 test files) | P1 | S | — | DONE (`55bc525e`, reviewed clean) |
| 010  | Delete superseded contact Server Action stack + latent IP-trust trap | P1 | M | — | DONE (`3f0ba75b`, reviewed clean) |
| 013  | Lead-path correctness: closed error union, one Turnstile classifier, real e2e proof | P1 | M | — | DONE (`990bd25a`..`63679528`, reviewed clean) |
| 011  | Single-source the profile→message-pack map; delete frozen migration tool; loud materializer skips | P1 | L | — | DONE (`e0058455`..`67a03772`) |
| 012  | Narrow doc-prose governance checks/tests to machine facts | P2 | M | — | DONE (`c261d5c`) |
| 014  | Prune process-pinning tests and unwired governance scripts (~3,000 lines) | P2 | M | 012 (soft) | DONE (`0815e2b`..`86aa37c`) |

Remaining execution (011 → 012 → 014) is handed off to Codex: see
`plans/CODEX-HANDOFF.md` for the working directory, pre-approved drift
annotations, and finishing gate.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

Notes: branch ready for merge review, full gate green at `4993395`.

## Dependency notes

- All five plans are independent; the order above is by leverage
  (impact ÷ effort, weighted by confidence and risk).
- 009 and 010 are pure deletions — do them first; they also shrink the test
  estate that later plans run against.
- If 010 and 013 run concurrently, coordinate on
  `src/lib/security/__tests__/` (010 trims client-ip tests; 013 does not
  touch them, but both branches edit under `src/app/api` adjacent areas).

## Backlog — confirmed findings worth doing, no plan written yet

Ordered roughly by value. Evidence file:line references were verified by the
advisor at `338df844`.

1. **Draft flag is inert (APP-02)** — `content.json.enableDrafts` is read by
   nothing on the live path; the manifest generator
   (`scripts/starter-checks.js` + `scripts/quality/checks/content-slugs.js:346`)
   only type-checks `draft`, never filters, so a `draft: true` page would
   ship publicly. Fix at manifest-emit time. (S)
2. **Missing content: static pages 500, blog 404s (APP-08)** —
   `src/lib/content-query/queries.ts` throws; `about/page.tsx:29` has no
   guard while `blog/[slug]/page.tsx:44,55` maps to `notFound()`. Pick one
   behavior. (S)
3. **SEO canonical/hreflang double implementation (DUP-03)** —
   `src/lib/seo-metadata.ts:77-102` re-implements
   `src/lib/seo/url-generator.ts:149-176` and `generateMetadataForPath`
   (seo-metadata.ts:330-345) overwrites the url-generator output. Also
   DUP-05: the `createStaticPageSeoDefaults` switch (seo-metadata.ts:163-226)
   duplicates `pages.config.ts` seoKeys with a silent `default`. (M)
4. **Config alias/forwarding layers (ARCH-01/02/03/04)** — 4-hop product
   catalog re-export chain; identity modules `site-facts.ts`,
   `site-definition-builder.ts`, `homepage-section-links.ts`;
   `config/paths.ts` barrel used by 29 files while 40 bypass it; dead
   `getStaticSitemapPages`/`getStaticSitemapPageConfigByPath`/
   `getStaticPageLastmodByPath` in `pages.config.ts` plus a duplicated
   lastmod ISO literal. All S-effort deletions/inlines. (S–M)
5. **Locale literal sprawl (DUP-01)** — `["en","zh"]` / `"zh" ? … : "en"`
   hand-inlined in ~16 files (runtime: `seo-metadata.ts:49-53`,
   `format-published-date.ts`; scripts: `starter-checks.js:125`,
   `content-slugs.js:8`, starter-profile scripts). Partially reduced by Plan
   011 (scripts side). Adding a locale today requires lockstep edits that
   fail silently. (M)
6. **Test-estate slimming (TEST-03/04/05/07)** — mobile-navigation: 10 files
   / ~4,100 lines for a 221-line component, flagship file mocks the state
   machine it claims to test; `resend.test.ts` (797 lines) re-asserts mocks
   already proven at the wire by `resend-http-client.test.ts`; Airtable
   suite ~2,900 lines with no real payload-shape assertion (Plan 013 adds
   one); `a-*-mutation-fast` generated lanes run in the default suite against
   the repo's own stated policy. (M, mostly deletions)
6b. **NARROW trims in mixed architecture tests** (full triage 2026-07-01;
   Plan 014 handles the pure-DELETE set) — ~550-650 cuttable lines inside 11
   files that mix a real boundary check with doc-prose/impl-detail pins:
   `lib-facade-boundary.test.ts:189-248` (route inline-impl substring pins),
   `message-namespace-map.test.ts:74-198` (messages.md prose),
   `website-config-runtime-boundary.test.ts:70-104` (AGENTS/CLAUDE structure
   prose), `email-copy-boundary.test.ts:128-159`,
   `contact-page-boundary.test.ts:65-101` (legacy-export/comment pins),
   `proof-lane-labels.test.ts:24-73`, `page-expression-consumption-boundary
   .test.ts:40-50` (exact importer allowlist), plus small pins in
   storybook-preview-intl, radix-theme-route-footprint,
   real-service-canary-boundary, env-example-parity `:407-430`. Keep each
   file's structural core. (M)
6c. **Check-script narrowing** (triage 2026-07-01) — `brand.js` markers are
   frozen to the previous project's brand (`brand.js:49-58`: "Tianze", "PVC
   conduit"…): re-lifecycle to derive-once, off permanent CI;
   `eslint-disable.js:208-221` re-implements `eslint-comments`
   require-description/disable-enable-pair already error-level in the same
   `lint:check`; `cloudflare-smoke.js` has 3 near-identical request fns and
   `public-preview-smoke` ⊂ `deployed-smoke` (~130-150 line merge);
   `cloudflare-official-compare.js:188-215,240-243` retired flag branches;
   `content-slugs.js` CLI flags exercised only by unit tests. (M)
6d. **production-config wiring GAP (inverse finding — add, don't delete)** —
   `validate-production-config` runs ONLY in the pre-push hook
   (`lefthook.yml:110`); it is absent from CI and from
   `release:verify`/release-proof-manifest, so the production secrets/launch
   gate never runs at the actual deploy gate. Either wire it into
   `release:verify` or accept and document pre-push as its only home. (S)
7. **i18n perf grading engine (DEBT-04)** — `src/lib/i18n/performance.ts`:
   cache hit/miss counters never incremented, A–F grading never read; only
   `recordLoadTime`/`recordError` are live. Narrow to the two counters. (S)
8. **Structured-data 5-file family (DEBT-03/DUP-04)** —
   `generateStructuredData` (structured-data.ts:45-95) has zero production
   callers; `page-structured-data.ts` duplicates the Organization+WebSite
   pair; consumers already import `structured-data-generators.ts` directly
   per the repo's own rule. Collapse to the generators module. (M)
9. **Resend 3-file layer (DEBT-05)** — dead `getEmailStats` (hardcoded
   zeros)/`checkConnection`/`getEmailConfig`; three near-identical send
   methods; files scattered at `src/lib/` top level instead of
   `src/lib/email/`. (M)
10. **Governance layer follow-ups (GOV-04/05/06/07)** — registry stores 3
    derivable fields checked self-referentially (**requires governance
    decision** per project rule); `starter-checks.js` keeps two large checks
    inline (content-manifest, component-governance) contradicting its own
    module-boundary test, which asserts source substrings; fake-phone regex +
    starter-marker vocabulary duplicated between `content-readiness.js:285`
    and `production-config.js:29,143`; content-readiness's ~540-line
    per-profile ownership matrix re-encodes profile membership a 4th time. (M–L)
11. **Message compat files as test-only artifact (PROF-08)** — generated
    `messages/{en,zh}/*.json` serve only Storybook + ~20 test files, not the
    running app; the sync/check machinery exists for a convenience artifact.
    Bigger call; ride on Plan 011. (M, investigate first)
12. **Profile page-add lockstep (PROF-04)** — adding one page to a profile
    touches ~7-9 files across 5 modules; profile definitions could own their
    route/fixture roots. Investigate after Plan 011. (L)
13. **Three markdown renderers for one collection (APP-03/04/07)** — compiled
    MDX, a 328-line hand-rolled `render-static-markdown-content.tsx`, and a
    separate legal renderer + heading extractor; MDX pages also resolve the
    manifest twice and ship the body twice. Consolidate on compiled MDX. (L)
14. **Airtable email field formula-sanitization (CORR-06)** — LOW confidence;
    verify whether the Email field bypasses `sanitizeAirtableTextField`
    (`lead-records.ts:50-76`). Investigate only. (S)
15. **Section container/view/stories quadruplication (ARCH-06)** — stories
    exist for narrative sections that `.claude/rules/ui.md` says should not
    have them; fold views back for static sections. Partly a design-governance
    call. (M)

16. **Merge-review minor follow-ups (2026-07-02, whole-branch review)** —
    (a) `src/lib/i18n/message-pack-config.ts:16-19`: trailing `satisfies` is
    inert after the `as` cast; typo'd pack ids compile silently (covered at
    test time by the graph contract's `existsSync` check). (b)
    `scripts/starter-profile/materialize.ts:83-95`: `listRepoFiles` now derives
    from `git ls-files` — untracked files are intentionally not materialized;
    deserves a one-line comment. (c) `homepage-trust-strip.tsx` lost its
    server-boundary guard when `homepage-visual-absorption-boundary.test.ts`
    was deleted; ~3 lines to add it to `homepage-lcp-motion-boundary.test.ts`.
    (d) not-configured Turnstile surfaces `TURNSTILE_NETWORK_ERROR` (503 is
    correct; code name is a misnomer). All Minor. (S)

## Findings considered and rejected

- **Thin UI wrappers in `src/components/ui/*`** (label = Radix + `cn()`,
  separator = styled div): the wrapper-only import boundary is a documented
  decision in `docs/ref/decisions/ui-foundation.md` — by design, not debt.
- **`src/lib/env.ts` vs `public-runtime-env.ts` overlap**: the server/client
  env split is mandated by the repo's own security rules — deliberate
  boundary.
- **Blog as TS data source, plain-html production emails, generated
  content-manifest/mdx-importers, adapter re-exports from profile-fixtures
  (as a pattern), placeholder/example content**: all documented decisions in
  `docs/use/replace.md` / `docs/ref/**` — settled tradeoffs.
- **`distributed-rate-limit.test.ts` (1,367 lines) as bloat**: examined and
  cleared — it tests real atomicity/failure-mode behavior on a
  security-critical module with fake timers; only minor preset-case overlap
  with `rate-limit-store.test.ts`. Not worth touching.
- **The big fixture-driven script tests as bloat**: examined and cleared —
  `content-readiness-check.test.ts` (1,234), `mdx-slug-sync.test.ts` (927),
  `profile-materialization.test.ts` (534), `cloudflare-smoke.test.ts` (433),
  `validate-production-config.test.ts` (325), `client-boundary-budget.test.ts`
  (317), `release-verify.test.ts` (203) all drive real script logic through
  synthetic fixtures/DI and assert derived behavior. Volume is proportionate
  to the scripts they protect. KEEP.
- **The high-value architecture core**: `env-boundary`, `env-example-parity`,
  `cache-directive-policy`, `email-runtime-boundary`,
  `homepage-lcp-motion-boundary`, `design-token-contract`,
  `profile-materialization-contract`, `profile-fixtures-isolation`,
  `message-pack-graph-contract`, `product-market-slug-contract`,
  `zod-schema-boundary`, `deploy-workflow-contract` — all verified to check
  real structure (AST/imports/derived output). These are the tests the
  starter should keep investing in. KEEP.
- **`component-governance.test.ts` (677) as prose-lock**: examined — its core
  is real AST checking (registry↔source drift, Radix imports escaping the
  wrapper layer); only the retirement-prose block (~:639-676) is a doc-lock.
  Genuine KEEP, subject to the registry-derived-fields narrowing in backlog
  #10. By contrast `ui-component-playbook.test.ts` (129) is pure playbook
  prose pinning and `ui-component-index.test.ts` (140) is largely a
  registry→markdown mirror — both flagged delete-leaning but LOCKED behind
  the project's governance-retirement rule.
- **`getSingleSiteHomeLinkTargets` fallback ladder (ARCH-05)**: the profile
  flexibility is genuinely exercised by the starter's profile tests; a
  narrowing would be mostly cosmetic. Re-evaluate only if profiles shrink.
- **`cf-preview-deployed` / `real-service-canary` proof lanes as unused**:
  verified they ARE referenced by manual proof lanes and e2e smoke — not dead.
- **Deleting the component governance Registry/Playbook**: prohibited by
  project rule without a separate retirement proof; only the narrower
  derived-fields question (backlog #10) is on the table.

## What was NOT audited

- `docs/**` prose quality itself (only its enforcement machinery).
- `src/lib/security/{crypto,ip-parsing,ip-range,turnstile-config}.ts`,
  `src/lib/ops/**`, `src/lib/cloudflare/analytics-*`, `src/hooks/**`,
  `src/lib/airtable/service-internal/**` internals beyond the sampled files.
- E2E spec quality/flakiness (24 Playwright specs) beyond confirming coverage
  exists; no wall-clock/flake measurements were taken (read-only audit).
- `pnpm audit` / dependency posture (needs network).
- en/zh translation VALUE parity (only key-set machinery).
- Cache Components (`"use cache"`/cacheLife/cacheTag) directive correctness.
